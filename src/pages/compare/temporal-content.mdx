---
title: "Windmill vs Temporal: an honest side-by-side comparison"
description: "Evaluating Temporal and Windmill? A neutral, question-driven comparison across target audience, developer experience, durable execution, integrations, migration, performance, and pricing, including where each tool is genuinely the better choice."
---

import React from 'react';
import Link from '@docusaurus/Link';
import { motion } from 'framer-motion';
import {
	Check,
	X,
	Users,
	Terminal,
	Boxes,
	Puzzle,
	ArrowRightLeft,
	Gauge,
	Scale,
	Compass,
	ArrowRight,
} from 'lucide-react';
import GithubStarCount from '../../landing/GithubStarCount';
import HeroCTAButtons from '../../components/products/HeroCTAButtons';
import ProductCTA from '../../components/products/ProductCTA';
import UseCaseFAQ from '../../components/use-cases/UseCaseFAQ';
import CodeBlock from '@theme/CodeBlock';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import { BenchmarkVisualization } from '../../components/BenchmarkVisualization';
import LazyVideo from '../../components/products/LazyVideo';

export const fadeIn = {
	initial: { opacity: 0, y: 30 },
	whileInView: { opacity: 1, y: 0 },
	viewport: { once: true },
	transition: { duration: 0.5 },
};

export function BuildExperienceVideos() {
	const [platform, setPlatform] = React.useState('windmill');
	const [windmillMode, setWindmillMode] = React.useState('as-code');

	const windmillModes = {
		'as-code': {
			src: '/img/platform/workflow-editor/platform-flow-workflow-as-code.webm',
			caption: 'Define the workflow programmatically in Python or TypeScript directly in the Windmill UI, with native try/catch, parallel branches and full language control flow. Author, run and inspect from the same surface, no separate SDK or worker process to deploy.',
		},
		'low-code': {
			src: '/img/platform/workflow-editor/platform-flow-every-step-is-a-script.webm',
			caption: 'Compose steps visually as a DAG with branches, loops and approval steps directly in the Windmill UI. Each step is a real script in any of 20+ languages, edited and triggered in the same surface.',
		},
	};

	const temporalMode = {
		src: '/img/compare/temporal-authoring.webm',
		caption: (
			<>
				A completed ETL workflow viewed in the Temporal UI. The UI is observation-focused: you cannot
				author or trigger workflows there, just inspect results. Each step expands into the underlying
				event history, with state transitions, inputs and outputs recorded for every activity attempt.
				Browse the equivalent monitoring view in Windmill's <Link to="/platform/observability" className="text-blue-500 hover:underline">observability</Link> page.
			</>
		),
	};

	const active = platform === 'windmill' ? windmillModes[windmillMode] : temporalMode;

	return (
		<motion.div {...fadeIn} className="mb-20">
			<div className="flex w-fit mx-auto gap-1 rounded-xl border border-gray-200 dark:border-gray-700 bg-gray-50 dark:bg-gray-900/60 p-1 mb-4">
				<button
					type="button"
					onClick={() => setPlatform('windmill')}
					className={`inline-flex items-center gap-2 px-4 py-2 text-sm font-medium rounded-lg cursor-pointer transition-all ${
						platform === 'windmill'
							? 'bg-white dark:bg-gray-800 text-blue-600 dark:text-blue-400 shadow-sm'
							: 'text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:hover:text-gray-200'
					}`}
					style={{ border: 'none' }}
				>
					<img src="/img/windmill.svg" alt="" className="w-4 h-4" />
					Windmill
				</button>
				<button
					type="button"
					onClick={() => setPlatform('temporal')}
					className={`inline-flex items-center gap-2 px-4 py-2 text-sm font-medium rounded-lg cursor-pointer transition-all ${
						platform === 'temporal'
							? 'bg-white dark:bg-gray-800 text-gray-900 dark:text-white shadow-sm'
							: 'text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:hover:text-gray-200'
					}`}
					style={{ border: 'none' }}
				>
					<img src="/img/temporal.png" alt="" className="w-4 h-4 rounded" />
					Temporal
				</button>
			</div>

			{platform === 'windmill' && (
				<div className="flex gap-6 mb-4 border-b border-gray-200 dark:border-gray-800">
					<button
						type="button"
						onClick={() => setWindmillMode('as-code')}
						className={`pb-2 px-1 text-sm font-medium cursor-pointer border-b-2 -mb-px transition-colors ${
							windmillMode === 'as-code'
								? 'border-blue-500 text-blue-600 dark:text-blue-400'
								: 'border-transparent text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:hover:text-gray-200'
						}`}
						style={{ background: 'transparent' }}
					>
						Workflow as code
					</button>
					<button
						type="button"
						onClick={() => setWindmillMode('low-code')}
						className={`pb-2 px-1 text-sm font-medium cursor-pointer border-b-2 -mb-px transition-colors ${
							windmillMode === 'low-code'
								? 'border-blue-500 text-blue-600 dark:text-blue-400'
								: 'border-transparent text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:hover:text-gray-200'
						}`}
						style={{ background: 'transparent' }}
					>
						Low-code DAG
					</button>
				</div>
			)}

			{active.placeholder ? (
				<div className="rounded-2xl border border-dashed border-gray-300 dark:border-gray-700 bg-gray-50 dark:bg-gray-900/60 w-full aspect-video flex flex-col items-center justify-center gap-3 text-center px-6">
					<img src="/img/temporal.png" alt="Temporal" className="w-12 h-12 opacity-70 rounded" />
					<span className="text-sm font-medium text-gray-500 dark:text-gray-400">Temporal walkthrough video coming soon</span>
				</div>
			) : (
				<LazyVideo
					key={active.src}
					className="rounded-2xl border border-gray-200 dark:border-gray-700 w-full"
					src={active.src}
				/>
			)}
			<p className="text-base text-gray-700 dark:text-gray-300 mt-4 text-center max-w-2xl mx-auto leading-relaxed">{active.caption}</p>
		</motion.div>
	);
}

export function IntegrationsVideos() {
	const [platform, setPlatform] = React.useState('windmill');

	{/* TODO: capture a Temporal integrations video (e.g. an activity calling a vendor SDK + the Temporal UI showing the run) and save as /static/img/compare/temporal-integrations.webm */}
	const modes = {
		windmill: {
			src: '/img/platform/script-editor/platform-script-real-dependencies.webm',
			caption: 'Type `import stripe` in the script editor and run. Windmill detects the import, resolves the version and pins it in a per-script lockfile automatically. No venv, no pip install, no restart: the platform manages dependencies for you.',
		},
		temporal: {
			src: '/img/compare/temporal-integrations.webm',
			caption: 'Pip install any PyPI package (here, the Stripe SDK) into the worker venv and call it from an activity. No connector catalog, no plugin layer, no determinism rules inside activities. Each activity is a normal function in your SDK language.',
		},
	};

	const active = modes[platform];

	return (
		<motion.div {...fadeIn} className="mb-20">
			<div className="flex w-fit mx-auto gap-1 rounded-xl border border-gray-200 dark:border-gray-700 bg-gray-50 dark:bg-gray-900/60 p-1 mb-4">
				<button
					type="button"
					onClick={() => setPlatform('windmill')}
					className={`inline-flex items-center gap-2 px-4 py-2 text-sm font-medium rounded-lg cursor-pointer transition-all ${
						platform === 'windmill'
							? 'bg-white dark:bg-gray-800 text-blue-600 dark:text-blue-400 shadow-sm'
							: 'text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:hover:text-gray-200'
					}`}
					style={{ border: 'none' }}
				>
					<img src="/img/windmill.svg" alt="" className="w-4 h-4" />
					Windmill
				</button>
				<button
					type="button"
					onClick={() => setPlatform('temporal')}
					className={`inline-flex items-center gap-2 px-4 py-2 text-sm font-medium rounded-lg cursor-pointer transition-all ${
						platform === 'temporal'
							? 'bg-white dark:bg-gray-800 text-gray-900 dark:text-white shadow-sm'
							: 'text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:hover:text-gray-200'
					}`}
					style={{ border: 'none' }}
				>
					<img src="/img/temporal.png" alt="" className="w-4 h-4 rounded" />
					Temporal
				</button>
			</div>

			{active.placeholder ? (
				<div className="rounded-2xl border border-dashed border-gray-300 dark:border-gray-700 bg-gray-50 dark:bg-gray-900/60 w-full aspect-video flex flex-col items-center justify-center gap-3 text-center px-6">
					<img src="/img/temporal.png" alt="Temporal" className="w-12 h-12 opacity-70 rounded" />
					<span className="text-sm font-medium text-gray-500 dark:text-gray-400">Temporal walkthrough video coming soon</span>
				</div>
			) : (
				<LazyVideo
					key={active.src}
					className="rounded-2xl border border-gray-200 dark:border-gray-700 w-full"
					src={active.src}
				/>
			)}
			<p className="text-base text-gray-700 dark:text-gray-300 mt-4 text-center max-w-2xl mx-auto leading-relaxed">{active.caption}</p>
		</motion.div>
	);
}

{/* ── The 8 big questions ── */}

export const bigQuestions = [
	{ id: 'build',        kicker: 'What you can build',     short: 'Which solution fits for your specific use case', icon: Boxes },
	{ id: 'target',       kicker: 'Target',                 short: 'Who the platform is built for',                          icon: Users },
	{ id: 'devex',        kicker: 'Build experience',   short: 'How you build on each platform',              icon: Terminal },
	{ id: 'integrations', kicker: 'Integrations',           short: 'How the platform integrates with your existing stack',   icon: Puzzle },
	{ id: 'migration',    kicker: 'Migration & lock-in',    short: 'How hard to get in, how hard to get out',                icon: ArrowRightLeft },
	{ id: 'enterprise',   kicker: 'Enterprise requirements', short: 'Audit logs, observability, security, performance',      icon: Gauge },
	{ id: 'licensing',    kicker: 'Licensing & pricing',    short: 'Open source, pricing, self-hosting',                     icon: Scale },
	{ id: 'verdict',      kicker: 'Verdict',                short: 'The verdict',                                            icon: Compass },
];

export function EnterpriseBadge() {
	return (
		<span className="inline-flex items-center rounded bg-amber-100 dark:bg-amber-900/40 text-amber-700 dark:text-amber-400 text-[10px] font-semibold uppercase tracking-wide px-1.5 py-0.5 mx-1 align-middle whitespace-nowrap">
			Enterprise only
		</span>
	);
}

export function CloudBadge() {
	return (
		<span className="inline-flex items-center rounded bg-amber-100 dark:bg-amber-900/40 text-amber-700 dark:text-amber-400 text-[10px] font-semibold uppercase tracking-wide px-1.5 py-0.5 mx-1 align-middle whitespace-nowrap">
			Temporal Cloud only
		</span>
	);
}

export function CheckCell({ value }) {
	if (value === true) {
		return <Check className="w-5 h-5 text-emerald-500 mx-auto" />;
	}
	if (value === false) {
		return <X className="w-5 h-5 text-gray-400 mx-auto" />;
	}
	if (value === 'Enterprise') {
		return (
			<div className="flex flex-col items-center gap-1">
				<Check className="w-5 h-5 text-emerald-500" />
				<EnterpriseBadge />
			</div>
		);
	}
	if (value === 'Cloud') {
		return (
			<div className="flex flex-col items-center gap-1">
				<Check className="w-5 h-5 text-emerald-500" />
				<CloudBadge />
			</div>
		);
	}
	return <span className="text-sm text-amber-600 dark:text-amber-400 font-medium">{value}</span>;
}

{/* ── What you can build — row data ── */}

export const buildRows = [
	{ feature: 'Durable workflows',       href: '/docs/core_concepts/workflows_as_code', description: 'Long-running workflows that survive process crashes via checkpointing or event replay',                              windmill: true, temporal: true },
	{ feature: 'Workflows',               href: '/use-cases/workflows',         description: 'Chain scripts into flows with branching, retries and approval steps',                windmill: true, temporal: true },
	{ feature: 'Scripts',                 href: '/use-cases/scripts',           description: 'Standalone functions exposed as APIs, webhooks or cron jobs without a wrapping workflow', windmill: true, temporal: false },
	{ feature: 'AI agent workflows',      href: '/use-cases/ai-agents',         description: 'Build agents that call tools, branch on outputs and run as workflows',                windmill: true, temporal: true },
	{ feature: 'AI sandboxes & volumes',  href: '/platform/sandboxes',          description: 'Isolated environments with persistent volumes for running agents',                   windmill: true, temporal: false },
	{ feature: 'Data pipelines',          href: '/use-cases/data-pipelines',    description: 'ETL, syncs and scheduled data jobs with parallel branches',                          windmill: true, temporal: 'Partial' },
	{ feature: 'Low-code internal apps',  href: '/use-cases/internal-apps',     description: 'Drag-and-drop dashboards and admin tools with built-in components',                  windmill: true, temporal: false },
	{ feature: 'Full-code internal apps', href: '/platform/app-builder',        description: 'Custom dashboards and admin tools built in React or Svelte',                         windmill: true, temporal: false },
	{ feature: 'Scheduled tasks',         href: '/use-cases/scheduled-tasks',   description: 'Cron jobs with retries, error handling and alerting',                                windmill: true, temporal: true },
];

{/* ── FAQ ── */}

export const faqs = [
	{
		question: 'Is Windmill a drop-in replacement for Temporal?',
		textAnswer:
			'For the durable-execution use case the patterns transfer: workflows compose activities, retries are first-class, signals and human-in-the-loop steps are supported on both. The architectural models differ. Temporal records every workflow decision in an immutable event history and replays the workflow function from the beginning to recover state. Windmill workflow-as-code uses a mutable JSONB checkpoint with two step primitives (task() dispatches a child job, step() runs inline). Porting requires rewriting workflow code against Windmill\'s SDK, but activity bodies port cleanly as plain functions.',
	},
	{
		question: 'Can I migrate Temporal workflows to Windmill automatically?',
		textAnswer:
			'No, there is no automated Temporal-to-Windmill converter. In practice, activity bodies port as standard code in the same language; the workflow code itself is rewritten against Windmill\'s task() and step() primitives. Temporal-specific features (signals, queries, child workflows, search attributes, sticky execution) need to be reimplemented using Windmill\'s equivalents (suspend/resume steps, parameter inputs, sub-flows, custom resource types).',
	},
	{
		question: 'Does Windmill require workflows to be deterministic like Temporal does?',
		textAnswer:
			'No. Temporal\'s replay model requires workflow code to be deterministic, with sandboxed execution in TS and Python and convention-only enforcement in Go and Java. Windmill workflow-as-code stores task results in a JSONB checkpoint rather than replaying commands, so calls to Math.random(), Date.now() and other non-deterministic APIs work normally. The trade-off: Temporal\'s event history gives you a complete audit trail and replay-from-anywhere; Windmill\'s checkpoint gives you smaller writes and no determinism rules.',
	},
	{
		question: 'How does performance actually compare?',
		textAnswer:
			'We publish reproducible benchmarks covering Airflow, Prefect, Temporal, Kestra, Hatchet and Windmill. The result is mixed: Temporal is faster on long sequential workflows (per-step overhead is lower thanks to its History service), while Windmill is faster on parallel and fan-out workloads where dispatch overhead dominates. Both run lightweight Python tasks at sub-100ms per step on equivalent hardware. For heavy-duty activities running for seconds or minutes, the orchestration overhead is a rounding error on either side.',
	},
	{
		question: 'Which has more language support?',
		textAnswer:
			'Temporal ships first-party SDKs for TypeScript, Go, Java, Python, .NET, PHP and Ruby; activities run in the SDK\'s host language. Windmill supports 20+ languages natively (TypeScript on Deno, Bun and Node; Python; Go; Bash; SQL; PowerShell; PHP; Rust) for both scripts and step bodies, with any npm, PyPI, Go or Maven package as a first-class import. Different surface: Temporal is one codebase per worker, Windmill is one codebase per script with shared resources.',
	},
	{
		question: 'What about the licenses, MIT vs AGPLv3?',
		textAnswer:
			'Temporal\'s self-hosted core is MIT-licensed, the most permissive option. Windmill\'s OSS core is AGPLv3, which requires source availability for network-exposed modifications. Windmill ships Enterprise features in a separate proprietary codebase you can self-host on contract; Temporal\'s commercial features (multi-region, advanced support, higher SLAs) live exclusively in Temporal Cloud. If license permissiveness of the core is a hard constraint and self-hosting all features matters, Temporal\'s OSS wins this axis.',
	},
	{
		question: 'Is pricing public?',
		textAnswer:
			'Yes for both, with different shapes. Temporal Cloud is action-metered: Essentials $100/month (1M actions), Business $500/month (2.5M actions), Enterprise custom (10M+), with storage charged separately. Windmill publishes per-seat and per-worker Enterprise pricing on its pricing page, starting at $120/month, with the OSS core free to self-host at any scale.',
	},
];

{/* ── Section wrapper helper ── */}

export function QASection({ id, kicker, question, summary, temporal, windmill, subsections, bottomLine, afterHero, children, reverse }) {
	return (
		<section id={id} className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-20 scroll-mt-24">
			{/* Hero: title + subtitle */}
			<motion.div {...fadeIn} className="mb-12">
				<div className="text-sm font-semibold uppercase tracking-wider text-blue-600 dark:text-blue-400 mb-3">
					{kicker}
				</div>
				<h2 className="text-3xl sm:text-4xl font-bold text-gray-900 dark:text-white mb-5">
					{question}
				</h2>
				{bottomLine && (
					<p className="text-lg text-gray-600 dark:text-gray-300 leading-relaxed">
						{bottomLine}
					</p>
				)}
			</motion.div>

			{afterHero}

			{/* Details: subsections (preferred) or fallback 2-card layout */}
			{subsections && subsections.length > 0 && (
				<div className="divide-y divide-gray-200 dark:divide-gray-800">
					{/* Shared column headers — desktop only */}
					<div className="hidden md:grid md:grid-cols-[minmax(0,1fr)_minmax(0,2fr)_minmax(0,2fr)] md:gap-8 pb-4">
						<div />
						<div className="flex items-center gap-2">
							<img src="/img/windmill.svg" alt="Windmill" className="w-5 h-5" />
							<span className="text-base font-bold text-blue-600 dark:text-blue-400">Windmill</span>
						</div>
						<div className="flex items-center gap-2">
							<img src="/img/temporal.png" alt="Temporal" className="w-5 h-5 rounded" />
							<span className="text-base font-bold text-gray-900 dark:text-white">Temporal</span>
						</div>
					</div>

					{subsections.map((sub, i) => (
						<motion.div
							key={sub.label || i}
							{...fadeIn}
							className="py-6 md:grid md:grid-cols-[minmax(0,1fr)_minmax(0,2fr)_minmax(0,2fr)] md:gap-8 md:items-start"
						>
							{sub.label && (
								<h3 className="text-base font-semibold text-gray-900 dark:text-white mb-4 md:mb-0">
									{sub.label}
								</h3>
							)}
							<div className="mb-4 md:mb-0">
								<div className="md:hidden mb-2 flex items-center gap-2">
									<img src="/img/windmill.svg" alt="Windmill" className="w-4 h-4" />
									<span className="text-sm font-bold text-blue-600 dark:text-blue-400">Windmill</span>
								</div>
								<div className="text-sm text-gray-700 dark:text-gray-200 leading-relaxed space-y-2">
									{sub.windmill}
								</div>
							</div>
							<div>
								<div className="md:hidden mb-2 flex items-center gap-2">
									<img src="/img/temporal.png" alt="Temporal" className="w-4 h-4 rounded" />
									<span className="text-sm font-bold text-gray-900 dark:text-white">Temporal</span>
								</div>
								<div className="text-sm text-gray-700 dark:text-gray-200 leading-relaxed space-y-2">
									{sub.temporal}
								</div>
							</div>
						</motion.div>
					))}
				</div>
			)}

			{!subsections && (windmill || temporal) && (
				<div className="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8">
					<motion.div
						{...fadeIn}
						className="rounded-2xl border border-blue-200 dark:border-blue-800 bg-white dark:bg-gray-900 p-6 shadow-sm"
					>
						<div className="mb-4 flex items-center gap-2">
							<img src="/img/windmill.svg" alt="Windmill" className="w-5 h-5" />
							<span className="text-base font-bold text-blue-600 dark:text-blue-400">Windmill</span>
						</div>
						<div className="text-gray-700 dark:text-gray-200 leading-relaxed space-y-3">
							{windmill}
						</div>
					</motion.div>
					<motion.div
						{...fadeIn}
						className="rounded-2xl border border-gray-200 dark:border-gray-700 bg-gray-50 dark:bg-gray-900/60 p-6"
					>
						<div className="mb-4 flex items-center gap-2">
							<img src="/img/temporal.png" alt="Temporal" className="w-5 h-5 rounded" />
							<span className="text-base font-bold text-gray-900 dark:text-white">Temporal</span>
						</div>
						<div className="text-gray-700 dark:text-gray-200 leading-relaxed space-y-3">
							{temporal}
						</div>
					</motion.div>
				</div>
			)}

			{children}
		</section>
	);
}

export default function TemporalCompareContent({ title, description }) {
	return (
		<div className="pt-32 max-w-full">
			{/* ── Hero ── */}
			<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 pt-16 pb-8">
				<div className="grid grid-cols-1 md:grid-cols-2 gap-12 items-center">
					<motion.div {...fadeIn}>
						<div className="mb-4"><GithubStarCount /></div>
						<h1 className="!text-4xl sm:!text-6xl !font-semibold !tracking-tight text-gray-900 dark:text-white mb-6">
							Windmill vs Temporal
						</h1>
						<p className="text-xl text-gray-600 dark:text-gray-100 mb-8">
							Eight questions teams ask during a bake-off, with an honest answer on each one
							for where Windmill or Temporal is the better pick.
						</p>
						<HeroCTAButtons />
					</motion.div>

					<div className="hidden md:flex items-center justify-center">
						<div className="flex items-center gap-8">
							<motion.div
								initial={{ opacity: 0, x: -30 }}
								whileInView={{ opacity: 1, x: 0 }}
								viewport={{ once: true }}
								transition={{ duration: 0.5 }}
								className="flex items-center justify-center w-40 h-40 rounded-3xl border border-blue-200 dark:border-blue-800 bg-white dark:bg-gray-900 shadow-md"
							>
								<motion.img
									src="/img/windmill.svg"
									alt="Windmill"
									className="w-20 h-20"
									animate={{ rotate: 360 }}
									transition={{ duration: 6, repeat: Infinity, ease: 'linear' }}
								/>
							</motion.div>
							<motion.div
								initial={{ opacity: 0, scale: 0.5 }}
								whileInView={{ opacity: 1, scale: 1 }}
								viewport={{ once: true }}
								transition={{ duration: 0.4, delay: 0.2 }}
								className="text-xl font-semibold text-gray-400 dark:text-gray-500"
							>
								vs
							</motion.div>
							<motion.div
								initial={{ opacity: 0, x: 30 }}
								whileInView={{ opacity: 1, x: 0 }}
								viewport={{ once: true }}
								transition={{ duration: 0.5, delay: 0.1 }}
								className="flex items-center justify-center w-40 h-40 rounded-3xl border border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-900 shadow-sm"
							>
								<img src="/img/temporal.png" alt="Temporal" className="w-20 h-20 rounded" />
							</motion.div>
						</div>
					</div>
				</div>
			</div>

			{/* ── Jump-to navigation (table of contents) ── */}
			<section className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 pt-10 pb-4">
				<motion.div {...fadeIn} className="rounded-2xl border border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-900/60 p-6">
					<div className="text-xs font-semibold uppercase tracking-wider text-gray-500 dark:text-gray-400 mb-4">
						The 8 questions
					</div>
					<ol className="divide-y divide-gray-100 dark:divide-gray-800">
						{bigQuestions.map((q, i) => (
							<li key={q.id}>
								<a
									href={`#${q.id}`}
									className="group flex items-baseline gap-4 py-3 !no-underline hover:text-blue-600 dark:hover:text-blue-400"
								>
									<span className="text-xs font-mono font-semibold text-gray-400 dark:text-gray-500 w-6 shrink-0">
										{String(i + 1).padStart(2, '0')}
									</span>
									<span className="text-sm font-medium text-gray-500 dark:text-gray-400 w-48 shrink-0 whitespace-nowrap">
										{q.kicker}
									</span>
									<span className="text-base text-gray-900 dark:text-white group-hover:text-blue-600 dark:group-hover:text-blue-400 flex-1 whitespace-nowrap">
										{q.short}
									</span>
									<ArrowRight className="w-4 h-4 text-gray-300 dark:text-gray-600 group-hover:text-blue-500 group-hover:translate-x-1 transition-all shrink-0" />
								</a>
							</li>
						))}
					</ol>
				</motion.div>
			</section>


			{/* ── Quick take ── */}
			<section className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-20">
				<motion.div {...fadeIn} className="grid grid-cols-1 md:grid-cols-2 gap-6">
					<div className="rounded-2xl border border-blue-200 dark:border-blue-800 bg-white dark:bg-gray-900 p-6 shadow-sm">
						<div className="mb-3 flex items-center gap-2">
							<img src="/img/windmill.svg" alt="Windmill" className="w-5 h-5" />
							<span className="text-base font-bold text-blue-600 dark:text-blue-400">
								Windmill in one sentence
							</span>
						</div>
						<p className="text-base text-gray-700 dark:text-gray-200 leading-relaxed">
							An open-source platform to <strong>build and orchestrate all your internal software</strong>:
							<Link to="/use-cases/scripts" className="text-blue-500 hover:underline"> scripts</Link>,{' '}
							<Link to="/use-cases/workflows" className="text-blue-500 hover:underline">workflows</Link>,{' '}
							<Link to="/use-cases/data-pipelines" className="text-blue-500 hover:underline">data pipelines</Link>,{' '}
							<Link to="/use-cases/ai-agents" className="text-blue-500 hover:underline">AI agents</Link> and{' '}
							<Link to="/use-cases/internal-apps" className="text-blue-500 hover:underline">internal apps</Link>. Made for engineers who
							want full code flexibility and <Link to="/platform/local-dev" className="text-blue-500 hover:underline">local dev experience</Link>, both to build internal software and to manage the underlying
							infrastructure.
						</p>
					</div>
					<div className="rounded-2xl border border-gray-200 dark:border-gray-700 bg-gray-50 dark:bg-gray-900/60 p-6">
						<div className="mb-3 flex items-center gap-2">
							<img src="/img/temporal.png" alt="Temporal" className="w-5 h-5 rounded" />
							<span className="text-base font-bold text-gray-900 dark:text-white">
								Temporal in one sentence
							</span>
						</div>
						<p className="text-base text-gray-700 dark:text-gray-200 leading-relaxed">
							An open-source <strong>durable execution engine</strong> for long-running application
							workflows. Records every workflow decision in an immutable <a href="https://docs.temporal.io/encyclopedia/event-history" target="_blank" rel="noopener noreferrer" className="text-blue-500 hover:underline">event history</a> and replays
							the <a href="https://docs.temporal.io/workflows" target="_blank" rel="noopener noreferrer" className="text-blue-500 hover:underline">workflow function</a> to recover state after a crash. Best for engineering teams building
							mission-critical multi-step logic: payment flows, sagas, signup orchestration, AI agents.
						</p>
					</div>
				</motion.div>
			</section>

			{/* ── Q1: Scope ── */}
			<QASection
				id="build"
				kicker="01 · What you can build"
				question="Which internal software can you build and orchestrate?"
				summary="Both platforms run durable workflows. The real question is what else you can build on the same runtime."
				bottomLine={
					<>
						<strong>Windmill</strong> covers durable workflows on the same platform that runs scripts, data pipelines, AI agents and both low-code and full-code internal apps, with shared auth, secrets and observability.
						<strong> Temporal</strong> is laser-focused on durable workflow execution. The other primitives (apps, scripts as standalone APIs, data pipelines, low-code agents) sit outside its scope, so you bring your own stack for those.
					</>
				}
			>
				<motion.div {...fadeIn} className="overflow-hidden rounded-2xl border border-gray-200 dark:border-gray-700">
					<table className="w-full text-sm !table !mb-0 !border-collapse [&_th]:!border-0 [&_td]:!border-0 [&_tr]:!border-0 [&_tbody]:!border-0">
						<thead className="bg-gray-50 dark:bg-gray-800/60">
							<tr>
								<th className="px-6 py-4 text-left font-semibold text-gray-900 dark:text-white">Primitive</th>
								<th className="px-6 py-4 text-center font-semibold text-blue-600 dark:text-blue-400 w-32">
									<span className="inline-flex items-center gap-2">
										<img src="/img/windmill.svg" alt="Windmill" className="w-4 h-4" />
										Windmill
									</span>
								</th>
								<th className="px-6 py-4 text-center font-semibold text-gray-700 dark:text-gray-300 w-32">
									<span className="inline-flex items-center gap-2">
										<img src="/img/temporal.png" alt="Temporal" className="w-4 h-4 rounded" />
										Temporal
									</span>
								</th>
							</tr>
						</thead>
						<tbody className="divide-y divide-gray-100 dark:divide-gray-800 bg-white dark:bg-gray-900">
							{buildRows.map((row) => (
								<tr key={row.feature}>
									<td className="px-6 py-4">
										<div className="font-semibold text-gray-900 dark:text-white">
											{row.href ? (
												<Link to={row.href} className="text-gray-900 dark:text-white hover:text-blue-600 dark:hover:text-blue-400">
													{row.feature}
												</Link>
											) : (
												row.feature
											)}
										</div>
										{row.description && (
											<div className="text-sm text-gray-500 dark:text-gray-400 mt-0.5">{row.description}</div>
										)}
									</td>
									<td className="px-6 py-4 text-center align-top pt-5"><CheckCell value={row.windmill} /></td>
									<td className="px-6 py-4 text-center align-top pt-5"><CheckCell value={row.temporal} /></td>
								</tr>
							))}
						</tbody>
					</table>
				</motion.div>
			</QASection>

			{/* ── Q2: Audience ── */}
			<QASection
				id="target"
				kicker="02 · Target"
				question="Who is each platform built for?"
				summary="Both tools target backend engineers, but the workload shape they assume is different."
				subsections={[
					{
						label: 'Primary audience',
						windmill: (
							<p>
								Developer-led teams who want one platform for orchestration, scripts, internal apps,
								data pipelines and AI agents. Engineers own the workspace end-to-end: code in 20+
								languages, Git, local dev, code review, CI/CD, AI coding tools and infrastructure
								as code.
							</p>
						),
						temporal: (
							<p>
								Backend engineering teams building distributed systems where survival across crashes
								is the primary requirement: payment flows, sagas, multi-step user signup, long-running
								provisioning, AI agent runtimes. Strong adoption in Go and Java shops historically
								(ex-Cadence DNA), with TypeScript and Python now first-class.
							</p>
						),
					},
					{
						label: 'Workload shape',
						windmill: (
							<p>
								Mixed. Quick sub-second scripts, multi-minute data jobs, long-running flows with
								approval steps, scheduled cron, event-driven triggers, app backends. The same
								platform handles all of them.
							</p>
						),
						temporal: (
							<p>
								Long-running multi-step application logic where each step is itself substantive
								(an HTTP call, a payment API, a file transfer). Less suited to high-volume short
								tasks where the per-step event-history overhead dominates.
							</p>
						),
					},
					{
						label: 'Languages supported',
						windmill: (
							<p>
								TypeScript (Deno, Bun, Node), Python, Go, Bash, SQL, PowerShell, PHP, Rust, C# and
								any Docker image, all first-class for both scripts and flow steps.
							</p>
						),
						temporal: (
							<p>
								First-party SDKs for TypeScript, Python, Go, Java, .NET, PHP and Ruby. Workflows
								and activities are written in the SDK host language; activities are the escape
								hatch for any other language via process spawn.
							</p>
						),
					},
				]}
				bottomLine={
					<>
						<strong>Windmill</strong> is built for engineering teams who want orchestration plus the wider runtime (apps, scripts, agents, pipelines) on one platform.
						<strong> Temporal</strong> is built for engineering teams whose primary need is durable, long-running multi-step application logic across distributed systems.
					</>
				}
			/>

			{/* ── Q3: Authoring model ── */}
			<QASection
				reverse
				id="devex"
				kicker="03 · Build experience"
				question="How do you build on each platform?"
				summary="Covers how you write workflows, the local-dev loop, IDE and AI tooling, and how much of the platform itself is manageable from Git."
				afterHero={<BuildExperienceVideos />}
				subsections={[
					{
						label: 'Authoring',
						windmill: (
							<p>
								Build scripts in 20+ languages in a dedicated <Link to="/platform/script-editor" className="text-blue-500 hover:underline">script editor</Link>. Each script is a standalone
								primitive with its own UI, API endpoint and scheduler; no Python wrapper required to
								run. <Link to="/platform/flow-editor" className="text-blue-500 hover:underline">Flows</Link> glue scripts together through a visual editor backed by YAML. Any
								public or private package is a first-class import with automatic dependency
								resolution.
							</p>
						),
						temporal: (
							<p>
								<a href="https://docs.temporal.io/workflows" target="_blank" rel="noopener noreferrer" className="text-blue-500 hover:underline">Workflows</a> and <a href="https://docs.temporal.io/activities" target="_blank" rel="noopener noreferrer" className="text-blue-500 hover:underline">activities</a> are written using one of seven <a href="https://docs.temporal.io/develop" target="_blank" rel="noopener noreferrer" className="text-blue-500 hover:underline">SDKs</a> (TypeScript,
								Python, Go, Java, .NET, PHP, Ruby), each with its own decorator or annotation pattern
								(e.g. <code>@workflow.defn</code> + <code>@activity.defn</code> in Python). The strict
								<strong> workflow / activity split</strong>: workflows must be deterministic and orchestrate;
								activities run normal code with side effects. Determinism is sandboxed in TS and Python,
								convention-only in Go and Java.
							</p>
						),
					},
					{
						label: 'Execution model',
						windmill: (
							<>
								<p>
									Two ways to define workflows, one runtime under both. The <Link to="/platform/flow-editor" className="text-blue-500 hover:underline">visual flow builder</Link>
									produces JSON DAGs with multi-language steps, transparent data passing via{' '}
									<code>results.step_name</code> expressions, and built-in branches, loops, error
									handlers and approval steps. <Link to="/docs/core_concepts/workflows_as_code" className="text-blue-500 hover:underline">Workflow-as-code</Link> defines the same flow
									programmatically in Python or TypeScript.
								</p>
								<p>
									In the workflow-as-code engine, two step primitives carry different cost profiles.
									<code> task()</code> dispatches a child job in a separate process and surfaces as
									its own job in the UI, for steps that need isolation or independent retries.
									<code> step()</code> runs inline in the same process and checkpoints its result to
									Postgres in roughly 0.5ms, for cheap units like a calculation, a CSV parse or a
									small DB query. Each step picks the cost it actually needs.
								</p>
							</>
						),
						temporal: (
							<>
								<p>
									Event sourcing plus deterministic replay. Every workflow decision (schedule activity,
									start timer, send signal) is recorded as an event in an immutable <a href="https://docs.temporal.io/encyclopedia/event-history" target="_blank" rel="noopener noreferrer" className="text-blue-500 hover:underline">event history</a> in
									Postgres, MySQL or Cassandra. When the workflow resumes after a crash, the SDK replays
									the workflow function from the beginning, returning each previous step's result from
									the event history until it reaches the next decision.
								</p>
								<p>
									This gives true <a href="https://docs.temporal.io/temporal" target="_blank" rel="noopener noreferrer" className="text-blue-500 hover:underline">durable execution</a> (workflows can run for months and survive any
									crash) and a complete audit trail. The trade-off: workflow code must be deterministic
									(no <code>Math.random()</code>, no direct clock access, no I/O) and a 3-step workflow
									generates around 23 events. Activities run in normal code and are unrestricted.
								</p>
							</>
						),
					},
					{
						label: 'Local dev & IDE',
						windmill: (
							<p>
								Run scripts locally with the <Link to="/platform/local-dev" className="text-blue-500 hover:underline">Windmill CLI</Link>. VS Code extension, native language tooling
								(LSP, type-checking, linting) and AI coding tools (Claude Code, Codex).
							</p>
						),
						temporal: (
							<p>
								Standard tooling for whichever SDK language you pick: VS Code or any IDE, language-native
								type-checking and linting, and the <a href="https://docs.temporal.io/cli" target="_blank" rel="noopener noreferrer" className="text-blue-500 hover:underline">Temporal CLI</a> plus a local <code>temporal server
								start-dev</code> for an in-memory dev cluster. Workflows can be unit-tested with the SDK
								test framework that mocks time and activities.
							</p>
						),
					},
					{
						label: 'Resources & secrets',
						windmill: (
							<p>
								<Link to="/docs/core_concepts/resources_and_types" className="text-blue-500 hover:underline">Resources</Link> (typed JSON for credentials, connection info, configs)
								and <Link to="/docs/core_concepts/variables_and_secrets" className="text-blue-500 hover:underline">Variables</Link> (individual secrets) are first-class and reusable
								across scripts, flows and apps. They're encrypted at rest, scoped via folders and
								groups for access control, and versioned in Git. External secret backends (Vault, AWS
								Secrets Manager) are <EnterpriseBadge />.
							</p>
						),
						temporal: (
							<p>
								No first-class resource concept. Activities receive typed inputs and read credentials
								from environment variables, the worker process configuration or whatever secret
								manager you wire in. Temporal Cloud has Namespaces with API key auth, but
								connection details for downstream services are handled by your code, not the platform.
							</p>
						),
					},
					{
						label: 'Git & CI',
						windmill: (
							<p>
								Full IaC: scripts, flows, apps, resources, variables, secrets, schedules, folders,
								groups and permissions are all files in Git, deployed via the CLI and <Link to="/platform/deployment-versioning" className="text-blue-500 hover:underline">Git sync</Link>. Internal tooling runs like
								application code: PRs, reviews, rollbacks, CI/CD. Git sync is free for up to 2 users;
								beyond that is <EnterpriseBadge />.
							</p>
						),
						temporal: (
							<p>
								Workflow and activity code lives in a normal repo per SDK language. Workers are deployed
								as your service (Docker, Kubernetes, your platform of choice). Namespaces, schedules and
								search-attribute schemas are configured in Temporal Cloud or via the CLI; they are not
								automatically versioned alongside the workflow code.
							</p>
						),
					},
				]}
				bottomLine={
					<>
						<strong>Windmill</strong> covers durable workflows with workflow-as-code (dispatched <code>task()</code> + inline <code>step()</code>) plus a visual flow builder, on a runtime that also runs scripts, apps, agents and pipelines.
						<strong> Temporal</strong> is durable execution at its purest: SDKs in seven languages, event-sourced workflows, mature primitives (signals, queries, child workflows, search attributes). The trade-off is the determinism contract on workflow code.
					</>
				}
			/>

			{/* ── Q4: Integrations ── */}
			<QASection
				reverse
				id="integrations"
				kicker="04 · Integrations"
				question="How does the platform integrate with your existing stack?"
				summary="Three directions: calling external systems, receiving events from them, and extending the platform with things neither tool covers natively."
				afterHero={<IntegrationsVideos />}
				subsections={[
					{
						label: 'Connecting out',
						windmill: (
							<p>
								Any npm, PyPI, Go or Maven package is a first-class import with automatic
								per-script dependency resolution and lockfiles, and no plugin layer in between:
								you call the vendor's real SDK with full type inference and auto-completion in
								the editor. <Link to="/docs/integrations/integrations_on_windmill" className="text-blue-500 hover:underline">50+ pre-built resource types</Link> cover common databases (Postgres,
								Snowflake, BigQuery), SaaS (Slack, Stripe, GitHub, Notion, OpenAI) and infrastructure
								(S3, Redis, Kafka): paste an API key into a typed form and the resource is shared
								across scripts, flows and apps, encrypted at rest. A community{' '}
								<a href="https://hub.windmill.dev" target="_blank" rel="noopener noreferrer" className="text-blue-500 hover:underline">Hub</a> has reusable scripts and flows for common tasks.
							</p>
						),
						temporal: (
							<p>
								No connector catalog. Every integration is just an activity that imports the vendor
								SDK and calls it: real type inference, real auth flows, real retry policies, no
								plugin layer either side. Activities run in the worker process, so dependency management
								is whatever your language community already uses (npm, pip, Maven, etc.).
							</p>
						),
					},
					{
						label: 'Receiving events',
						windmill: (
							<p>
								Native <Link to="/platform/triggers" className="text-blue-500 hover:underline">triggers</Link> for HTTP, cron, Kafka, NATS, Postgres CDC, SQS, MQTT, SMTP
								and WebSocket. Each trigger is a typed primitive in the UI: configure topic,
								subject or path, attach it to a script or flow, and Windmill handles the
								connection, replay and consumer-group state. Every script also gets an HTTP
								endpoint and a webhook URL for free, so most receive paths need zero glue
								code.
							</p>
						),
						temporal: (
							<p>
								Two native primitives: <a href="https://docs.temporal.io/schedules" target="_blank" rel="noopener noreferrer" className="text-blue-500 hover:underline">Schedules</a> (cron-style triggering of workflows) and{' '}
								<a href="https://docs.temporal.io/encyclopedia/workflow-message-passing" target="_blank" rel="noopener noreferrer" className="text-blue-500 hover:underline">Signals</a> (external events sent into a running workflow via gRPC). No native
								Kafka, Postgres CDC, SQS, MQTT or webhook trigger: you write a worker that consumes
								from those sources and starts workflows or signals existing ones.
							</p>
						),
					},
					{
						label: 'Extending',
						windmill: (
							<p>
								A new integration is a script with a new import: save and run, no restart, no
								image rebuild. The loop is hot-reload all the way through, in the web editor,
								the <Link to="/platform/local-dev" className="text-blue-500 hover:underline">CLI</Link> and <Link to="/platform/deployment-versioning" className="text-blue-500 hover:underline">Git sync</Link>. Custom logic can be exposed as a typed
								script, packaged as a <Link to="/docs/core_concepts/resources_and_types" className="text-blue-500 hover:underline">resource type</Link> for shared use, or published to the
								private workspace Hub for the team.
							</p>
						),
						temporal: (
							<p>
								Write another activity, deploy a new worker, or add a worker pool for a different
								queue. Custom auth, custom HTTP semantics and unusual integrations all live in
								activity code, with whatever helpers your team already wrote, no plugin or release
								cycle to wait on.
							</p>
						),
					},
				]}
				bottomLine={
					<>
						<strong>Windmill</strong> imports any public package as a first-class dependency with automatic resolution and 50+ pre-built typed resource types for credentials, plus a UI-driven trigger primitive surface for HTTP, cron, Kafka, NATS, Postgres CDC and more.
						<strong> Temporal</strong> treats every integration as just-an-activity, calling vendor SDKs directly. Triggers are limited to Schedules + Signals; anything else (Kafka consumer, webhook handler, CDC listener) is a worker you write.
					</>
				}
			/>

			{/* ── Q5: Migration ── */}
			<QASection
				id="migration"
				kicker="05 · Migration & lock-in"
				question="How hard to get in, and how hard to get out?"
				summary="Two simple questions: how much work to move your existing workflows onto the platform, and how much to move them off if you ever change your mind."
				subsections={[
					{
						label: 'Getting in',
						windmill: (
							<p>
								Paste a function body into the <Link to="/platform/script-editor" className="text-blue-500 hover:underline">script editor</Link>, Windmill infers args, generates a UI,
								handles dependencies. No Python wrapper or decorator to learn. <Link to="/platform/triggers" className="text-blue-500 hover:underline">Triggers</Link>, schedules,
								variables and <Link to="/platform/app-builder" className="text-blue-500 hover:underline">apps</Link> migrate one by one.
							</p>
						),
						temporal: (
							<p>
								Pick an SDK, write a workflow function and one or more activities, run a worker
								process. The mental model takes some learning: the workflow / activity split, the
								determinism contract, signals and queries, and how to think about replay. The official
								<a href="https://learn.temporal.io" target="_blank" rel="noopener noreferrer" className="text-blue-500 hover:underline"> Temporal University</a> is solid; expect a few days for the first production-ready
								workflow.
							</p>
						),
					},
					{
						label: 'Getting out',
						windmill: (
							<p>
								Step logic is already standard code (TS, Python, Go, Bash, SQL) that runs anywhere. The
								CLI exports the full workspace as plain files. What you lose leaving Windmill is the
								runtime, not the step code.
							</p>
						),
						temporal: (
							<p>
								Activity bodies are plain code in your SDK language: strip the activity decorator and
								you have a normal function that runs anywhere. What needs rewriting: workflow code
								itself (<a href="https://docs.temporal.io/encyclopedia/workflow-message-passing" target="_blank" rel="noopener noreferrer" className="text-blue-500 hover:underline">signals and queries</a>, <a href="https://docs.temporal.io/develop/typescript/child-workflows" target="_blank" rel="noopener noreferrer" className="text-blue-500 hover:underline">child workflows</a>, search attributes, the determinism
								patterns) and any infrastructure tied to the Temporal server.
							</p>
						),
					},
				]}
				bottomLine={
					<>
						<strong>Windmill</strong> step bodies and Windmill activity equivalents both port cleanly. The orchestration layer (workflow-as-code wrappers, flow YAML, resources) is the part to rewrite.
						<strong> Temporal</strong> activity bodies port cleanly too. The workflow code itself, with its rich primitives and determinism rules, is what locks in.
					</>
				}
			/>

			{/* ── Q6: Enterprise requirements ── */}
			<QASection
				reverse
				id="enterprise"
				kicker="06 · Enterprise requirements"
				question="Audit logs, observability, security, performance"
				summary="Once it's in production, the non-functional requirements dominate. Here's how each platform handles the four that matter most at scale."
				subsections={[
					{
						label: 'Observability',
						windmill: (
							<p>
								<Link to="/platform/observability" className="text-blue-500 hover:underline">Real-time streaming logs</Link>, per-run inputs / outputs / duration, built-in worker queue
								metrics and a Prometheus exporter. Trace ID on every job.
							</p>
						),
						temporal: (
							<p>
								The Temporal UI shows a per-workflow event timeline (every state transition, signal
								and activity attempt) which is genuinely strong for debugging and replay. OpenTelemetry,
								Prometheus metrics and structured server logs are available in OSS. Advanced
								dashboards and managed monitoring are <CloudBadge />.
							</p>
						),
					},
					{
						label: 'Audit logs',
						windmill: (
							<p>
								Full trail of who ran / edited / deployed what. Extended retention is <EnterpriseBadge />.
							</p>
						),
						temporal: (
							<p>
								The event history per workflow is itself an immutable audit trail of every decision and
								state change. Plus a separate audit log of API calls and admin actions, which is{' '}
								<CloudBadge /> on the higher tiers.
							</p>
						),
					},
					{
						label: 'Security',
						windmill: (
							<p>
								SOC 2 Type II compliant. <Link to="/platform/rbac" className="text-blue-500 hover:underline">RBAC</Link>, SSO (up to 10 users), encrypted secrets at rest and
								sandboxed script execution in open source. Uncapped SSO, audit logs and advanced access
								controls (SCIM, SAML) are <EnterpriseBadge />.
							</p>
						),
						temporal: (
							<p>
								Self-hosted core ships mTLS and namespace-level auth. RBAC, SSO (SAML / OIDC), SCIM and
								fine-grained namespace permissions are <CloudBadge />. Temporal Cloud is SOC 2 Type II,
								HIPAA and ISO 27001 compliant.
							</p>
						),
					},
					{
						label: 'Multi-tenancy & isolation',
						windmill: (
							<p>
								Multiple isolated <Link to="/docs/core_concepts/roles_and_permissions#workspace" className="text-blue-500 hover:underline">workspaces</Link> on the same instance, each with their own users,
								resources, secrets and access controls. Free tier is capped at 3 workspaces; unlimited
								is <EnterpriseBadge />.
							</p>
						),
						temporal: (
							<p>
								<a href="https://docs.temporal.io/namespaces" target="_blank" rel="noopener noreferrer" className="text-blue-500 hover:underline">Namespaces</a> isolate workflows, task queues and search-attribute schemas.
								Self-hosted supports multiple namespaces on one cluster. Temporal Cloud bills
								per-namespace storage and actions.
							</p>
						),
					},
					{
						label: 'Operational complexity',
						windmill: (
							<p>
								One Postgres database and a worker pool. Self-hosting is <code>docker compose up</code>
								for a single-node setup; production scaling is horizontal workers against the same
								Postgres.
							</p>
						),
						temporal: (
							<p>
								The Temporal Service is four roles (Frontend, History, Matching, internal Worker)
								plus a database (Postgres, MySQL or Cassandra) and optionally Elasticsearch for advanced
								visibility. Production self-hosting is non-trivial; many teams pick Temporal Cloud to
								avoid the operational lift.
							</p>
						),
					},
					{
						label: 'Performance',
						windmill: (
							<p>
								Workers poll Postgres directly with <code>SELECT ... FOR UPDATE SKIP LOCKED</code> and
								self-schedule. <Link to="/platform/workers" className="text-blue-500 hover:underline">Dedicated-worker mode</Link> <EnterpriseBadge /> keeps the runtime and
								dependencies pre-warmed. Arguments pass directly between steps, results are checkpointed
								as JSONB in Postgres with no built-in size limit. Strong on parallel and fan-out
								workloads in our <Link to="/docs/misc/benchmarks/competitors" className="text-blue-500 hover:underline">benchmarks</Link>.
							</p>
						),
						temporal: (
							<p>
								Workers long-poll the Frontend service via gRPC; tasks dispatch through the Matching
								service, with state persisted by the History service to the database. The architecture
								is optimized for sequential durable workflows: in our benchmarks, Temporal is faster on
								long sequential workflows (per-step overhead lower thanks to the History service);
								Windmill is faster on parallel and fan-out workloads. Write amplification is real
								(roughly 7 events per activity).
							</p>
						),
					},
				]}
				bottomLine={
					<>
						<strong>Temporal</strong> has the stronger durable-execution audit story (event history is itself the audit trail) and is faster on long sequential workflows. The trade-off is operational complexity: a four-service server plus database plus optional Elasticsearch.
						<strong> Windmill</strong> runs on a single Postgres with no separate scheduler, is faster on parallel and fan-out workloads, and ships more enterprise basics (RBAC, SSO up to 10 users, multi-workspace) in the open-source core.
					</>
				}
			>
				<motion.div {...fadeIn} className="max-w-5xl mx-auto mt-12 mb-8">
					<BenchmarkVisualization
						usecase="fibonacci_40_10"
						language="python"
						engines={['temporal', 'windmill', 'windmill_dedicated']}
						workers={1}
						title="40 lightweight Python tasks: Temporal vs Windmill"
						maintainAspectRatio={false}
					/>
				</motion.div>
			</QASection>

			{/* ── Q7: Licensing ── */}
			<QASection
				id="licensing"
				kicker="07 · Licensing & pricing"
				question="Open source, pricing, and self-hosting?"
				summary="Both are free to self-host. The real differences are the license terms, how much sits behind the managed tier, and whether Enterprise pricing is public."
				subsections={[
					{
						label: 'Open-source license',
						windmill: (
							<p>
								AGPLv3 core, free and unlimited self-hosted. Enterprise features (SSO, dedicated
								workers, audit logs, external secret backends) ship in a separate proprietary
								codebase. Managed cloud available.
							</p>
						),
						temporal: (
							<p>
								<strong>MIT-licensed</strong> self-hosted core, the most permissive option. The core
								(server, SDKs, CLI, UI) is fully open-source. Enterprise-grade governance features
								(SSO, RBAC, SCIM, audit logs, namespace permissions, dedicated infra, multi-region)
								live exclusively in <a href="https://temporal.io/cloud" target="_blank" rel="noopener noreferrer" className="text-blue-500 hover:underline">Temporal Cloud</a>, not as a self-hostable Enterprise build.
							</p>
						),
					},
					{
						label: 'Enterprise pricing',
						windmill: (
							<p>
								Enterprise adds SSO, audit logs, dedicated workers, advanced worker groups.{' '}
								<strong>Public per-seat and per-worker pricing</strong> on the <Link to="/pricing" className="text-blue-500 hover:underline">pricing page</Link>.
							</p>
						),
						temporal: (
							<p>
								<a href="https://temporal.io/pricing" target="_blank" rel="noopener noreferrer" className="text-blue-500 hover:underline">Temporal Cloud</a> is action-metered: <strong>Essentials $100/month</strong> (1M
								actions, 1GB active / 40GB retained storage), <strong>Business $500/month</strong> (2.5M
								actions, SAML SSO), <strong>Enterprise custom</strong> (10M+ actions, 24/7 support).
								Beyond base allocations, additional actions cost $50/M (declining to $25/M at 200M+).
								Storage charged separately at $0.042 per GBh active.
							</p>
						),
					},
				]}
				bottomLine={
					<>
						<strong>Temporal's</strong> OSS core is MIT-licensed, the most permissive option, and you can self-host the entire core including the UI for free. Governance features (SSO, RBAC, audit logs) live in Temporal Cloud only.
						<strong> Windmill's</strong> OSS core is AGPLv3 with Enterprise features in a separate proprietary codebase you can self-host on contract, with public per-seat and per-worker Enterprise pricing.
					</>
				}
			/>

			{/* ── Q8: Verdict ── */}
			<section id="verdict" className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-20 scroll-mt-24">
				<motion.div {...fadeIn}>
					<div className="text-sm font-semibold uppercase tracking-wider text-blue-600 dark:text-blue-400 mb-2">
						08 · Verdict
					</div>
					<h2 className="text-3xl sm:text-4xl font-bold text-gray-900 dark:text-white mb-4">
						The verdict
					</h2>
					<p className="text-lg text-gray-600 dark:text-gray-300 mb-5">
						Windmill and Temporal both solve durable execution, but with two different state-representation choices. Temporal is the canonical event-sourcing engine: every workflow decision is recorded in an immutable history, the workflow function replays from the beginning to recover state, and rich primitives (signals, queries, child workflows, search attributes) make sophisticated long-running logic expressible directly in code. Windmill workflow-as-code uses a mutable JSONB checkpoint with a dual primitive: <code>task()</code> dispatches a child job, <code>step()</code> runs inline and checkpoints in roughly 0.5ms. The engine has fewer ceremony rules but a smaller primitive surface.
					</p>
					<p className="text-lg text-gray-600 dark:text-gray-300 mb-5">
						<strong>Temporal can be a good fit if</strong> your scope is durable application logic with strong audit and replay needs: payment flows, sagas, multi-step user signup, long-running provisioning, AI agent runtimes. The mature ecosystem (seven SDKs, signals, queries, search attributes, sticky execution, replay tooling) is genuinely deeper than any alternative, and the event history doubles as a complete audit trail. The trade-offs are real: a four-service server architecture, a determinism contract on workflow code, and most governance features available only in Temporal Cloud.
					</p>
					<p className="text-lg text-gray-600 dark:text-gray-300 mb-5">
						<strong>Windmill works well for the same scope</strong> with a simpler architecture (single Postgres, no four-service split) and no determinism rules: workflow code can call <code>Math.random()</code>, read the clock, do anything Python or TypeScript normally does, because the checkpoint stores results rather than replaying commands. The runtime scope is also wider: the same platform that runs your workflows also powers <Link to="/use-cases/scripts" className="text-blue-500 hover:underline">scripts as standalone APIs</Link>, <Link to="/use-cases/data-pipelines" className="text-blue-500 hover:underline">data pipelines</Link>, both low-code and <Link to="/platform/app-builder" className="text-blue-500 hover:underline">full-code internal apps</Link>, and <Link to="/use-cases/ai-agents" className="text-blue-500 hover:underline">AI agents</Link>, with shared auth, secrets and observability across all of them. More of the enterprise foundation (RBAC, SSO up to 10 users, multi-workspace) ships in open source, and Enterprise pricing is published upfront on the <Link to="/pricing" className="text-blue-500 hover:underline">pricing page</Link>.
					</p>
					<p className="text-lg text-gray-600 dark:text-gray-300">
						The switching cost is symmetric in shape: activity bodies port cleanly off either platform, and the orchestration code (Temporal SDK primitives or Windmill workflow-as-code wrappers) is the part to rewrite. Performance is mixed too: Temporal is faster on long sequential workflows, Windmill is faster on parallel and fan-out workloads. If you're deciding between the two, the fastest way to judge is to spend an afternoon in each.
					</p>
				</motion.div>
			</section>

			{/* ── FAQ ── */}
			<UseCaseFAQ faqs={faqs} />

			{/* ── Final CTA ── */}
			<ProductCTA />
		</div>
	);
}
