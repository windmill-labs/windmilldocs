---
title: "Windmill vs Kestra: an honest side-by-side comparison"
description: "Evaluating Kestra and Windmill? A neutral, question-driven comparison across target audience, developer experience, scope, integrations, migration, performance, and pricing, including where each tool is genuinely the better choice."
---

import React from 'react';
import Link from '@docusaurus/Link';
import { motion } from 'framer-motion';
import {
	Check,
	X,
	Users,
	Terminal,
	Boxes,
	Puzzle,
	ArrowRightLeft,
	Gauge,
	Scale,
	Compass,
	ArrowRight,
} from 'lucide-react';
import GithubStarCount from '../../landing/GithubStarCount';
import HeroCTAButtons from '../../components/products/HeroCTAButtons';
import ProductCTA from '../../components/products/ProductCTA';
import UseCaseFAQ from '../../components/use-cases/UseCaseFAQ';
import { BenchmarkVisualization } from '../../components/BenchmarkVisualization';

export const fadeIn = {
	initial: { opacity: 0, y: 30 },
	whileInView: { opacity: 1, y: 0 },
	viewport: { once: true },
	transition: { duration: 0.5 },
};

{/* ── The 8 big questions ── */}

export const bigQuestions = [
	{ id: 'target',       kicker: 'Target',                 short: 'Who the platform is built for',                          icon: Users },
	{ id: 'devex',        kicker: 'Developer experience',   short: 'How developers interact with the platform',              icon: Terminal },
	{ id: 'build',        kicker: 'What you can build',     short: 'Data pipelines, internal apps, workflows, AI agents, …', icon: Boxes },
	{ id: 'integrations', kicker: 'Integrations',           short: 'How the platform integrates with your existing stack',   icon: Puzzle },
	{ id: 'migration',    kicker: 'Migration & lock-in',    short: 'How hard to get in, how hard to get out',                icon: ArrowRightLeft },
	{ id: 'enterprise',   kicker: 'Enterprise requirements', short: 'Audit logs, observability, security, performance',      icon: Gauge },
	{ id: 'licensing',    kicker: 'Licensing & pricing',    short: 'Open source, pricing, self-hosting',                     icon: Scale },
	{ id: 'verdict',      kicker: 'Verdict',                short: 'The verdict',                                            icon: Compass },
];

{/* ── Comparison table data ── */}

export const comparisonRows = [
	{ section: 'Platform scope' },
	{ feature: 'Workflow orchestration', windmill: true, kestra: true },
	{ feature: 'Scripts as standalone APIs & scheduled jobs', windmill: true, kestra: false },
	{ feature: 'Internal apps (low-code + React / Svelte)', windmill: true, kestra: false },
	{ feature: 'AI agents with tool calling & sandboxes', windmill: true, kestra: 'Partial' },
	{ feature: 'Approval steps & operator portals', windmill: true, kestra: false },

	{ section: 'Authoring experience' },
	{ feature: 'Declarative YAML authoring', windmill: false, kestra: true },
	{ feature: 'Step code in TypeScript, Python, Go, Bash, SQL', windmill: true, kestra: 'Partial' },
	{ feature: 'Direct argument passing between steps', windmill: true, kestra: 'Via outputs' },
	{ feature: 'Local dev, CLI, Git sync, VS Code, AI coding tools', windmill: true, kestra: 'Partial' },
	{ feature: 'Automated dependency resolution & caching', windmill: true, kestra: false },
	{ feature: 'Auto-generated UIs from script inputs', windmill: true, kestra: false },

	{ section: 'Ecosystem & integrations' },
	{ feature: 'Curated plugin / task catalog', windmill: 'Hub', kestra: '1,200+' },
	{ feature: 'Any npm / PyPI / Go module as an import', windmill: true, kestra: 'Partial' },
	{ feature: 'Community templates & blueprints', windmill: true, kestra: true },

	{ section: 'Collaboration' },
	{ feature: 'Non-developers can author workflows', windmill: 'Partial', kestra: true },
	{ feature: 'Non-developers can run & monitor workflows', windmill: true, kestra: true },
	{ feature: 'Dedicated operator role / operator UI', windmill: true, kestra: 'Partial' },

	{ section: 'Production & governance' },
	{ feature: 'Open source & self-hostable', windmill: 'AGPLv3', kestra: 'Apache 2.0' },
	{ feature: 'RBAC, audit logs, SSO', windmill: true, kestra: true },
	{ feature: '~3× faster execution (lightweight tasks, dedicated mode)', windmill: true, kestra: false },
	{ feature: 'Public Enterprise pricing', windmill: true, kestra: false },
];

export function CheckCell({ value }) {
	if (value === true) {
		return <Check className="w-5 h-5 text-emerald-500 mx-auto" />;
	}
	if (value === false) {
		return <X className="w-5 h-5 text-gray-400 mx-auto" />;
	}
	return <span className="text-sm text-amber-600 dark:text-amber-400 font-medium">{value}</span>;
}

{/* ── What you can build — row data ── */}

export const buildRows = [
	{ feature: 'Workflows',              description: 'Chain scripts into flows with branching, retries and approval steps', windmill: true, kestra: true },
	{ feature: 'Data pipelines',         description: 'ETL, syncs and scheduled data jobs with parallel branches',           windmill: true, kestra: true },
	{ feature: 'Scripts',                description: 'Functions in any language, deployed as APIs, webhooks or cron jobs',  windmill: true, kestra: false },
	{ feature: 'AI sandboxes & volumes', description: 'Isolated environments with persistent volumes for running agents',    windmill: true, kestra: false },
	{ feature: 'Internal apps',          description: 'Dashboards and admin tools (low-code or full-code in React / Svelte)',              windmill: true, kestra: false },
	{ feature: 'Scheduled tasks',        description: 'Cron jobs with retries, error handling and alerting',                 windmill: true, kestra: true },
];

{/* ── FAQ ── */}

export const faqs = [
	{
		question: 'Is Windmill a drop-in replacement for Kestra?',
		textAnswer:
			'Both platforms solve workflow orchestration with a queue-based, event-driven architecture, so the mental model transfers. Porting involves rewriting YAML workflow definitions as Windmill scripts and flows. Workflows that rely on specific Kestra plugins for exotic systems may require replacing those plugins with equivalent SDK calls in code.',
	},
	{
		question: 'Can I migrate Kestra YAML flows to Windmill automatically?',
		textAnswer:
			'No, there is no automated YAML-to-Windmill converter today. In practice, the migration is a manual rewrite where each Kestra task becomes a Windmill script or flow step. Most ported workflows end up shorter because Windmill passes arguments directly between steps and auto-generates input UIs.',
	},
	{
		question: 'Does Windmill have a plugin catalog comparable to Kestra\'s 1,200+ plugins?',
		textAnswer:
			'No. Windmill has a community Hub of reusable scripts and flows but not a curated plugin catalog the size of Kestra\'s. The trade-off: in Windmill, any npm, PyPI, Go or Maven package is a first-class integration via a normal import, so the effective catalog is the package ecosystems themselves. For mainstream data-pipeline tools that Kestra has official plugins for, Kestra is often easier to get started with.',
	},
	{
		question: 'Can non-technical users author workflows on Windmill?',
		textAnswer:
			'Only partially. Windmill has a visual flow editor and auto-generated UIs, but authoring steps still involves writing code. Kestra\'s declarative YAML (and no-code UI that generates it) is easier for analysts or business users to author directly. If that audience is central, Kestra is the better fit.',
	},
	{
		question: 'How does performance actually compare?',
		textAnswer:
			'We publish reproducible benchmarks covering Airflow, Prefect, Temporal, Kestra, Hatchet and Windmill. For lightweight task batches on a single worker, Windmill is slightly faster than Kestra in normal mode, and roughly 3× faster in dedicated-worker mode. For heavy-duty tasks that run for minutes, per-task overhead is a rounding error and the gap disappears.',
	},
	{
		question: 'What about the licenses, AGPLv3 vs Apache 2.0?',
		textAnswer:
			'Kestra\'s community edition is Apache 2.0, which is more permissive if you plan to modify and redistribute. Windmill is AGPLv3, which requires source availability for network-exposed modifications. Both are genuinely free to self-host for unmodified use. If license permissiveness is a hard constraint, Kestra wins this axis.',
	},
	{
		question: 'Is pricing public?',
		textAnswer:
			'Windmill publishes per-seat and per-worker Enterprise pricing on its pricing page. Kestra\'s Enterprise pricing is not publicly disclosed and requires a sales conversation.',
	},
];

{/* ── Section wrapper helper ── */}

export function QASection({ id, kicker, question, summary, kestra, windmill, bottomLine, children }) {
	return (
		<section id={id} className="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8 py-20 scroll-mt-24">
			<motion.div {...fadeIn} className="mb-10">
				<div className="text-sm font-semibold uppercase tracking-wider text-blue-600 dark:text-blue-400 mb-2">
					{kicker}
				</div>
				<h2 className="text-3xl sm:text-4xl font-bold text-gray-900 dark:text-white mb-4">
					{question}
				</h2>
				{summary && (
					<p className="text-lg text-gray-600 dark:text-gray-300">
						{summary}
					</p>
				)}
			</motion.div>

			{bottomLine && (
				<motion.div
					{...fadeIn}
					className="rounded-xl border-l-4 border-blue-500 bg-blue-50/70 dark:bg-blue-950/30 p-5 mb-8"
				>
					<div className="text-xs font-semibold uppercase tracking-wider text-blue-700 dark:text-blue-300 mb-1">
						Short answer
					</div>
					<div className="text-base text-gray-700 dark:text-gray-200 leading-relaxed">
						{bottomLine}
					</div>
				</motion.div>
			)}

			{(windmill || kestra) && (
				<div className="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8">
					<motion.div
						{...fadeIn}
						className="rounded-2xl border border-blue-200 dark:border-blue-800 bg-white dark:bg-gray-900 p-6 shadow-sm"
					>
						<div className="mb-4 flex items-center gap-2">
							<img src="/img/windmill.svg" alt="Windmill" className="w-5 h-5" />
							<span className="text-base font-bold text-blue-600 dark:text-blue-400">Windmill</span>
						</div>
						<div className="text-gray-700 dark:text-gray-200 leading-relaxed space-y-3">
							{windmill}
						</div>
					</motion.div>
					<motion.div
						{...fadeIn}
						className="rounded-2xl border border-gray-200 dark:border-gray-700 bg-gray-50 dark:bg-gray-900/60 p-6"
					>
						<div className="mb-4 flex items-center gap-2">
							<img src="/img/kestra.svg" alt="Kestra" className="w-5 h-5" />
							<span className="text-base font-bold text-gray-900 dark:text-white">Kestra</span>
						</div>
						<div className="text-gray-700 dark:text-gray-200 leading-relaxed space-y-3">
							{kestra}
						</div>
					</motion.div>
				</div>
			)}

			{children}
		</section>
	);
}

export default function KestraCompareContent({ title, description }) {
	return (
		<div className="pt-32 max-w-full">
			{/* ── Hero ── */}
			<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 pt-16 pb-8">
				<div className="grid grid-cols-1 md:grid-cols-2 gap-12 items-center">
					<motion.div {...fadeIn}>
						<div className="mb-4"><GithubStarCount /></div>
						<h1 className="!text-4xl sm:!text-6xl !font-semibold !tracking-tight text-gray-900 dark:text-white mb-6">
							Windmill vs Kestra
						</h1>
						<p className="text-xl text-gray-600 dark:text-gray-100 mb-8">
							Eight questions teams ask during a bake-off, with an honest answer on each one
							for where Windmill or Kestra is the better pick.
						</p>
						<HeroCTAButtons />
					</motion.div>

					<div className="hidden md:flex items-center justify-center">
						<div className="flex items-center gap-8">
							<motion.div
								initial={{ opacity: 0, x: -30 }}
								whileInView={{ opacity: 1, x: 0 }}
								viewport={{ once: true }}
								transition={{ duration: 0.5 }}
								className="flex items-center justify-center w-40 h-40 rounded-3xl border border-blue-200 dark:border-blue-800 bg-white dark:bg-gray-900 shadow-md"
							>
								<motion.img
									src="/img/windmill.svg"
									alt="Windmill"
									className="w-20 h-20"
									animate={{ rotate: 360 }}
									transition={{ duration: 6, repeat: Infinity, ease: 'linear' }}
								/>
							</motion.div>
							<motion.div
								initial={{ opacity: 0, scale: 0.5 }}
								whileInView={{ opacity: 1, scale: 1 }}
								viewport={{ once: true }}
								transition={{ duration: 0.4, delay: 0.2 }}
								className="text-xl font-semibold text-gray-400 dark:text-gray-500"
							>
								vs
							</motion.div>
							<motion.div
								initial={{ opacity: 0, x: 30 }}
								whileInView={{ opacity: 1, x: 0 }}
								viewport={{ once: true }}
								transition={{ duration: 0.5, delay: 0.1 }}
								className="flex items-center justify-center w-40 h-40 rounded-3xl border border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-900 shadow-sm"
							>
								<img src="/img/kestra.svg" alt="Kestra" className="w-20 h-20" />
							</motion.div>
						</div>
					</div>
				</div>
			</div>

			{/* ── Jump-to navigation (table of contents) ── */}
			<section className="mx-auto max-w-5xl px-4 sm:px-6 lg:px-8 pt-10 pb-4">
				<motion.div {...fadeIn} className="rounded-2xl border border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-900/60 p-6">
					<div className="text-xs font-semibold uppercase tracking-wider text-gray-500 dark:text-gray-400 mb-4">
						The 8 questions
					</div>
					<ol className="divide-y divide-gray-100 dark:divide-gray-800">
						{bigQuestions.map((q, i) => (
							<li key={q.id}>
								<a
									href={`#${q.id}`}
									className="group flex items-baseline gap-4 py-3 !no-underline hover:text-blue-600 dark:hover:text-blue-400"
								>
									<span className="text-xs font-mono font-semibold text-gray-400 dark:text-gray-500 w-6 shrink-0">
										{String(i + 1).padStart(2, '0')}
									</span>
									<span className="text-sm font-medium text-gray-500 dark:text-gray-400 w-48 shrink-0 whitespace-nowrap">
										{q.kicker}
									</span>
									<span className="text-base text-gray-900 dark:text-white group-hover:text-blue-600 dark:group-hover:text-blue-400 flex-1 whitespace-nowrap">
										{q.short}
									</span>
									<ArrowRight className="w-4 h-4 text-gray-300 dark:text-gray-600 group-hover:text-blue-500 group-hover:translate-x-1 transition-all shrink-0" />
								</a>
							</li>
						))}
					</ol>
				</motion.div>
			</section>


			{/* ── Quick take ── */}
			<section className="mx-auto max-w-5xl px-4 sm:px-6 lg:px-8 py-24">
				<motion.div {...fadeIn} className="grid grid-cols-1 md:grid-cols-2 gap-6">
					<div className="rounded-2xl border border-blue-200 dark:border-blue-800 bg-white dark:bg-gray-900 p-6 shadow-sm">
						<div className="mb-3 flex items-center gap-2">
							<img src="/img/windmill.svg" alt="Windmill" className="w-5 h-5" />
							<span className="text-base font-bold text-blue-600 dark:text-blue-400">
								Windmill in one sentence
							</span>
						</div>
						<p className="text-base text-gray-700 dark:text-gray-200 leading-relaxed">
							An open-source <strong>developer platform</strong> where one runtime powers scripts,
							flows, internal apps and AI agents, authored in code (TS / Python / Go / Bash / SQL)
							with the full local-dev loop. Built for engineering-led teams.
						</p>
					</div>
					<div className="rounded-2xl border border-gray-200 dark:border-gray-700 bg-gray-50 dark:bg-gray-900/60 p-6">
						<div className="mb-3 flex items-center gap-2">
							<img src="/img/kestra.svg" alt="Kestra" className="w-5 h-5" />
							<span className="text-base font-bold text-gray-900 dark:text-white">
								Kestra in one sentence
							</span>
						</div>
						<p className="text-base text-gray-700 dark:text-gray-200 leading-relaxed">
							An open-source <strong>orchestration platform</strong> focused on data, AI and infrastructure
							workflows, authored declaratively in YAML, with a large plugin catalog and a UI designed
							for mixed teams including non-developers.
						</p>
					</div>
				</motion.div>
			</section>

			{/* ── Q1: Audience ── */}
			<QASection
				id="target"
				kicker="01 · Target"
				question="Who is each platform built for?"
				summary="Both tools say 'for everyone'. In practice, the authoring model picks a primary audience, and it matters a lot once the team grows."
				kestra={
					<>
						<p>
							<strong>Mixed teams.</strong> YAML + a no-code UI let analysts and ops people edit workflows directly
							alongside engineers. Kestra's strongest design axis.
						</p>
					</>
				}
				windmill={
					<>
						<p>
							<strong>Developer-led teams.</strong> Engineers own the platform end-to-end: code, Git, local dev,
							code review, CI/CD, AI coding tools.
						</p>
						<p>
							Non-developers consume through auto-generated UIs, custom apps and an operator role.
							They don't author workflows.
						</p>
					</>
				}
				bottomLine={
					<>
						Pick <strong>Windmill</strong> if engineers build and own the platform.
						Pick <strong>Kestra</strong> if analysts, ops or business users need to edit workflows themselves.
					</>
				}
			/>

			{/* ── Q2: Authoring model ── */}
			<QASection
				id="devex"
				kicker="02 · Developer experience"
				question="How do developers interact with the platform?"
				summary="Covers the authoring model, the local-dev loop, IDE and AI tooling, and how much of the platform itself is manageable from Git."
				kestra={
					<>
						<p>
							<strong>Authoring.</strong> Declarative YAML. Each step is a plugin task (e.g.
							<code> io.kestra.plugin.scripts.python.Script</code>). A no-code UI generates the same YAML.
						</p>
						<p>
							<strong>Local dev & IDE.</strong> YAML schema with validation in VS Code and JetBrains plugins.
							Local server for running flows before deploy.
						</p>
						<p>
							<strong>Git & CI.</strong> <code>kestractl</code> + <a href="https://kestra.io/docs/version-control-cicd/git" target="_blank" rel="noopener noreferrer" className="text-blue-500 hover:underline">Git sync</a> deploy flows and namespace files. Users,
							RBAC and tenant settings still live in the UI or behind the API.
						</p>
					</>
				}
				windmill={
					<>
						<p>
							<strong>Authoring.</strong> A step is a function in TS, Python, Go, Bash or SQL. Arguments flow
							between steps as return values. Flows can be visual or code; both compile to the same thing.
						</p>
						<p>
							<strong>Local dev & IDE.</strong> Run scripts locally with the <Link to="/docs/advanced/cli" className="text-blue-500 hover:underline">Windmill CLI</Link>. <Link to="/docs/script_editor/vs_code_scripts" className="text-blue-500 hover:underline">VS Code extension</Link>,
							native language tooling (LSP, type-checking, linting) and AI coding tools (Claude Code, Codex).
						</p>
						<p>
							<strong>Git & CI (full IaC).</strong> Scripts, flows, apps, resources, variables, secrets, schedules,
							folders, groups and permissions are all files in Git, deployed via the CLI and <Link to="/docs/advanced/git_sync" className="text-blue-500 hover:underline">Git sync</Link>.
							Internal tooling runs like application code: PRs, reviews, rollbacks, CI/CD.
						</p>
					</>
				}
				bottomLine={
					<>
						Pick <strong>Windmill</strong> if you want full code flexibility and a whole-workspace Git workflow
						(scripts, flows, apps, resources, permissions all versioned like application code).
						Pick <strong>Kestra</strong> if you prefer a declarative YAML layer that non-engineers can read and edit.
					</>
				}
			/>

			{/* ── Q3: Scope ── */}
			<QASection
				id="build"
				kicker="03 · What you can build"
				question="Data pipelines, internal apps, workflows, AI agents, …?"
				summary="Both platforms orchestrate workflows. The real question is what else you can build on the same runtime."
				bottomLine={
					<>
						Pick <strong>Windmill</strong> if you want to build and orchestrate all your internal software on
						one platform (scripts, workflows, data pipelines, full-code apps, AI agents, operator UIs).
						Pick <strong>Kestra</strong> if workflow orchestration is the only thing you need and you bring a
						separate stack for dashboards, internal apps and agent front-ends.
					</>
				}
			>
				<motion.div {...fadeIn} className="overflow-hidden rounded-2xl border border-gray-200 dark:border-gray-700">
					<table className="w-full text-sm !table !mb-0 [&_th]:!border-0 [&_td]:!border-0">
						<thead className="bg-gray-50 dark:bg-gray-800/60">
							<tr>
								<th className="px-6 py-4 text-left font-semibold text-gray-900 dark:text-white">Primitive</th>
								<th className="px-6 py-4 text-center font-semibold text-blue-600 dark:text-blue-400 w-32">
									<span className="inline-flex items-center gap-2">
										<img src="/img/windmill.svg" alt="Windmill" className="w-4 h-4" />
										Windmill
									</span>
								</th>
								<th className="px-6 py-4 text-center font-semibold text-gray-700 dark:text-gray-300 w-32">
									<span className="inline-flex items-center gap-2">
										<img src="/img/kestra.svg" alt="Kestra" className="w-4 h-4" />
										Kestra
									</span>
								</th>
							</tr>
						</thead>
						<tbody className="divide-y divide-gray-100 dark:divide-gray-800 bg-white dark:bg-gray-900">
							{buildRows.map((row) => (
								<tr key={row.feature}>
									<td className="px-6 py-4">
										<div className="font-semibold text-gray-900 dark:text-white">{row.feature}</div>
										{row.description && (
											<div className="text-sm text-gray-500 dark:text-gray-400 mt-0.5">{row.description}</div>
										)}
									</td>
									<td className="px-6 py-4 text-center align-top pt-5"><CheckCell value={row.windmill} /></td>
									<td className="px-6 py-4 text-center align-top pt-5"><CheckCell value={row.kestra} /></td>
								</tr>
							))}
						</tbody>
					</table>
				</motion.div>
			</QASection>

			{/* ── Q4: Integrations ── */}
			<QASection
				id="integrations"
				kicker="04 · Integrations"
				question="How does the platform integrate with your existing stack?"
				summary="Three directions: calling external systems, receiving events from them, and extending the platform with things neither tool covers natively."
				kestra={
					<>
						<p>
							<strong>Connecting out.</strong> A curated catalog of 1,200+ plugins. Official connectors for
							Snowflake, BigQuery, dbt, Airbyte, Salesforce, Databricks and most common data-stack tools.
						</p>
						<p>
							<strong>Receiving events.</strong> Native triggers for Kafka, AMQP, JMS, MQTT, S3 events,
							webhooks and cron.
						</p>
						<p>
							<strong>Extending.</strong> No plugin for what you need? Write a Java / Kotlin plugin, or drop
							to an inline script inside a flow.
						</p>
					</>
				}
				windmill={
					<>
						<p>
							<strong>Connecting out.</strong> Any npm, PyPI, Go or Maven package is a first-class import.
							No plugin layer: you call the vendor's real SDK. A community <a href="https://hub.windmill.dev" target="_blank" rel="noopener noreferrer" className="text-blue-500 hover:underline">Hub</a> has reusable
							scripts and flows for common tasks.
						</p>
						<p>
							<strong>Receiving events.</strong> Native triggers for HTTP, cron, Kafka, NATS, Postgres CDC,
							SQS, MQTT, SMTP and WebSocket.
						</p>
						<p>
							<strong>Extending.</strong> A new integration is just a script with a new import. No plugin
							release cycle to wait on.
						</p>
					</>
				}
				bottomLine={
					<>
						Pick <strong>Windmill</strong> if your stack relies on niche or bleeding-edge systems you'd rather
						call with the vendor's real SDK. Pick <strong>Kestra</strong> if your workflows revolve around
						mainstream data-stack tools with an official plugin (Snowflake, dbt, Airbyte, Databricks).
					</>
				}
			/>

			{/* ── Q5: Collaboration ── */}
			<QASection
				id="migration"
				kicker="05 · Migration & lock-in"
				question="How hard to get in, and how hard to get out?"
				summary="Two simple questions: how much work to move your existing workflows onto the platform, and how much to move them off if you ever change your mind."
				kestra={
					<>
						<p>
							<strong>Getting in.</strong> If you already think in YAML DAGs (Airflow), the mental model
							transfers. Each task becomes a plugin reference; scripts inline or as files.
						</p>
						<p>
							<strong>Getting out.</strong> Workflow YAML is tied to <code>io.kestra.plugin.*</code> task types. Leaving
							means rewriting plugin-backed tasks against the underlying vendor SDKs, or reproducing
							the plugin layer on the new platform.
						</p>
					</>
				}
				windmill={
					<>
						<p>
							<strong>Getting in.</strong> Paste a function body, Windmill infers args, generates a UI, handles
							dependencies. No YAML to learn. Triggers, schedules, variables, apps migrate one by one.
						</p>
						<p>
							<strong>Getting out.</strong> Step logic is already standard code (TS, Python, Go, Bash, SQL) that
							runs anywhere. The CLI exports the full workspace as plain files. What you lose leaving
							Windmill is the runtime, not the step code.
						</p>
					</>
				}
				bottomLine={
					<>
						Pick <strong>Windmill</strong> if low switching cost matters: step code is standard and runs anywhere,
						including after you leave. Pick <strong>Kestra</strong> if you already think in YAML DAGs
						(from Airflow or similar) and don't plan to migrate off.
					</>
				}
			/>

			{/* ── Q6: Performance ── */}
			<QASection
				id="enterprise"
				kicker="06 · Enterprise requirements"
				question="Audit logs, observability, security, performance"
				summary="Once it's in production, the non-functional requirements dominate. Here's how each platform handles the four that matter most at scale."
				kestra={
					<>
						<p>
							<strong>Observability.</strong> Execution logs, task-level timings, metrics via OpenTelemetry.
							Dashboards per namespace.
						</p>
						<p>
							<strong>Audit logs.</strong> Enterprise feature. Tracks platform events and user activity.
						</p>
						<p>
							<strong>Security.</strong> SOC 2 compliant on Enterprise. RBAC, SSO (SAML / OIDC), secret
							manager integrations, multi-tenancy (Enterprise).
						</p>
						<p>
							<strong>Performance.</strong> Queue-based, horizontally scalable. Per-task overhead from JVM
							startup, plugin loading and state persistence. Negligible for multi-minute tasks.
						</p>
					</>
				}
				windmill={
					<>
						<p>
							<strong>Observability.</strong> Real-time streaming logs, per-run inputs / outputs / duration,
							built-in worker queue metrics and a <Link to="/docs/core_concepts/monitor_past_and_future_runs" className="text-blue-500 hover:underline">Prometheus exporter</Link>. Trace ID on every job.
						</p>
						<p>
							<strong>Audit logs.</strong> Full trail of who ran / edited / deployed what (Enterprise adds
							extended retention).
						</p>
						<p>
							<strong>Security.</strong> SOC 2 Type II compliant. <Link to="/docs/core_concepts/roles_and_permissions" className="text-blue-500 hover:underline">RBAC</Link>, SSO (SAML / OIDC),
							encrypted secrets at rest, sandboxed script execution, workspace isolation.
						</p>
						<p>
							<strong>Performance.</strong> ~10ms cold starts. <Link to="/docs/core_concepts/dedicated_workers" className="text-blue-500 hover:underline">Dedicated-worker mode</Link> keeps runtimes
							and dependencies pre-warmed. <Link to="/docs/misc/benchmarks/competitors" className="text-blue-500 hover:underline">Roughly 3× faster</Link> than Kestra on lightweight
							tasks on equivalent hardware.
						</p>
					</>
				}
				bottomLine={
					<>
						Both cover the enterprise basics (RBAC, SSO, audit logs, SOC 2). Pick <strong>Windmill</strong> if
						short, high-volume workloads dominate (webhooks, alerts, small scripts): ~3× lower per-task
						overhead becomes real money at scale. Pick <strong>Kestra</strong> if your workload is long-running
						data tasks where per-task overhead doesn't matter.
					</>
				}
			>
				<motion.div {...fadeIn} className="max-w-5xl mx-auto mb-8">
					<BenchmarkVisualization
						usecase="fibonacci_40_10"
						language="python"
						engines={['kestra', 'windmill', 'windmill_dedicated']}
						workers={1}
						title="40 lightweight Python tasks: Kestra vs Windmill"
						maintainAspectRatio={false}
					/>
				</motion.div>
			</QASection>

			{/* ── Q7: Licensing ── */}
			<QASection
				id="licensing"
				kicker="07 · Licensing & pricing"
				question="Open source, pricing, and self-hosting?"
				summary="Both are free to self-host. The real differences are the license terms and whether Enterprise pricing is public."
				kestra={
					<>
						<p>
							<strong>Apache 2.0,</strong> free and unlimited self-hosted. Managed cloud available.
						</p>
						<p>
							Enterprise adds SSO, multi-tenancy, audit logs, advanced RBAC.
							<strong> Pricing not public;</strong> sales conversation required.
						</p>
					</>
				}
				windmill={
					<>
						<p>
							<strong>AGPLv3,</strong> free and unlimited self-hosted. Managed cloud available.
						</p>
						<p>
							Enterprise adds SSO, audit logs, dedicated workers, advanced worker groups.
							<strong> Public per-seat and per-worker pricing</strong> on the <Link to="/pricing" className="text-blue-500 hover:underline">pricing page</Link>.
						</p>
					</>
				}
				bottomLine={
					<>
						Pick <strong>Windmill</strong> if you want upfront per-seat / per-worker pricing without a sales call.
						Pick <strong>Kestra</strong> if Apache 2.0 licensing matters (more permissive than Windmill's AGPLv3
						for redistribution). Both are free to self-host.
					</>
				}
			/>

			{/* ── Q8: Verdict ── */}
			<section id="verdict" className="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8 py-20 scroll-mt-24">
				<motion.div {...fadeIn} className="mb-12">
					<div className="text-sm font-semibold uppercase tracking-wider text-blue-600 dark:text-blue-400 mb-2">
						08 · Verdict
					</div>
					<h2 className="text-3xl sm:text-4xl font-bold text-gray-900 dark:text-white mb-4">
						The verdict
					</h2>
					<p className="text-lg text-gray-600 dark:text-gray-300">
						No tool wins on every axis. Here are the scenarios where each platform is genuinely
						the right pick.
					</p>
				</motion.div>

				<div className="grid grid-cols-1 md:grid-cols-2 gap-6">
					<motion.div
						{...fadeIn}
						className="rounded-2xl border border-blue-200 dark:border-blue-800 bg-white dark:bg-gray-900 p-8 shadow-md"
					>
						<div className="mb-4 flex items-center gap-2">
							<img src="/img/windmill.svg" alt="Windmill" className="w-6 h-6" />
							<span className="text-xl font-bold text-blue-600 dark:text-blue-400">
								Pick Windmill if…
							</span>
						</div>
						<ul className="space-y-3 text-gray-700 dark:text-gray-200">
							<li className="flex items-start gap-2">
								<Check className="w-5 h-5 text-blue-500 shrink-0 mt-0.5" />
								<span>You want <strong>one platform for scripts, flows, internal apps and AI agents</strong>, not three separate systems.</span>
							</li>
							<li className="flex items-start gap-2">
								<Check className="w-5 h-5 text-blue-500 shrink-0 mt-0.5" />
								<span>Your team is <strong>developer-led</strong> and values a real engineering loop (CLI, VS Code, Git sync, AI coding tools).</span>
							</li>
							<li className="flex items-start gap-2">
								<Check className="w-5 h-5 text-blue-500 shrink-0 mt-0.5" />
								<span>You want <strong>any npm / PyPI / Go package</strong> as an integration, not a plugin catalog.</span>
							</li>
							<li className="flex items-start gap-2">
								<Check className="w-5 h-5 text-blue-500 shrink-0 mt-0.5" />
								<span>Low per-task latency matters: <strong>high-volume short tasks</strong>, webhooks, alerts, internal tooling.</span>
							</li>
							<li className="flex items-start gap-2">
								<Check className="w-5 h-5 text-blue-500 shrink-0 mt-0.5" />
								<span>You want <strong>public, per-seat Enterprise pricing</strong> without a sales call.</span>
							</li>
						</ul>
					</motion.div>

					<motion.div
						{...fadeIn}
						className="rounded-2xl border border-gray-200 dark:border-gray-700 bg-gray-50 dark:bg-gray-900/60 p-8"
					>
						<div className="mb-4 flex items-center gap-2">
							<img src="/img/kestra.svg" alt="Kestra" className="w-6 h-6" />
							<span className="text-xl font-bold text-gray-900 dark:text-white">
								Pick Kestra if…
							</span>
						</div>
						<ul className="space-y-3 text-gray-700 dark:text-gray-200">
							<li className="flex items-start gap-2">
								<Check className="w-5 h-5 text-emerald-500 shrink-0 mt-0.5" />
								<span>Non-developers (analysts, ops, business users) need to <strong>author</strong> workflows, not just run them.</span>
							</li>
							<li className="flex items-start gap-2">
								<Check className="w-5 h-5 text-emerald-500 shrink-0 mt-0.5" />
								<span>Your workflows are dominated by <strong>mainstream data-stack tools</strong> (Snowflake, dbt, Airbyte, Databricks…) and you benefit from an official plugin.</span>
							</li>
							<li className="flex items-start gap-2">
								<Check className="w-5 h-5 text-emerald-500 shrink-0 mt-0.5" />
								<span>You prefer <strong>declarative YAML's consistency</strong> over the flexibility of arbitrary code.</span>
							</li>
							<li className="flex items-start gap-2">
								<Check className="w-5 h-5 text-emerald-500 shrink-0 mt-0.5" />
								<span>You need <strong>Apache 2.0 licensing</strong> specifically.</span>
							</li>
							<li className="flex items-start gap-2">
								<Check className="w-5 h-5 text-emerald-500 shrink-0 mt-0.5" />
								<span>You only need orchestration and already have solutions for internal apps and admin UIs.</span>
							</li>
						</ul>
					</motion.div>
				</div>
			</section>

			{/* ── FAQ ── */}
			<UseCaseFAQ faqs={faqs} />

			{/* ── Final CTA ── */}
			<ProductCTA />
		</div>
	);
}
