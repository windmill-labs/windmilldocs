---
title: "Local development"
description: "How do I develop Windmill scripts and flows locally? Use the CLI, VS Code extension, and AI coding assistants to build and test from your own environment."
---

import React, { useRef, useState } from 'react';
import Link from '@docusaurus/Link';
import { motion, useInView } from 'framer-motion';
import { ArrowRight, BookOpen, Terminal, FileCode, FolderSync, Plug, ChevronRight, Code2, Workflow, LayoutDashboard, Copy, Check } from 'lucide-react';
import UseCaseFAQ from '../../components/use-cases/UseCaseFAQ';

export const fadeIn = {
	initial: { opacity: 0, y: 30 },
	whileInView: { opacity: 1, y: 0 },
	viewport: { once: true },
	transition: { duration: 0.5 }
};

export const stagger = (delay = 0) => ({
	...fadeIn,
	transition: { duration: 0.5, delay }
});

{/* ── Copy snippet ── */}

export function CopySnippet({ text }) {
	const [copied, setCopied] = useState(false);
	const handleCopy = () => {
		navigator.clipboard.writeText(text);
		setCopied(true);
		setTimeout(() => setCopied(false), 2000);
	};
	return (
		<div className="mb-6 inline-flex items-center gap-2 rounded-lg bg-gray-900 dark:bg-gray-900 border border-gray-700 dark:border-gray-700 px-4 py-2.5 font-mono text-sm text-gray-200">
			<Terminal className="w-4 h-4 text-gray-500 flex-shrink-0" />
			<span>{text}</span>
			<button onClick={handleCopy} className="ml-2 p-1 rounded hover:bg-gray-700 transition-colors flex-shrink-0" aria-label="Copy to clipboard">
				{copied
					? <Check className="w-3.5 h-3.5 text-green-400" />
					: <Copy className="w-3.5 h-3.5 text-gray-500 hover:text-gray-300" />
				}
			</button>
		</div>
	);
}

{/* ── Hero animation: terminal showing wmill sync pull ── */}

export function HeroSteps() {
	const ref = useRef(null);
	const isInView = useInView(ref, { once: true, margin: "-40px" });
	const [phase, setPhase] = useState(0);
	React.useEffect(() => {
		if (!isInView) return;
		const t1 = setTimeout(() => setPhase(1), 2500);
		const t2 = setTimeout(() => setPhase(2), 5000);
		return () => { clearTimeout(t1); clearTimeout(t2); };
	}, [isInView]);
	const stepLabels = ['1. Pull', '2. Edit', '3. Deploy'];
	return (
		<div ref={ref} className="rounded-xl border border-gray-700 overflow-hidden bg-gray-900 dark:bg-gray-950 shadow-lg">
			{/* Step indicators */}
			<div className="flex items-center gap-0 bg-gray-800 dark:bg-gray-900 border-b border-gray-700">
				{stepLabels.map((label, i) => (
					<div
						key={label}
						className={`flex-1 text-center py-2 text-[10px] font-medium uppercase tracking-wider transition-colors duration-300 ${
							i === phase
								? 'text-white bg-gray-700/50'
								: i < phase
									? 'text-green-400/60'
									: 'text-gray-600'
						} ${i > 0 ? 'border-l border-gray-700' : ''}`}
					>
						{i < phase ? '✓ ' : ''}{label}
					</div>
				))}
			</div>
			{/* Content area */}
			<div className="relative h-[300px] overflow-hidden">
				{/* Phase 0: Pull */}
				<motion.div
					initial={{ opacity: 0 }}
					animate={{ opacity: phase === 0 ? 1 : 0, x: phase === 0 ? 0 : -20 }}
					transition={{ duration: 0.3 }}
					className="absolute inset-0 p-3 font-mono text-sm leading-7"
				>
					<div className="text-green-400">$ wmill sync pull</div>
					<motion.div
						initial={{ opacity: 0 }}
						animate={isInView ? { opacity: 1 } : {}}
						transition={{ delay: 0.5 }}
						className="text-gray-500"
					>Pulling workspace prod...</motion.div>
					{['u/alice/fetch_orders.ts', 'u/alice/fetch_orders.script.yaml', 'u/alice/transform.py', 'u/alice/transform.script.yaml'].map((f, i) => (
						<motion.div
							key={f}
							initial={{ opacity: 0 }}
							animate={isInView ? { opacity: 1 } : {}}
							transition={{ delay: 0.8 + i * 0.15 }}
							className="text-gray-400"
						>  {f}</motion.div>
					))}
					<motion.div
						initial={{ opacity: 0 }}
						animate={isInView ? { opacity: 1 } : {}}
						transition={{ delay: 1.6 }}
						className="text-green-300 mt-1"
					>Done. 4 files pulled.</motion.div>
				</motion.div>
				{/* Phase 1: Edit */}
				<motion.div
					initial={{ opacity: 0 }}
					animate={{ opacity: phase === 1 ? 1 : 0, x: phase === 1 ? 0 : phase < 1 ? 20 : -20 }}
					transition={{ duration: 0.3 }}
					className="absolute inset-0 p-3 font-mono text-sm leading-7"
				>
					<div className="text-gray-600 mb-1">fetch_orders.ts</div>
					<div className="text-gray-500">import {'{'} getResource {'}'} from "windmill-client";</div>
					<div className="text-gray-500">&nbsp;</div>
					<motion.div
						initial={{ opacity: 0 }}
						animate={phase >= 1 ? { opacity: 1 } : {}}
						transition={{ delay: 0.4 }}
						className="bg-red-950/40 text-red-400 -mx-3 px-3"
					>- export async function main(limit = 100) {'{'}</motion.div>
					<motion.div
						initial={{ opacity: 0 }}
						animate={phase >= 1 ? { opacity: 1 } : {}}
						transition={{ delay: 0.7 }}
						className="bg-green-950/40 text-green-400 -mx-3 px-3"
					>+ export async function main(limit = 100, offset = 0) {'{'}</motion.div>
					<div className="text-gray-500">  const db = await getResource("f/orders/pg");</div>
					<motion.div
						initial={{ opacity: 0 }}
						animate={phase >= 1 ? { opacity: 1 } : {}}
						transition={{ delay: 1.0 }}
						className="bg-red-950/40 text-red-400 -mx-3 px-3"
					>-   `SELECT * FROM orders LIMIT $1`, [limit]</motion.div>
					<motion.div
						initial={{ opacity: 0 }}
						animate={phase >= 1 ? { opacity: 1 } : {}}
						transition={{ delay: 1.3 }}
						className="bg-green-950/40 text-green-400 -mx-3 px-3"
					>+   `SELECT * FROM orders LIMIT $1 OFFSET $2`, [limit, offset]</motion.div>
				</motion.div>
				{/* Phase 2: Push */}
				<motion.div
					initial={{ opacity: 0 }}
					animate={{ opacity: phase === 2 ? 1 : 0, x: phase === 2 ? 0 : 20 }}
					transition={{ duration: 0.3 }}
					className="absolute inset-0 p-3 font-mono text-sm leading-7"
				>
					<div className="text-green-400">$ wmill sync push</div>
					<motion.div
						initial={{ opacity: 0 }}
						animate={phase >= 2 ? { opacity: 1 } : {}}
						transition={{ delay: 0.4 }}
						className="text-gray-500"
					>Pushing to workspace prod...</motion.div>
					<motion.div
						initial={{ opacity: 0 }}
						animate={phase >= 2 ? { opacity: 1 } : {}}
						transition={{ delay: 0.7 }}
						className="text-green-400"
					>  + u/alice/fetch_orders.ts (modified)</motion.div>
					<motion.div
						initial={{ opacity: 0 }}
						animate={phase >= 2 ? { opacity: 1 } : {}}
						transition={{ delay: 1.0 }}
						className="text-green-300 mt-1"
					>Done. 1 script deployed.</motion.div>
				</motion.div>
			</div>
		</div>
	);
}

{/* ── Section helper ── */}

export function Section({ title, desc, docLink, image, reverse, children }) {
	const hasImage = !!image;
	return (
		<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-16">
			<motion.div
				{...fadeIn}
				className={hasImage ? 'grid grid-cols-1 lg:grid-cols-2 gap-12 items-center' : ''}
			>
				<div className={hasImage && reverse ? 'lg:order-2' : ''}>
					<h2 className="text-3xl sm:text-4xl font-bold text-gray-900 dark:text-white mb-4">{title}</h2>
					<p className="text-lg text-gray-600 dark:text-gray-300 mb-4">{desc}</p>
					{docLink && (
						<Link
							to={docLink}
							className="inline-flex items-center gap-1.5 text-blue-600 dark:text-blue-400 text-sm font-medium hover:underline !no-underline"
						>
							<BookOpen className="w-4 h-4" /> Read the docs
						</Link>
					)}
				</div>
				{hasImage && (
					<div className={reverse ? 'lg:order-1' : ''}>
						<img
							src={image}
							alt={title}
							className="rounded-2xl border border-gray-200 dark:border-gray-700 w-full shadow-lg"
						/>
					</div>
				)}
			</motion.div>
			{children}
		</div>
	);
}

{/* ── Main page ── */}

export default function LocalDevContent({ title, description }) {
	return (
		<div className="pt-32 max-w-full">
			{/* ═══════════════ Hero ═══════════════ */}
			<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 pt-16 pb-8">
				<div className="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
					<motion.div {...fadeIn}>
						<h1 className="!text-4xl sm:!text-5xl lg:!text-6xl !font-semibold !tracking-tight text-gray-900 dark:text-white mb-6">
							Develop locally, deploy to Windmill
						</h1>
						<p className="text-xl text-gray-600 dark:text-gray-100 mb-6">
							The Windmill CLI syncs your workspace to a local folder. Edit scripts and flows in any editor, push changes back with one command.
						</p>
						<CopySnippet text="npm install -g windmill-cli" />
						<div className="flex flex-col sm:flex-row gap-4">
							<a
								href="https://app.windmill.dev"
								target="_blank"
								rel="noopener noreferrer"
								className="inline-flex items-center gap-2 px-6 py-3 rounded-lg bg-blue-600 text-white font-medium hover:bg-blue-700 transition-colors !no-underline"
							>
								Try Windmill free
								<ArrowRight className="w-4 h-4" />
							</a>
							<Link
								to="/docs/advanced/local_development"
								className="inline-flex items-center gap-2 px-6 py-3 rounded-lg border border-gray-300 dark:border-gray-600 text-gray-700 dark:text-gray-200 font-medium hover:border-gray-400 dark:hover:border-gray-500 transition-colors !no-underline"
							>
								Read the docs
							</Link>
						</div>
					</motion.div>

					<motion.div {...stagger(0.2)} className="hidden lg:block">
						<HeroSteps />
					</motion.div>
				</div>
			</div>

			{/* ═══════════════ Sync pull / push ═══════════════ */}
			<Section
				title="Sync your workspace to a local folder"
				desc={<span><code>wmill sync pull</code> downloads your entire workspace as plain files: scripts, <Link to="/docs/getting_started/flows_quickstart" className="text-blue-600 dark:text-blue-400 hover:underline">flows</Link>, apps, resources, triggers, and variables. Edit anything locally, then <code>wmill sync push</code> to deploy. Both commands support <Link to="/docs/advanced/cli/sync" className="text-blue-600 dark:text-blue-400 hover:underline">path filters</Link> to sync specific folders.</span>}
				image="/img/money-pages/workflow-code.webp"
				docLink="/docs/advanced/cli/sync"
			/>

			{/* ═══════════════ VS Code extension ═══════════════ */}
			<Section
				title="VS Code extension"
				desc={<span>The Windmill <Link to="/docs/cli_local_dev/vscode-extension" className="text-blue-600 dark:text-blue-400 hover:underline">VS Code extension</Link> previews auto-generated UIs while you write scripts, tests scripts and flows from the editor, and validates flow YAML in real time. Bidirectional editing lets you switch between the YAML file and a visual flow editor.</span>}
				image="/img/money-pages/vscode-extension.webp"
				docLink="/docs/cli_local_dev/vscode-extension"
				reverse
			/>

			{/* ═══════════════ Create scripts, flows, and apps ═══════════════ */}
			<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-16">
				<motion.div {...fadeIn}>
					<h2 className="text-3xl sm:text-4xl font-bold text-gray-900 dark:text-white mb-4">Create scripts, flows, and apps from the CLI</h2>
					<p className="text-lg text-gray-600 dark:text-gray-300 mb-8">Scaffold new items locally with one command. The CLI generates the file structure Windmill needs for deployment.</p>
				</motion.div>
				<div className="grid grid-cols-1 md:grid-cols-3 gap-6">
					{[
						{
							icon: Code2,
							title: 'Script',
							command: 'wmill script bootstrap <path> <lang>',
							files: ['fetch_orders.py', 'fetch_orders.script.yaml', 'fetch_orders.lock'],
							desc: <span>Scaffold a script in any of <Link to="/docs/getting_started/scripts_quickstart" className="text-blue-600 dark:text-blue-400 hover:underline">20+ languages</Link> with its metadata and lockfile.</span>,
							docLink: '/docs/advanced/cli/script',
						},
						{
							icon: Workflow,
							title: 'Flow',
							command: 'wmill flow bootstrap <path>',
							files: ['flow.yaml', 'step_1.inline_script.ts', 'step_2.inline_script.py'],
							desc: <span>Scaffold a <Link to="/docs/getting_started/flows_quickstart" className="text-blue-600 dark:text-blue-400 hover:underline">flow</Link> folder with a YAML definition and inline scripts.</span>,
							docLink: '/docs/advanced/cli/flow',
						},
						{
							icon: LayoutDashboard,
							title: 'Full-code app',
							command: 'wmill app new',
							files: ['App.tsx', 'raw_app.yaml', 'backend/get_users.ts'],
							desc: <span>Scaffold a <Link to="/docs/getting_started/full_code_apps_quickstart" className="text-blue-600 dark:text-blue-400 hover:underline">React or Svelte app</Link> with backend runnables and a dev server.</span>,
							docLink: '/docs/full_code_apps/cli_workflow',
						},
					].map((item, i) => (
						<motion.div
							key={item.title}
							{...fadeIn}
							transition={{ duration: 0.5, delay: i * 0.1 }}
							className="flex flex-col p-5 rounded-xl border border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-950"
						>
							<div className="flex items-center gap-2.5 mb-3">
								<item.icon className="w-5 h-5 text-blue-500" />
								<h3 className="text-lg font-semibold text-gray-900 dark:text-white">{item.title}</h3>
							</div>
							<div className="rounded-lg bg-gray-900 dark:bg-gray-900 px-3 py-2 mb-3 font-mono text-[11px] text-green-400 whitespace-nowrap overflow-x-auto">
								<span className="text-gray-500">$ </span>{item.command}
							</div>
							<div className="mb-3 font-mono text-[11px] text-gray-500 dark:text-gray-400 space-y-0.5">
								{item.files.map(f => <div key={f}>  {f}</div>)}
							</div>
							<p className="text-sm text-gray-600 dark:text-gray-300 mb-3 flex-1">{item.desc}</p>
							<Link
								to={item.docLink}
								className="inline-flex items-center gap-1.5 text-blue-600 dark:text-blue-400 text-sm font-medium hover:underline !no-underline mt-auto"
							>
								<BookOpen className="w-3.5 h-3.5" /> Read the docs
							</Link>
						</motion.div>
					))}
				</div>
			</div>

			{/* ═══════════════ Run locally ═══════════════ */}
			<Section
				title="Run and debug locally"
				desc={<span>Execute Windmill scripts on your machine with access to remote <Link to="/docs/core_concepts/resources_and_types" className="text-blue-600 dark:text-blue-400 hover:underline">resources</Link> and state. Set <code>WM_TOKEN</code> and <code>BASE_INTERNAL_URL</code> to connect to a running instance, or use <Link to="/docs/advanced/local_development#mocked-api-files" className="text-blue-600 dark:text-blue-400 hover:underline">mocked API files</Link> to work offline. Debug with VS Code breakpoints or JetBrains run configurations.</span>}
				image="/img/money-pages/scripts-preview.webp"
				docLink="/docs/advanced/local_development/run_locally"
				reverse
			/>

			{/* ═══════════════ Visual break ═══════════════ */}
			<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
				<div className="border-t border-gray-200 dark:border-gray-800" />
			</div>

			{/* ═══════════════ Built-in skills and AGENTS.md ═══════════════ */}
			<Section
				title="Built-in skills and AGENTS.md"
				desc={<span>Run <code>wmill init</code> to generate <Link to="/docs/misc/guides/local_dev_with_ai" className="text-blue-600 dark:text-blue-400 hover:underline">context files</Link> and skill files for Claude Code, Cursor, and other AI tools. The generated <code>AGENTS.md</code> teaches assistants Windmill's file structure, script conventions, and deployment workflow. AI tools can also connect to a workspace through the <Link to="/docs/core_concepts/mcp" className="text-blue-600 dark:text-blue-400 hover:underline">MCP server</Link> to execute scripts, manage resources, and monitor jobs directly.</span>}
				image="/img/money-pages/ai-mcp.webp"
				docLink="/docs/misc/guides/local_dev_with_ai"
			/>

			{/* ═══════════════ Git integration ═══════════════ */}
			<Section
				title="Deploy and sync to Git"
				desc={<span><code>wmill sync push</code> deploys to your Windmill workspace and, when <Link to="/docs/advanced/git_sync" className="text-blue-600 dark:text-blue-400 hover:underline">Git Sync</Link> is enabled, automatically commits the changes to your repository. Changes can also be pulled from Git via CI/CD pipelines, making Git the single source of truth. For staging and production workflows, see <Link to="/product/versioning" className="text-blue-600 dark:text-blue-400 hover:underline">versioning and deployment</Link>.</span>}
				image="/img/money-pages/gitsync.webp"
				docLink="/docs/advanced/git_sync"
				reverse
			/>

			{/* ═══════════════ FAQ ═══════════════ */}
			<UseCaseFAQ faqs={[
				{ question: 'Do I need the CLI to develop locally?', textAnswer: 'The CLI is the primary tool for syncing a workspace to a local folder. Once synced, you can edit files in any editor. The VS Code extension adds previews and testing on top.' },
				{ question: 'Which editors are supported?', textAnswer: 'Any text editor works for editing scripts and flows as files. The VS Code extension provides the richest experience with live previews, YAML validation, and direct testing. Cursor is also supported.' },
				{ question: 'Can AI assistants create flows?', textAnswer: 'Yes. The wmill init command generates skill files that teach AI assistants how to write valid flow YAML. Combined with the VS Code YAML linter, AI tools can create and iterate on flows with real-time validation feedback.' },
				{ question: 'How does the MCP server differ from the CLI?', textAnswer: 'The CLI syncs files between your local directory and a workspace. The MCP server gives AI tools direct API access to a workspace: executing scripts, managing resources, and monitoring jobs. They complement each other.' },
				{ question: 'Can I run scripts locally without a Windmill instance?', textAnswer: 'You can run script code locally like any regular script. To access Windmill features like resources and state, you need a running instance or can use mocked API files to simulate the Windmill environment.' },
				{ question: 'How do I set up a local development workflow?', textAnswer: 'Install the CLI with npm install -g windmill-cli, run wmill workspace add to connect to your instance, then wmill sync pull to clone the workspace. Optionally run wmill init for AI context files and install the VS Code extension.' },
			]} />

			{/* ═══════════════ Transition CTA ═══════════════ */}
			<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-16">
				<motion.div {...fadeIn} className="text-center py-16 px-8 rounded-3xl bg-gradient-to-br from-blue-50 to-blue-100 dark:from-blue-950 dark:to-gray-900 border border-blue-200 dark:border-blue-800">
					<h2 className="text-3xl font-bold text-gray-900 dark:text-white mb-4">Deploy with confidence</h2>
					<p className="text-gray-600 dark:text-gray-300 mb-8 max-w-2xl mx-auto text-lg">
						Sync to Git, promote across workspaces, and track every deployment with full version history.
					</p>
					<div className="flex flex-col sm:flex-row gap-4 justify-center">
						<Link
							to="/product/versioning"
							className="inline-flex items-center gap-2 px-6 py-3 rounded-lg bg-blue-600 text-white font-medium hover:bg-blue-700 transition-colors !no-underline"
						>
							Explore versioning and deployment
							<ArrowRight className="w-4 h-4" />
						</Link>
						<a
							href="https://app.windmill.dev"
							target="_blank"
							rel="noopener noreferrer"
							className="inline-flex items-center gap-2 px-6 py-3 rounded-lg border border-gray-300 dark:border-gray-600 text-gray-700 dark:text-gray-200 font-medium hover:border-gray-400 dark:hover:border-gray-500 transition-colors !no-underline"
						>
							Try Windmill free
						</a>
					</div>
				</motion.div>
			</div>
		</div>
	);
}
