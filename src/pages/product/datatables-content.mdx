---
title: "Managed data tables"
description: "How do I store and query relational data in Windmill? Near-zero setup SQL storage for scripts, flows, and apps with automatic credential management."
---

import React from 'react';
import Link from '@docusaurus/Link';
import { motion } from 'framer-motion';
import { ArrowRight, BookOpen } from 'lucide-react';
import UseCaseFAQ from '../../components/use-cases/UseCaseFAQ';

export const fadeIn = {
	initial: { opacity: 0, y: 30 },
	whileInView: { opacity: 1, y: 0 },
	viewport: { once: true },
	transition: { duration: 0.5 }
};

export const stagger = (delay = 0) => ({
	...fadeIn,
	transition: { duration: 0.5, delay }
});

{/* ── Section helper ── */}

export function Section({ title, desc, docLink, image, reverse, children }) {
	return (
		<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-16">
			<motion.div
				{...fadeIn}
				className="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center"
			>
				<div className={reverse ? 'lg:order-2' : ''}>
					<h2 className="text-3xl sm:text-4xl font-bold text-gray-900 dark:text-white mb-4">{title}</h2>
					<p className="text-lg text-gray-600 dark:text-gray-300 mb-4">{desc}</p>
					{docLink && (
						<Link
							to={docLink}
							className="inline-flex items-center gap-1.5 text-blue-600 dark:text-blue-400 text-sm font-medium hover:underline !no-underline"
						>
							<BookOpen className="w-4 h-4" /> Read the docs
						</Link>
					)}
				</div>
				<div className={reverse ? 'lg:order-1' : ''}>
					{image ? (
						<img
							src={image}
							alt={title}
							className="rounded-2xl border border-gray-200 dark:border-gray-700 w-full shadow-lg"
						/>
					) : (
						<div className="w-full h-64 rounded-2xl bg-gray-100 dark:bg-gray-800 border border-gray-200 dark:border-gray-700 flex items-center justify-center">
							<span className="text-gray-400 dark:text-gray-500 text-sm">Screenshot coming soon</span>
						</div>
					)}
				</div>
			</motion.div>
			{children}
		</div>
	);
}

{/* ── Main page ── */}

export default function DatatablesContent({ title, description }) {
	return (
		<div className="pt-32 max-w-full">
			{/* ═══════════════ Hero ═══════════════ */}
			<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 pt-16 pb-8">
				<div className="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
					<motion.div {...fadeIn}>
						<h1 className="!text-4xl sm:!text-5xl lg:!text-6xl !font-semibold !tracking-tight text-gray-900 dark:text-white mb-6">
							Managed data tables
						</h1>
						<p className="text-xl text-gray-600 dark:text-gray-100 mb-8">
							Store and query relational data from scripts, flows, and apps. Windmill manages the database, credentials, and schema so you just write SQL.
						</p>
						<div className="flex flex-col sm:flex-row gap-4">
							<a
								href="https://app.windmill.dev"
								target="_blank"
								rel="noopener noreferrer"
								className="inline-flex items-center gap-2 px-6 py-3 rounded-lg bg-blue-600 text-white font-medium hover:bg-blue-700 transition-colors !no-underline"
							>
								Try Windmill free
								<ArrowRight className="w-4 h-4" />
							</a>
							<Link
								to="/docs/core_concepts/persistent_storage/data_tables"
								className="inline-flex items-center gap-2 px-6 py-3 rounded-lg border border-gray-300 dark:border-gray-600 text-gray-700 dark:text-gray-200 font-medium hover:border-gray-400 dark:hover:border-gray-500 transition-colors !no-underline"
							>
								Read the docs
							</Link>
						</div>
					</motion.div>

					<motion.div {...stagger(0.2)} className="hidden lg:block">
						{/* Placeholder: screenshot showing the Database Studio with a datatable open, columns visible, ~1200x800px, save as /static/img/money-pages/datatables-hero.webp */}
						<div className="w-full h-80 rounded-2xl bg-gray-100 dark:bg-gray-800 border border-gray-200 dark:border-gray-700 flex items-center justify-center">
							<span className="text-gray-400 dark:text-gray-500 text-sm">Screenshot coming soon</span>
						</div>
					</motion.div>
				</div>
			</div>

			{/* ═══════════════ Intro ═══════════════ */}
			<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-16">
				<motion.div {...fadeIn}>
					<h2 className="text-3xl sm:text-4xl font-bold text-gray-900 dark:text-white mb-4">SQL storage with near-zero setup</h2>
					<p className="text-lg text-gray-600 dark:text-gray-300 leading-relaxed">
						Datatables are a workspace-scoped layer on top of your own PostgreSQL database, added as a <Link to="/docs/core_concepts/resources_and_types" className="text-blue-600 dark:text-blue-400 hover:underline">resource</Link>. Configure it once, then any <Link to="/docs/getting_started/scripts_quickstart" className="text-blue-600 dark:text-blue-400 hover:underline">script</Link>, <Link to="/docs/getting_started/flows_quickstart" className="text-blue-600 dark:text-blue-400 hover:underline">flow</Link>, or <Link to="/docs/getting_started/apps_quickstart" className="text-blue-600 dark:text-blue-400 hover:underline">app</Link> can query it. Windmill manages credentials internally so they are never exposed to users.
					</p>
				</motion.div>
			</div>

			{/* ═══════════════ Query from any language ═══════════════ */}
			<Section
				title="Query from any language"
				desc={<span>TypeScript, Python, DuckDB, and raw SQL are supported. In TypeScript, string interpolation is automatically converted to <Link to="/docs/core_concepts/persistent_storage/data_tables#security" className="text-blue-600 dark:text-blue-400 hover:underline">parameterized queries</Link> to prevent SQL injection. Python uses positional arguments for the same purpose.</span>}
				docLink="/docs/core_concepts/persistent_storage/data_tables"
			/>

			{/* ═══════════════ Schema organization ═══════════════ */}
			<Section
				title="Schema organization"
				desc={<span>Use schemas to organize tables within a datatable. Set a default <Link to="/docs/core_concepts/persistent_storage/data_tables#schemas" className="text-blue-600 dark:text-blue-400 hover:underline">search path</Link> per script or use <code>schema.table</code> syntax. Typically one or a few datatables per workspace, with schemas for logical separation.</span>}
				docLink="/docs/core_concepts/persistent_storage/data_tables"
				reverse
			/>

			{/* ═══════════════ Asset tracking and data lineage ═══════════════ */}
			<Section
				title="Asset tracking and data lineage"
				desc={<span>Windmill auto-detects datatable usage in scripts and shows them as <Link to="/docs/core_concepts/assets" className="text-blue-600 dark:text-blue-400 hover:underline">asset nodes</Link> in flows. Read vs write mode is detected automatically. Click through to the Database Explorer to inspect schemas and data.</span>}
				docLink="/docs/core_concepts/assets"
			/>

			{/* ═══════════════ Full-code apps integration ═══════════════ */}
			<Section
				title="Full-code apps integration"
				desc={<span>Whitelist tables in <code>raw_app.yaml</code> and query them from backend runnables with SQL. The CLI generates a <code>DATATABLES.md</code> context file so AI coding assistants understand the data model.</span>}
				docLink="/docs/full_code_apps/data_tables"
				reverse
			/>

			{/* ═══════════════ Database Studio ═══════════════ */}
			<Section
				title="Database Studio"
				desc={<span>A built-in <Link to="/docs/apps/app_configuration_settings/database_studio" className="text-blue-600 dark:text-blue-400 hover:underline">app component</Link> for browsing, editing, and managing tables visually. Available in both the app editor and as a standalone tool.</span>}
				docLink="/docs/apps/app_configuration_settings/database_studio"
			/>

			{/* ═══════════════ FAQ ═══════════════ */}
			<UseCaseFAQ faqs={[
				{ question: 'What databases are supported?', textAnswer: 'PostgreSQL. Attach any PostgreSQL instance as a workspace resource.' },
				{ question: 'Are queries safe from SQL injection?', textAnswer: 'Yes. TypeScript string interpolation is converted to parameterized queries. Python uses positional arguments.' },
				{ question: 'Who can access data tables?', textAnswer: 'All workspace members. Credentials are managed internally and never exposed.' },
				{ question: 'Can I use data tables from full-code apps?', textAnswer: 'Yes. Whitelist tables in raw_app.yaml and query them from backend runnables.' },
				{ question: 'Can I use DuckDB with data tables?', textAnswer: 'Yes. DuckDB scripts can ATTACH a datatable and query it with standard SQL.' },
			]} />

			{/* ═══════════════ Transition CTA ═══════════════ */}
			<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-16">
				<motion.div {...fadeIn} className="text-center py-16 px-8 rounded-3xl bg-gradient-to-br from-blue-50 to-blue-100 dark:from-blue-950 dark:to-gray-900 border border-blue-200 dark:border-blue-800">
					<h2 className="text-3xl font-bold text-gray-900 dark:text-white mb-4">Build apps on top of your data</h2>
					<p className="text-gray-600 dark:text-gray-300 mb-8 max-w-2xl mx-auto text-lg">
						Connect datatables to custom React or Svelte frontends, or use the low-code app builder for instant CRUD interfaces.
					</p>
					<div className="flex flex-col sm:flex-row gap-4 justify-center">
						<Link
							to="/product/app-builder"
							className="inline-flex items-center gap-2 px-6 py-3 rounded-lg bg-blue-600 text-white font-medium hover:bg-blue-700 transition-colors !no-underline"
						>
							Explore the app builder
							<ArrowRight className="w-4 h-4" />
						</Link>
						<a
							href="https://app.windmill.dev"
							target="_blank"
							rel="noopener noreferrer"
							className="inline-flex items-center gap-2 px-6 py-3 rounded-lg border border-gray-300 dark:border-gray-600 text-gray-700 dark:text-gray-200 font-medium hover:border-gray-400 dark:hover:border-gray-500 transition-colors !no-underline"
						>
							Try Windmill free
						</a>
					</div>
				</motion.div>
			</div>
		</div>
	);
}
