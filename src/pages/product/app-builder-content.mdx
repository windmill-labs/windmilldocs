---
title: "Full-code frontend builder"
description: "How do I build custom frontends with Windmill? Write React or Svelte apps connected to backend runnables in 20+ languages, with local dev, type safety, and one-command deploy."
---

import React, { useState } from 'react';
import Link from '@docusaurus/Link';
import { motion } from 'framer-motion';
import { ArrowRight, BookOpen, Terminal, FileCode, Layers, Play, Plug, Database, Zap, Bot, Globe } from 'lucide-react';
import { SiReact, SiSvelte } from 'react-icons/si';
import UseCaseFAQ from '../../components/use-cases/UseCaseFAQ';

export const fadeIn = {
	initial: { opacity: 0, y: 30 },
	whileInView: { opacity: 1, y: 0 },
	viewport: { once: true },
	transition: { duration: 0.5 }
};

export const stagger = (delay = 0) => ({
	...fadeIn,
	transition: { duration: 0.5, delay }
});

{/* ── Section helper ── */}

export function Section({ title, desc, docLink, image, reverse, children }) {
	return (
		<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-16">
			<motion.div
				{...fadeIn}
				className="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center"
			>
				<div className={reverse ? 'lg:order-2' : ''}>
					<h2 className="text-3xl sm:text-4xl font-bold text-gray-900 dark:text-white mb-4">{title}</h2>
					<p className="text-lg text-gray-600 dark:text-gray-300 mb-4">{desc}</p>
					{docLink && (
						<Link
							to={docLink}
							className="inline-flex items-center gap-1.5 text-blue-600 dark:text-blue-400 text-sm font-medium hover:underline !no-underline"
						>
							<BookOpen className="w-4 h-4" /> Read the docs
						</Link>
					)}
				</div>
				<div className={reverse ? 'lg:order-1' : ''}>
					{image ? (
						<img
							src={image}
							alt={title}
							className="rounded-2xl border border-gray-200 dark:border-gray-700 w-full shadow-lg"
						/>
					) : (
						<div className="w-full h-64 rounded-2xl bg-gray-100 dark:bg-gray-800 border border-gray-200 dark:border-gray-700 flex items-center justify-center">
							<span className="text-gray-400 dark:text-gray-500 text-sm">Screenshot coming soon</span>
						</div>
					)}
				</div>
			</motion.div>
			{children}
		</div>
	);
}

{/* ── Tab section ── */}

export function TabSection({ title, subtitle, features }) {
	const [active, setActive] = useState(0);
	return (
		<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-16">
			<motion.div {...fadeIn} className="mb-16">
				<h2 className="text-3xl sm:text-4xl font-bold text-gray-900 dark:text-white mb-4">
					{title}
				</h2>
				{subtitle && (
					<p className="text-lg text-gray-600 dark:text-gray-300">{subtitle}</p>
				)}
			</motion.div>

			<div className="grid grid-cols-1 md:grid-cols-[280px_1fr] gap-6">
				<div className="flex flex-col gap-2">
					{features.map((feat, i) => (
						<button
							key={feat.label || feat.title}
							onClick={() => setActive(i)}
							className={`flex items-center gap-3 px-4 py-4 rounded-xl text-left transition-colors border ${
								i === active
									? 'bg-blue-600 text-white border-blue-600'
									: 'bg-white dark:bg-gray-950 text-gray-700 dark:text-gray-300 border-gray-200 dark:border-gray-700 hover:border-gray-300 dark:hover:border-gray-600'
							}`}
						>
							<feat.icon
								className={`w-5 h-5 flex-shrink-0 ${
									i === active ? 'text-white' : 'text-blue-500'
								}`}
							/>
							<span className="font-medium text-sm">
								{feat.label || feat.title}
							</span>
						</button>
					))}
				</div>

				<div className="p-8 rounded-2xl border border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-950 relative overflow-hidden">
					{features.map((feat, i) => (
						<div
							key={feat.label || feat.title}
							className={`transition-opacity duration-200 ${
								i === active
									? 'opacity-100 relative'
									: 'opacity-0 absolute inset-0 pointer-events-none p-8'
							}`}
						>
							<div className="flex items-center gap-3 mb-4">
								<div className="w-10 h-10 rounded-lg bg-blue-100 dark:bg-blue-900/30 flex items-center justify-center">
									<feat.icon className="w-5 h-5 text-blue-600 dark:text-blue-400" />
								</div>
								<h3 className="text-2xl font-semibold text-gray-900 dark:text-white">
									{feat.title}
								</h3>
							</div>
							<p className="text-gray-600 dark:text-gray-300 text-lg mb-4">
								{feat.desc}
							</p>
							{feat.content ? (
								feat.content
							) : feat.image ? (
								<img
									src={feat.image}
									alt={feat.title}
									className="rounded-lg border border-gray-200 dark:border-gray-700 w-full"
								/>
							) : (
								<div className="w-full h-48 rounded-lg bg-gray-100 dark:bg-gray-800 border border-gray-200 dark:border-gray-700 flex items-center justify-center">
									<span className="text-gray-400 dark:text-gray-500 text-sm">
										Screenshot coming soon
									</span>
								</div>
							)}
							{feat.docLink && (
								<div className="flex justify-end mt-3">
									<Link
										to={feat.docLink}
										className="inline-flex items-center gap-1.5 text-blue-600 dark:text-blue-400 text-sm font-medium hover:underline !no-underline"
									>
										<BookOpen className="w-4 h-4" /> Read the docs
									</Link>
								</div>
							)}
						</div>
					))}
				</div>
			</div>
		</div>
	);
}

{/* ── Code block helper ── */}

export function CodeBlock({ code, language }) {
	return (
		<pre className="rounded-lg bg-gray-900 text-gray-100 p-4 overflow-x-auto text-sm">
			<code>{code}</code>
		</pre>
	);
}

{/* ── Local dev features data ── */}

export const localDevFeatures = [
	{
		icon: Terminal,
		label: 'wmill app new',
		title: 'Scaffold a project',
		desc: <span>Run <code>wmill app new</code> to create a new full-code app. An interactive wizard prompts for the app path, framework (React 19/18 or Svelte 5), and optional data table configuration. The CLI generates the project structure, installs dependencies, and creates the <code>raw_app.yaml</code> config.</span>,
		docLink: '/docs/full_code_apps/cli_workflow',
	},
	{
		icon: Play,
		label: 'wmill app dev',
		title: 'Local dev server',
		desc: <span>Run <code>wmill app dev</code> to start a local development server with hot module replacement. Frontend changes reload instantly. Backend runnables execute on Windmill workers with results streamed back via WebSocket. The <code>wmill.d.ts</code> type definitions regenerate automatically when runnables change.</span>,
		docLink: '/docs/full_code_apps/cli_workflow',
	},
	{
		icon: Zap,
		label: 'Type generation',
		title: 'Auto-generated types',
		desc: <span>The dev server generates <code>wmill.d.ts</code> with typed signatures for every backend runnable. Parameter types and return types are inferred from your code. Your editor provides autocomplete and type checking when calling <code>backend.my_runnable()</code> without any manual configuration.</span>,
		docLink: '/docs/full_code_apps/frontend',
	},
	{
		icon: Bot,
		label: 'Claude Code & Codex',
		title: 'Claude Code & Codex',
		desc: <span>Run <code>wmill app generate-agents</code> to create <code>AGENTS.md</code> and <code>DATATABLES.md</code> context files. <Link to="/docs/misc/guides/local_dev_with_ai" className="text-blue-600 dark:text-blue-400 hover:underline">Claude Code, Codex, and Cursor</Link> read these files to understand your project structure, available runnables, and data table schemas. Claude Code skills let AI write valid frontend and backend code for your app out of the box.</span>,
		docLink: '/docs/misc/guides/local_dev_with_ai',
	},
];

{/* ── Dev mode switch section ── */}

export function DevModeSection() {
	const [mode, setMode] = useState('local');
	return (
		<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-16">
			<motion.div {...fadeIn}>
				<h2 className="text-3xl sm:text-4xl font-bold text-gray-900 dark:text-white mb-4">Develop from the Windmill UI or locally</h2>
				<p className="text-lg text-gray-600 dark:text-gray-300 mb-6">
					Build full-code apps directly in the browser or use the CLI for a full local development workflow with your own editor and AI tools.
				</p>
				<div className="inline-flex rounded-lg border border-gray-200 dark:border-gray-700 p-1 mb-8 bg-gray-50 dark:bg-gray-900">
					<button
						onClick={() => setMode('ui')}
						className={`flex items-center gap-2 px-4 py-2 rounded-md text-sm font-medium transition-colors ${
							mode === 'ui'
								? 'bg-white dark:bg-gray-800 text-gray-900 dark:text-white shadow-sm border border-gray-200 dark:border-gray-600'
								: 'text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:hover:text-gray-300 border border-transparent'
						}`}
					>
						<Globe className="w-4 h-4" />
						Windmill UI
					</button>
					<button
						onClick={() => setMode('local')}
						className={`flex items-center gap-2 px-4 py-2 rounded-md text-sm font-medium transition-colors ${
							mode === 'local'
								? 'bg-white dark:bg-gray-800 text-gray-900 dark:text-white shadow-sm border border-gray-200 dark:border-gray-600'
								: 'text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:hover:text-gray-300 border border-transparent'
						}`}
					>
						<Terminal className="w-4 h-4" />
						Local dev
					</button>
				</div>
			</motion.div>

			{mode === 'ui' ? (
				<div>
					<p className="text-gray-600 dark:text-gray-300 text-lg mb-6">
						Create and edit apps directly in the <Link to="/docs/full_code_apps/ui_editor" className="text-blue-600 dark:text-blue-400 hover:underline">Windmill browser editor</Link>. File tree, code editor, runnables panel, data table config, and preview without deployment.
					</p>
					{/* Placeholder: screenshot of the Windmill UI full-code app editor showing the file tree
						on the left, code editor in the center, and runnables panel on the right.
						Save as static/img/money-pages/full-code-ui-editor.webp */}
					<div className="w-full h-80 rounded-2xl bg-gray-100 dark:bg-gray-800 border border-gray-200 dark:border-gray-700 flex items-center justify-center">
						<span className="text-gray-400 dark:text-gray-500 text-sm">Screenshot coming soon</span>
					</div>
					<div className="flex justify-end mt-3">
						<Link
							to="/docs/full_code_apps/ui_editor"
							className="inline-flex items-center gap-1.5 text-blue-600 dark:text-blue-400 text-sm font-medium hover:underline !no-underline"
						>
							<BookOpen className="w-4 h-4" /> Read the docs
						</Link>
					</div>
				</div>
			) : (
				<div>
					<div className="grid grid-cols-1 md:grid-cols-[280px_1fr] gap-6">
						<DevLocalTabs />
					</div>
				</div>
			)}
		</div>
	);
}

export function DevLocalTabs() {
	const [active, setActive] = useState(0);
	const features = localDevFeatures;
	return (
		<>
			<div className="flex flex-col gap-2">
				{features.map((feat, i) => (
					<button
						key={feat.label}
						onClick={() => setActive(i)}
						className={`flex items-center gap-3 px-4 py-4 rounded-xl text-left transition-colors border ${
							i === active
								? 'bg-blue-600 text-white border-blue-600'
								: 'bg-white dark:bg-gray-950 text-gray-700 dark:text-gray-300 border-gray-200 dark:border-gray-700 hover:border-gray-300 dark:hover:border-gray-600'
						}`}
					>
						<feat.icon
							className={`w-5 h-5 flex-shrink-0 ${
								i === active ? 'text-white' : 'text-blue-500'
							}`}
						/>
						<span className="font-medium text-sm">{feat.label}</span>
					</button>
				))}
			</div>
			<div className="p-8 rounded-2xl border border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-950 relative overflow-hidden">
				{features.map((feat, i) => (
					<div
						key={feat.label}
						className={`transition-opacity duration-200 ${
							i === active
								? 'opacity-100 relative'
								: 'opacity-0 absolute inset-0 pointer-events-none p-8'
						}`}
					>
						<div className="flex items-center gap-3 mb-4">
							<div className="w-10 h-10 rounded-lg bg-blue-100 dark:bg-blue-900/30 flex items-center justify-center">
								<feat.icon className="w-5 h-5 text-blue-600 dark:text-blue-400" />
							</div>
							<h3 className="text-2xl font-semibold text-gray-900 dark:text-white">{feat.title}</h3>
						</div>
						<p className="text-gray-600 dark:text-gray-300 text-lg mb-4">{feat.desc}</p>
						{feat.docLink && (
							<div className="flex justify-end mt-3">
								<Link
									to={feat.docLink}
									className="inline-flex items-center gap-1.5 text-blue-600 dark:text-blue-400 text-sm font-medium hover:underline !no-underline"
								>
									<BookOpen className="w-4 h-4" /> Read the docs
								</Link>
							</div>
						)}
					</div>
				))}
			</div>
		</>
	);
}

{/* ── Main page ── */}

export default function FrontendBuilderContent({ title, description }) {
	return (
		<div className="pt-32 max-w-full">
			{/* ═══════════════ Hero ═══════════════ */}
			<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 pt-16 pb-8">
				<div className="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
					<motion.div {...fadeIn}>
						<h1 className="!text-4xl sm:!text-5xl lg:!text-6xl !font-semibold !tracking-tight text-gray-900 dark:text-white mb-6">
							Full-code frontends connected to your backend
						</h1>
						<p className="text-xl text-gray-600 dark:text-gray-100 mb-8">
							Build custom UIs in React or Svelte. Call backend runnables in 20+ languages through an auto-generated, type-safe API. Develop locally, deploy to Windmill.
						</p>
						<div className="flex flex-col sm:flex-row gap-4">
							<a
								href="https://app.windmill.dev"
								target="_blank"
								rel="noopener noreferrer"
								className="inline-flex items-center gap-2 px-6 py-3 rounded-lg bg-blue-600 text-white font-medium hover:bg-blue-700 transition-colors !no-underline"
							>
								Try Windmill free
								<ArrowRight className="w-4 h-4" />
							</a>
							<Link
								to="/docs/getting_started/full_code_apps_quickstart"
								className="inline-flex items-center gap-2 px-6 py-3 rounded-lg border border-gray-300 dark:border-gray-600 text-gray-700 dark:text-gray-200 font-medium hover:border-gray-400 dark:hover:border-gray-500 transition-colors !no-underline"
							>
								Read the docs
							</Link>
						</div>
					</motion.div>

					<motion.div {...stagger(0.2)} className="hidden lg:block">
						{/* Placeholder: screenshot of a full-code React app running in the browser with
							the Windmill backend connected. Ideally show a data table or dashboard UI
							calling backend runnables. Save as static/img/money-pages/full-code-app.webp */}
						{/* Placeholder: screenshot of a full-code React app, ~1200x800px, save as /static/img/money-pages/frontend.webp */}
						<div className="w-full h-80 rounded-2xl bg-gray-100 dark:bg-gray-800 border border-gray-200 dark:border-gray-700 flex items-center justify-center">
							<span className="text-gray-400 dark:text-gray-500 text-sm">Screenshot coming soon</span>
						</div>
					</motion.div>
				</div>
			</div>

			{/* ═══════════════ Backend runnables intro ═══════════════ */}
			<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-16">
				<motion.div {...fadeIn}>
					<h2 className="text-3xl sm:text-4xl font-bold text-gray-900 dark:text-white mb-4">Your backend logic runs on Windmill</h2>
					<p className="text-lg text-gray-600 dark:text-gray-300 mb-8">
						Windmill executes backend functions ("runnables") on isolated workers in 20+ languages. Full-code apps connect your frontend to these runnables through a typed API.
					</p>
				</motion.div>
				<div className="grid grid-cols-1 md:grid-cols-2 gap-6 mb-12">
					<motion.div {...stagger(0.1)}>
						<div className="p-5 rounded-2xl border border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-950 h-full">
							<div className="flex items-center gap-3 mb-2">
								<div className="w-9 h-9 rounded-lg bg-blue-100 dark:bg-blue-900/30 flex items-center justify-center flex-shrink-0">
									<FileCode className="w-4 h-4 text-blue-600 dark:text-blue-400" />
								</div>
								<h3 className="text-lg font-semibold text-gray-900 dark:text-white">
									<Link to="/product/script-editor" className="text-gray-900 dark:text-white hover:text-blue-600 dark:hover:text-blue-400">Scripts</Link>
								</h3>
							</div>
							<p className="text-sm text-gray-600 dark:text-gray-300">
								Single functions in TypeScript, Python, Go, SQL, Bash, and 15+ other languages. Each script runs on an isolated worker.
							</p>
						</div>
					</motion.div>
					<motion.div {...stagger(0.2)}>
						<div className="p-5 rounded-2xl border border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-950 h-full">
							<div className="flex items-center gap-3 mb-2">
								<div className="w-9 h-9 rounded-lg bg-violet-100 dark:bg-violet-900/30 flex items-center justify-center flex-shrink-0">
									<Layers className="w-4 h-4 text-violet-600 dark:text-violet-400" />
								</div>
								<h3 className="text-lg font-semibold text-gray-900 dark:text-white">
									<Link to="/product/flow-editor" className="text-gray-900 dark:text-white hover:text-blue-600 dark:hover:text-blue-400">Flows</Link>
								</h3>
							</div>
							<p className="text-sm text-gray-600 dark:text-gray-300">
								Multi-step workflows that chain scripts into DAGs with branching, loops, retries, and approval steps.
							</p>
						</div>
					</motion.div>
				</div>
			</div>

			{/* ═══════════════ Full-code apps concept ═══════════════ */}
			<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-16">
				<motion.div {...fadeIn} className="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
					<div>
						<div className="flex items-center gap-4 mb-4">
							<SiReact className="w-7 h-7 text-[#61DAFB]" />
							<SiSvelte className="w-7 h-7 text-[#FF3E00]" />
						</div>
						<h2 className="text-3xl sm:text-4xl font-bold text-gray-900 dark:text-white mb-4">Full-code apps: custom frontends on the Windmill platform</h2>
						<p className="text-lg text-gray-600 dark:text-gray-300 mb-4">
							Windmill's <Link to="/docs/full_code_apps" className="text-blue-600 dark:text-blue-400 hover:underline">full-code app builder</Link> lets you write a React or Svelte frontend that connects to these runnables and runs on the Windmill platform. Full control over the UI, while Windmill handles execution, auth, and hosting. Build from the <Link to="/docs/full_code_apps/ui_editor" className="text-blue-600 dark:text-blue-400 hover:underline">browser editor</Link> or <Link to="/docs/full_code_apps/cli_workflow" className="text-blue-600 dark:text-blue-400 hover:underline">locally from the CLI</Link>.
						</p>
						<Link
							to="/docs/full_code_apps"
							className="inline-flex items-center gap-1.5 text-blue-600 dark:text-blue-400 text-sm font-medium hover:underline !no-underline"
						>
							<BookOpen className="w-4 h-4" /> Read the docs
						</Link>
					</div>
					<div>
						{/* Placeholder: screenshot of full-code apps on Windmill, ~1200x800px, save as /static/img/money-pages/frontend.webp */}
						<div className="w-full h-80 rounded-2xl bg-gray-100 dark:bg-gray-800 border border-gray-200 dark:border-gray-700 flex items-center justify-center">
							<span className="text-gray-400 dark:text-gray-500 text-sm">Screenshot coming soon</span>
						</div>
					</div>
				</motion.div>
			</div>

			{/* ═══════════════ Typed backend API ═══════════════ */}
			<Section
				title="Auto-generated typed API"
				desc={<span>Adding a script to the <code>backend/</code> folder makes it callable from the frontend. Windmill generates a <code>wmill.d.ts</code> with typed signatures inferred from your code, giving your editor autocomplete and type checking for <code>backend.my_runnable()</code> calls. Runnables execute on <Link to="/docs/core_concepts/worker_groups" className="text-blue-600 dark:text-blue-400 hover:underline">workers</Link> and stream results back via WebSocket.</span>}
				docLink="/docs/full_code_apps/frontend"
				reverse
			/>

			{/* ═══════════════ Datatables ═══════════════ */}
			<Section
				title="Datatables"
				desc={<span><Link to="/docs/core_concepts/persistent_storage/within_windmill#datatables" className="text-blue-600 dark:text-blue-400 hover:underline">Datatables</Link> are a layer on top of your own database (added as a <Link to="/docs/core_concepts/resources_and_types" className="text-blue-600 dark:text-blue-400 hover:underline">resource</Link>) that provides schema management, SQL runnables, and shared access across scripts, flows, and apps. Tables and schemas are accessible from backend runnables. The CLI auto-generates a <code>DATATABLES.md</code> context file so AI assistants understand the schema.</span>}
				docLink="/docs/core_concepts/persistent_storage/within_windmill#datatables"
			/>
			{/* TODO: screenshot of the datatables UI showing a table with columns, rows, and the schema editor. Save as static/img/money-pages/datatables.webp (approx 1200x800px) */}

			{/* ═══════════════ Develop from UI or locally (switch) ═══════════════ */}
			<DevModeSection />

			{/* ═══════════════ Deploy, version, and share ═══════════════ */}
			<Section
				title="Deploy, version, and share"
				desc={<span>Deploy with <code>wmill sync push</code> or from the browser editor. Each deployment is versioned. Sync to Git with <Link to="/docs/advanced/git_sync" className="text-blue-600 dark:text-blue-400 hover:underline">Git Sync</Link> and promote across <Link to="/product/versioning" className="text-blue-600 dark:text-blue-400 hover:underline">staging and production workspaces</Link>. Apps can be public, have custom URL paths, or use <Link to="/docs/full_code_apps/deployment" className="text-blue-600 dark:text-blue-400 hover:underline">execution policies</Link>.</span>}
				docLink="/docs/full_code_apps/deployment"
			/>

			{/* ═══════════════ Observability ═══════════════ */}
			<Section
				title="Observability"
				desc={<span>Every backend runnable call is logged as a <Link to="/docs/core_concepts/monitor_past_and_future_runs" className="text-blue-600 dark:text-blue-400 hover:underline">job</Link> with its inputs, outputs, duration, and status. The run history is searchable and filterable. Failed jobs show full error traces. <Link to="/docs/core_concepts/critical_alert_channels" className="text-blue-600 dark:text-blue-400 hover:underline">Critical alerts</Link> can notify on failures, and <Link to="/docs/core_concepts/audit_logs" className="text-blue-600 dark:text-blue-400 hover:underline">audit logs</Link> track every action across the workspace.</span>}
				docLink="/docs/core_concepts/monitor_past_and_future_runs"
				reverse
			/>

			{/* ═══════════════ FAQ ═══════════════ */}
			<UseCaseFAQ faqs={[
				{ question: 'What is the difference between full-code and low-code apps?', textAnswer: 'Full-code apps give you complete control over the frontend with React or Svelte, using the full framework ecosystem. Low-code apps use a drag-and-drop editor with pre-built components. Full-code apps are better for custom UIs and complex interactions. Low-code apps are faster for simple dashboards, forms, and CRUD interfaces.' },
				{ question: 'Can I use any npm package?', textAnswer: 'Yes. Full-code apps use standard package.json for dependencies. Install any npm package and use it in your frontend code. The CLI bundles everything with esbuild for deployment.' },
				{ question: 'How does the backend execute?', textAnswer: 'Backend runnables execute on Windmill workers, not in the browser. The frontend communicates with Windmill via WebSocket. Each call creates a job that runs on an isolated worker with full CPU and memory. Results stream back to the frontend in real time.' },
				{ question: 'Can I use Tailwind CSS?', textAnswer: 'Yes. You can add Tailwind or any CSS framework to your project. The frontend is standard React or Svelte with full control over styling.' },
				{ question: 'Can I build with AI coding assistants?', textAnswer: 'Yes. Running wmill app generate-agents creates AGENTS.md and DATATABLES.md context files that teach AI assistants the project structure, available runnables, and data table schemas. AI tools like Claude Code or Cursor can write valid frontend and backend code using these context files.' },
				{ question: 'Do I need a running Windmill instance for local dev?', textAnswer: 'Yes. The wmill app dev server connects to a Windmill instance to execute backend runnables on workers. Frontend hot reload works locally, but backend calls require a connected workspace (cloud or self-hosted).' },
			]} />

			{/* ═══════════════ Transition CTA ═══════════════ */}
			<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-16">
				<motion.div {...fadeIn} className="text-center py-16 px-8 rounded-3xl bg-gradient-to-br from-blue-50 to-blue-100 dark:from-blue-950 dark:to-gray-900 border border-blue-200 dark:border-blue-800">
					<h2 className="text-3xl font-bold text-gray-900 dark:text-white mb-4">Develop from your own environment</h2>
					<p className="text-gray-600 dark:text-gray-300 mb-8 max-w-2xl mx-auto text-lg">
						Use the CLI, VS Code extension, and AI coding assistants to build and test locally.
					</p>
					<div className="flex flex-col sm:flex-row gap-4 justify-center">
						<Link
							to="/product/local-dev"
							className="inline-flex items-center gap-2 px-6 py-3 rounded-lg bg-blue-600 text-white font-medium hover:bg-blue-700 transition-colors !no-underline"
						>
							Explore local development
							<ArrowRight className="w-4 h-4" />
						</Link>
						<a
							href="https://app.windmill.dev"
							target="_blank"
							rel="noopener noreferrer"
							className="inline-flex items-center gap-2 px-6 py-3 rounded-lg border border-gray-300 dark:border-gray-600 text-gray-700 dark:text-gray-200 font-medium hover:border-gray-400 dark:hover:border-gray-500 transition-colors !no-underline"
						>
							Try Windmill free
						</a>
					</div>
				</motion.div>
			</div>
		</div>
	);
}
