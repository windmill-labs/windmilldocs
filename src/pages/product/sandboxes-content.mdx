---
title: "AI sandboxes"
description: "How do I run AI agents safely in Windmill? Isolated sandboxes for Claude Code, Codex, or custom agents with persistent storage and pre-configured tools."
---

import React from 'react';
import Link from '@docusaurus/Link';
import { motion } from 'framer-motion';
import { ArrowRight, BookOpen, Shield, Lock, Bot, Server } from 'lucide-react';
import UseCaseFAQ from '../../components/use-cases/UseCaseFAQ';
import ProductCTA from '../../components/products/ProductCTA';

export const fadeIn = {
	initial: { opacity: 0, y: 30 },
	whileInView: { opacity: 1, y: 0 },
	viewport: { once: true },
	transition: { duration: 0.5 }
};

export const stagger = (delay = 0) => ({
	...fadeIn,
	transition: { duration: 0.5, delay }
});

{/* Section helper */}

export function Section({ title, desc, docLink, image, reverse, children }) {
	return (
		<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-16">
			<motion.div
				{...fadeIn}
				className="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center"
			>
				<div className={reverse ? 'lg:order-2' : ''}>
					<h2 className="text-3xl sm:text-4xl font-bold text-gray-900 dark:text-white mb-4">{title}</h2>
					<p className="text-lg text-gray-600 dark:text-gray-300 mb-4">{desc}</p>
					{docLink && (
						<Link
							to={docLink}
							className="inline-flex items-center gap-1.5 text-blue-600 dark:text-blue-400 text-sm font-medium hover:underline !no-underline"
						>
							<BookOpen className="w-4 h-4" /> Read the docs
						</Link>
					)}
				</div>
				<div className={reverse ? 'lg:order-1' : ''}>
					{image ? (
						<img
							src={image}
							alt={title}
							className="rounded-2xl border border-gray-200 dark:border-gray-700 w-full shadow-lg"
						/>
					) : (
						<div className="w-full h-64 rounded-2xl bg-gray-100 dark:bg-gray-800 border border-gray-200 dark:border-gray-700 flex items-center justify-center">
							<span className="text-gray-400 dark:text-gray-500 text-sm">Screenshot coming soon</span>
						</div>
					)}
				</div>
			</motion.div>
			{children}
		</div>
	);
}

{/* Main page */}

export default function SandboxesContent({ title, description }) {
	return (
		<div className="pt-32 max-w-full">
			{/* Hero */}
			<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 pt-16 pb-8">
				<div className="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
					<motion.div {...fadeIn}>
						<p className="text-sm font-medium text-blue-600 dark:text-blue-400 mb-2 tracking-wide uppercase">AI sandboxes</p>
						<h1 className="!text-4xl sm:!text-5xl lg:!text-6xl !font-semibold !tracking-tight text-gray-900 dark:text-white mb-6">
							Isolated environments for AI agents
						</h1>
						<p className="text-xl text-gray-600 dark:text-gray-100 mb-8">
							Run AI coding agents in sandboxes with persistent storage, pre-configured tools, and security boundaries.
						</p>
						<div className="flex flex-col sm:flex-row gap-4">
							<a
								href="https://app.windmill.dev"
								target="_blank"
								rel="noopener noreferrer"
								className="inline-flex items-center gap-2 px-6 py-3 rounded-lg bg-blue-600 text-white font-medium hover:bg-blue-700 transition-colors !no-underline"
							>
								Try Windmill free
								<ArrowRight className="w-4 h-4" />
							</a>
							<Link
								to="/docs/core_concepts/ai_sandbox"
								className="inline-flex items-center gap-2 px-6 py-3 rounded-lg border border-gray-300 dark:border-gray-600 text-gray-700 dark:text-gray-200 font-medium hover:border-gray-400 dark:hover:border-gray-500 transition-colors !no-underline"
							>
								Read the docs
							</Link>
						</div>
					</motion.div>

					<motion.div {...stagger(0.2)} className="hidden lg:block">
						{/* Placeholder: screenshot of AI sandbox configuration showing Claude Code running in an isolated environment, ~1200x800px, save as /static/img/money-pages/ai-sandbox-hero.webp */}
						<div className="w-full h-80 rounded-2xl bg-gray-100 dark:bg-gray-800 border border-gray-200 dark:border-gray-700 flex items-center justify-center">
							<span className="text-gray-400 dark:text-gray-500 text-sm">Screenshot coming soon</span>
						</div>
					</motion.div>
				</div>
			</div>

			{/* What are AI sandboxes */}
			<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-16">
				<motion.div {...fadeIn}>
					<h2 className="text-3xl sm:text-4xl font-bold text-gray-900 dark:text-white mb-4">What are AI sandboxes</h2>
					<p className="text-lg text-gray-600 dark:text-gray-300">
						AI sandboxes combine two Windmill features to create secure, persistent environments for AI coding agents. Any <Link to="/product/script-editor" className="text-blue-600 dark:text-blue-400 hover:underline">script</Link> can become a sandbox with two annotations: <code>// sandbox</code> for process isolation via <Link to="/docs/advanced/security_isolation" className="text-blue-600 dark:text-blue-400 hover:underline">NSJAIL</Link>, and <code>// volume: name path</code> for <Link to="/docs/core_concepts/volumes" className="text-blue-600 dark:text-blue-400 hover:underline">persistent storage</Link> across runs. This pattern works for any agent that operates on a local filesystem: Claude Code, Codex, OpenCode, or custom agents.
					</p>
				</motion.div>
			</div>

			{/* Claude Code */}
			<Section
				title="Claude Code"
				desc={<span>Windmill includes a built-in <Link to="/docs/core_concepts/ai_sandbox" className="text-blue-600 dark:text-blue-400 hover:underline">Claude Code template</Link> using <code>@anthropic-ai/claude-agent-sdk</code>. Select it from the script editor to get a pre-configured sandbox with session persistence: the session ID is stored in the volume so the agent resumes where it left off. Pass instructions and skill files as input parameters to give the agent project-specific context.</span>}
				docLink="/docs/core_concepts/ai_sandbox"
			/>

			{/* Codex and custom agents */}
			<Section
				title="Codex and custom agents"
				desc={<span>The same pattern works for <Link to="/docs/core_concepts/ai_sandbox" className="text-blue-600 dark:text-blue-400 hover:underline">OpenAI Codex (CLI)</Link> or any agent that operates on a local filesystem. Two annotations are all you need: <code>// sandbox</code> for isolation, and <code>// volume: name path</code> for persistence.</span>}
				docLink="/docs/core_concepts/ai_sandbox"
				reverse
			/>

			{/* Persistent volumes */}
			<Section
				title="Persistent volumes"
				desc={<span>Files are synced to <Link to="/docs/core_concepts/volumes" className="text-blue-600 dark:text-blue-400 hover:underline">workspace object storage</Link> (S3, Azure Blob, GCS). Exclusive leasing prevents concurrent writes. Use dynamic volume names with <code>{'$workspace'}</code> or <code>{'$args[userId]'}</code> to scope storage per user or workspace. Up to 10 volumes per script, with an LRU cache up to 10 GB per worker.</span>}
				docLink="/docs/core_concepts/volumes"
			/>

			{/* Process isolation */}
			<Section
				title="Process isolation"
				desc={<span>Sandboxes use <Link to="/docs/advanced/security_isolation" className="text-blue-600 dark:text-blue-400 hover:underline">NSJAIL</Link> for filesystem isolation, network restrictions, and resource limits (CPU, memory, disk). The <code>// sandbox</code> annotation enables it per script, or it can be forced instance-wide for all jobs. For lighter workloads, <Link to="/docs/advanced/security_isolation" className="text-blue-600 dark:text-blue-400 hover:underline">PID namespace isolation</Link> is available as a faster alternative with lower overhead.</span>}
				docLink="/docs/advanced/security_isolation"
				reverse
			/>

			{/* Resources and permissions */}
			<Section
				title="Resources and permissions"
				desc={<span>API keys and credentials are injected through <Link to="/docs/core_concepts/resources_and_types" className="text-blue-600 dark:text-blue-400 hover:underline">Windmill resources</Link>, so secrets never appear in code. <Link to="/product/rbac" className="text-blue-600 dark:text-blue-400 hover:underline">RBAC permissions</Link> control which scripts, flows, and resources each agent can access, and volume permissions (owner, read-only, read-write) scope storage access.</span>}
			/>

			{/* Observability */}
			<Section
				title="Observability"
				desc={<span>Every agent run is tracked with full <Link to="/product/observability" className="text-blue-600 dark:text-blue-400 hover:underline">observability</Link>: logs, execution history, and token usage for cost monitoring. Set up <Link to="/docs/core_concepts/critical_alerts" className="text-blue-600 dark:text-blue-400 hover:underline">alerts</Link> on failures or cost thresholds, and audit agent activity across workspaces.</span>}
				docLink="/product/observability"
				reverse
			/>

			{/* FAQ */}
			<UseCaseFAQ faqs={[
				{ question: 'Which AI agents are supported?', textAnswer: 'Claude Code and OpenAI Codex (CLI) have first-class support with dedicated templates. Any agent that operates on a local filesystem can run in a sandbox using the // sandbox and // volume annotations.' },
				{ question: 'How does session persistence work?', textAnswer: 'Each sandbox can attach a persistent volume. For Claude Code, the session ID is stored in the volume so the agent resumes from where it left off. Files are synced to object storage (S3, Azure Blob, GCS) between runs.' },
				{ question: 'What isolation does the sandbox provide?', textAnswer: 'Sandboxes use NSJAIL for filesystem isolation, network restrictions, and resource limits. The agent cannot see or access the host filesystem, other jobs, or the network beyond what is explicitly allowed. PID namespace isolation is available as a lighter alternative.' },
			]} />

			<ProductCTA />
		</div>
	);
}
