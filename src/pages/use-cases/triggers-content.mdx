---
title: "How to trigger scripts and flows with Windmill"
description: "How do I trigger scripts and flows with Windmill? Schedules, webhooks, Kafka, Postgres, websockets, emails and more — with retries and alerting built in."
---

import React, { useState } from 'react';
import Link from '@docusaurus/Link';
import { motion } from 'framer-motion';
import { ArrowRight, Clock, AlertTriangle, Eye, Calendar, Bell, Shield, Play, CheckCircle, Database, RefreshCw, Mail, Trash2, BookOpen, RotateCcw, Code, Code2, Plug, Globe, Rocket, Terminal, FolderPlus } from 'lucide-react';
import TabFeatureSection from '../../components/use-cases/TabFeatureSection';
import CodeBlock from '@theme/CodeBlock';
import UseCaseFAQ from '../../components/use-cases/UseCaseFAQ';
import CaseStudyCard from '../../components/use-cases/CaseStudyCard';
import UseCaseCarousel from '../../components/use-cases/UseCaseCarousel';
import LogoClouds from '../../landing/LogoClouds';
import GithubStarCount from '../../landing/GithubStarCount';
import { useColorMode } from '@docusaurus/theme-common';
import { cfaInstituteCaseStudy } from '../../data/case-studies/cfa-institute';
import { zoomCaseStudy } from '../../data/case-studies/zoom';

export const fadeIn = {
	initial: { opacity: 0, y: 30 },
	whileInView: { opacity: 1, y: 0 },
	viewport: { once: true },
	transition: { duration: 0.5 }
};

export const stagger = (delay = 0) => ({
	...fadeIn,
	transition: { duration: 0.5, delay }
});

export const journeyFeatures = [
	{
		id: 'write',
		icon: Code,
		label: '1. Write a script',
		title: 'Write a script in any language',
		desc: <span>Write a script in <Link to="/docs/getting_started/scripts_quickstart" className="text-blue-600 dark:text-blue-400 hover:underline">TypeScript, Python, Go, Bash, SQL</Link> or 20+ languages, from the Windmill UI or locally using the <Link to="/docs/advanced/cli" className="text-blue-600 dark:text-blue-400 hover:underline">CLI</Link>.</span>,
		image: '/img/money-pages/script-for-endpoint.webp',
		docLink: '/docs/script_editor',
	},
	{
		id: 'connect',
		icon: Plug,
		label: '2. Connect resources',
		title: 'Connect to databases and APIs',
		desc: <span>Connect <Link to="/docs/core_concepts/resources_and_types" className="text-blue-600 dark:text-blue-400 hover:underline">resources</Link> like PostgreSQL, MySQL, S3, Slack, or any of <Link to="/docs/integrations/integrations_on_windmill" className="text-blue-600 dark:text-blue-400 hover:underline">50+ integrations</Link>. Credentials are encrypted and injected at runtime.</span>,
		image: '/img/money-pages/resource.webp',
		docLink: '/docs/core_concepts/resources_and_types',
	},
	{
		id: 'deploy',
		icon: Rocket,
		label: '3. Deploy',
		title: 'Deploy from draft to production',
		desc: <span>Go from <Link to="/docs/core_concepts/draft_and_deploy" className="text-blue-600 dark:text-blue-400 hover:underline">draft to deployed</Link> in one click. Every save creates an immutable version with diff history and instant rollback. Sync with <Link to="/docs/advanced/deploy_gh_gl" className="text-blue-600 dark:text-blue-400 hover:underline">GitHub or GitLab</Link> for CI/CD.</span>,
		image: '/img/money-pages/deploy-workflow.webp',
		docLink: '/docs/core_concepts/draft_and_deploy',
	},
	{
		id: 'use',
		icon: Globe,
		label: '4. Use as endpoint',
		title: 'Use as a webhook, API or scheduled job',
		desc: <span>Every script gets a sync and async <Link to="/docs/core_concepts/webhooks" className="text-blue-600 dark:text-blue-400 hover:underline">webhook</Link>. Add a <Link to="/docs/core_concepts/scheduling" className="text-blue-600 dark:text-blue-400 hover:underline">cron schedule</Link> or trigger from <Link to="/docs/getting_started/triggers" className="text-blue-600 dark:text-blue-400 hover:underline">Kafka, SQS, Postgres CDC</Link> or any event source.</span>,
		image: '/img/money-pages/triggers.webp',
		docLink: '/docs/core_concepts/webhooks',
	},
	{
		id: 'monitor',
		icon: Eye,
		label: '5. Monitor',
		title: 'Full observability out of the box',
		desc: <span>Every execution is logged with inputs, outputs, duration and status. Filter <Link to="/docs/core_concepts/jobs" className="text-blue-600 dark:text-blue-400 hover:underline">runs</Link>, inspect logs, re-run with one click. Set up alerts on failures and view <Link to="/docs/core_concepts/audit_logs" className="text-blue-600 dark:text-blue-400 hover:underline">audit logs</Link>.</span>,
		image: '/img/money-pages/monitoring.webp',
		docLink: '/docs/core_concepts/jobs',
	},
];


export function CaseStudyAndEmbed() {
	return (
		<div>
			<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-16">
				<motion.div {...fadeIn} className="mb-12">
					<h2 className="text-3xl sm:text-4xl font-bold text-gray-900 dark:text-white mb-4">
						Triggers in production
					</h2>
					<p className="text-lg text-gray-600 dark:text-gray-300">
						From synthetic monitoring to environment hydration, teams rely on Windmill triggers for their most critical recurring jobs.
					</p>
				</motion.div>

				<div className="grid grid-cols-1 md:grid-cols-2 gap-8">
					<CaseStudyCard
						caseStudy={cfaInstituteCaseStudy}
						title="How CFA Institute monitors production with scheduled synthetic tests"
						description="CFA Institute runs 25 synthetic monitoring tests every 5 minutes on Windmill schedules, catching regressions before users do. Error handlers and recovery alerts keep the ops team informed in real time."
						from="triggers"
					/>
					<CaseStudyCard
						caseStudy={zoomCaseStudy}
						title="How Zoom hydrates demo environments with scheduled flows"
						description="Zoom's demo platform uses Windmill schedules to hydrate and refresh demo environments for 400+ solution engineers. Scheduled flows provision infrastructure, seed data and keep environments ready around the clock."
						from="triggers"
					/>
				</div>
			</div>
		</div>
	);
}

export default function ScheduledTasksContent({ title, description }) {
	const [scheduleView, setScheduleView] = useState('script');
	return (
		<div className="pt-32 max-w-full">
			{/* Hero */}
			<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 pt-16 pb-8">
				<div className="grid grid-cols-1 md:grid-cols-2 gap-12 items-center">
					<motion.div {...fadeIn}>
						<div className="mb-4"><GithubStarCount /></div>
						<h1 className="!text-4xl sm:!text-6xl !font-semibold !tracking-tight text-gray-900 dark:text-white mb-6">
							Trigger your most critical tasks with confidence
						</h1>
						<p className="text-xl text-gray-600 dark:text-gray-100 mb-8">
							For ops and engineering teams who run critical jobs. Trigger any script or workflow from schedules, webhooks, Kafka, Postgres, websockets and more — with error handlers and recovery logic powered by Windmill's <Link to="/docs/flows/flow_editor" className="text-blue-400 hover:underline !no-underline">workflow engine</Link>.
						</p>
						<div className="flex flex-col sm:flex-row gap-4">
							<a
								href="https://app.windmill.dev"
								target="_blank"
								rel="noopener noreferrer"
								className="inline-flex items-center gap-2 px-6 py-3 rounded-lg bg-blue-600 text-white font-medium hover:bg-blue-700 transition-colors !no-underline"
							>
								Try Windmill free
								<ArrowRight className="w-4 h-4" />
							</a>
							<Link
								to="/docs/core_concepts/scheduling"
								className="inline-flex items-center gap-2 px-6 py-3 rounded-lg border border-gray-300 dark:border-gray-600 text-gray-700 dark:text-gray-200 font-medium hover:border-gray-400 dark:hover:border-gray-500 transition-colors !no-underline"
							>
								Read the docs
							</Link>
						</div>
						<div className="mt-8 flex flex-col gap-3">
							<div className="flex items-center gap-3 text-base text-gray-600 dark:text-gray-100">
								<span className="text-blue-500 font-bold">✓</span>
								<span>Trigger scripts in <Link to="/docs/getting_started/scripts_quickstart" className="text-blue-500 hover:underline !no-underline">Python, TypeScript, SQL, Go, Bash</Link> or <Link to="/use-cases/data-pipelines" className="text-blue-500 hover:underline !no-underline">full workflows</Link></span>
							</div>
							<div className="flex items-center gap-3 text-base text-gray-600 dark:text-gray-100">
								<span className="text-blue-500 font-bold">✓</span>
								<span>Connect to <Link to="/docs/core_concepts/resources_and_types" className="text-blue-500 hover:underline !no-underline">databases, APIs, private packages</Link> and any resource in your infra</span>
							</div>
							<div className="flex items-center gap-3 text-base text-gray-600 dark:text-gray-100">
								<span className="text-blue-500 font-bold">✓</span>
								<span>Catch failures with <Link to="/docs/core_concepts/scheduling#error-handler" className="text-blue-500 hover:underline !no-underline">error handlers, retries</Link> and alerts</span>
							</div>
							<div className="flex items-center gap-3 text-base text-gray-600 dark:text-gray-100">
								<span className="text-blue-500 font-bold">✓</span>
								<span>Deploy in one click with <Link to="/docs/core_concepts/roles_and_permissions" className="text-blue-500 hover:underline !no-underline">RBAC, SSO</Link> and audit logs</span>
							</div>
						</div>
					</motion.div>

					<div className="hidden md:flex items-center justify-center">
						<div className="relative w-[480px] h-[480px]">
							{/* Orbit ring */}
							<div className="absolute inset-[60px] rounded-full border border-gray-200 dark:border-gray-700/50" />
							{/* Tick marks — all 12, fully inside the orbit */}
							<svg className="absolute inset-0 w-full h-full" style={{ pointerEvents: 'none' }}>
								{Array.from({ length: 12 }, (_, i) => {
									const angle = (i * 30 - 90) * Math.PI / 180;
									const r1 = 166;
									const r2 = 178;
									const cx = 240, cy = 240;
									return (
										<line key={`tick-${i}`}
											x1={cx + r1 * Math.cos(angle)} y1={cy + r1 * Math.sin(angle)}
											x2={cx + r2 * Math.cos(angle)} y2={cy + r2 * Math.sin(angle)}
											className="stroke-gray-400 dark:stroke-gray-500" strokeWidth={2} strokeLinecap="round"
										/>
									);
								})}
								{/* Lines from center to each card */}
								{[-90, 0, 90, 180].map((angle, i) => {
									const rad = (angle * Math.PI) / 180;
									return (
										<line key={`line-${i}`}
											x1={240 + 40 * Math.cos(rad)} y1={240 + 40 * Math.sin(rad)}
											x2={240 + 180 * Math.cos(rad)} y2={240 + 180 * Math.sin(rad)}
											className="stroke-gray-200 dark:stroke-gray-700/50" strokeWidth={1.5}
										/>
									);
								})}
							</svg>
							{/* Center Windmill logo */}
							<div className="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 z-10">
								<img src="/img/windmill.svg" alt="Windmill" className="w-20 h-20" />
							</div>
							{/* Clock hand — 12s full rotation */}
							<motion.div className="absolute z-[5]" style={{ left: 240, top: 240, transformOrigin: '0 0' }}
								animate={{ rotate: 360 }}
								transition={{ duration: 12, repeat: Infinity, ease: 'linear' }}
							>
								<div className="w-[2px] h-[120px] bg-blue-500 rounded-full" style={{ transform: 'translateX(-1px) translateY(-120px)' }} />
							</motion.div>
							{/* Center dot */}
							<div className="absolute z-[6] w-3 h-3 rounded-full bg-blue-500" style={{ left: 240 - 6, top: 240 - 6 }} />
							{/* Task cards on the orbit — trigger flow animation when hand passes */}
							{[
								{ label: 'DB backup', time: '12:00', Icon: Database, angle: -90, triggerDelay: 0 },
								{ label: 'Sync CRM', time: '03:00', Icon: RefreshCw, angle: 0, triggerDelay: 3 },
								{ label: 'Send report', time: '06:00', Icon: Mail, angle: 90, triggerDelay: 6 },
								{ label: 'Cleanup', time: '09:00', Icon: Trash2, angle: 180, triggerDelay: 9 },
							].map((task) => {
								const rad = (task.angle * Math.PI) / 180;
								const r = 180;
								const x = 240 + r * Math.cos(rad);
								const y = 240 + r * Math.sin(rad);
								return (
									<div key={task.label} className="absolute z-10"
										style={{ left: x, top: y, transform: 'translate(-50%, -50%)' }}
									>
										{/* Card container */}
										<motion.div
											className="relative rounded-xl bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 shadow-sm min-w-[110px] overflow-hidden whitespace-nowrap px-3 py-2 text-center"
											animate={{
												borderColor: ['rgb(229,231,235)', 'rgb(59,130,246)', 'rgb(34,197,94)', 'rgb(34,197,94)', 'rgb(229,231,235)'],
												boxShadow: ['0 1px 2px rgba(0,0,0,0.05)', '0 0 16px rgba(59,130,246,0.4)', '0 0 16px rgba(34,197,94,0.4)', '0 0 16px rgba(34,197,94,0.4)', '0 1px 2px rgba(0,0,0,0.05)'],
											}}
											transition={{ duration: 2.4, delay: task.triggerDelay, repeat: Infinity, repeatDelay: 9.6, times: [0, 0.12, 0.35, 0.7, 1] }}
										>
											<div className="flex items-center gap-1.5 justify-center mb-0.5">
												<task.Icon className="w-3.5 h-3.5 text-gray-400 dark:text-gray-500" />
												<span className="text-[11px] font-semibold text-gray-700 dark:text-gray-200">{task.label}</span>
											</div>
											{/* Shared slot: time ↔ Running... ↔ Success */}
											<div className="relative h-[14px]">
												{/* Time (default) */}
												<motion.div
													className="absolute inset-0 flex items-center justify-center"
													animate={{ opacity: [1, 0, 0, 0, 1] }}
													transition={{ duration: 2.4, delay: task.triggerDelay, repeat: Infinity, repeatDelay: 9.6, times: [0, 0.1, 0.35, 0.9, 1] }}
												>
													<span className="text-[10px] text-gray-400 font-mono">{task.time}</span>
												</motion.div>
												{/* Running... */}
												<motion.div
													className="absolute inset-0 flex items-center justify-center gap-1 text-blue-500"
													animate={{ opacity: [0, 1, 0, 0, 0] }}
													transition={{ duration: 2.4, delay: task.triggerDelay, repeat: Infinity, repeatDelay: 9.6, times: [0, 0.1, 0.34, 0.35, 1] }}
												>
													<motion.div
														animate={{ rotate: [0, 360] }}
														transition={{ duration: 1, repeat: Infinity, ease: 'linear' }}
													>
														<RefreshCw className="w-2.5 h-2.5" />
													</motion.div>
													<span className="text-[9px] font-medium">Running...</span>
												</motion.div>
												{/* Success */}
												<motion.div
													className="absolute inset-0 flex items-center justify-center gap-1 text-emerald-500"
													animate={{ opacity: [0, 0, 0, 1, 0] }}
													transition={{ duration: 2.4, delay: task.triggerDelay, repeat: Infinity, repeatDelay: 9.6, times: [0, 0.1, 0.34, 0.35, 0.9] }}
												>
													<CheckCircle className="w-2.5 h-2.5" />
													<span className="text-[9px] font-medium">Success</span>
												</motion.div>
											</div>
										</motion.div>
									</div>
								);
							})}
						</div>
					</div>
				</div>
			</div>

			<div className="mt-8 -mb-12 md:-mb-16"><LogoClouds /></div>

			{/* From scripts to pipelines */}
			<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-16">
				<motion.div {...fadeIn} className="mb-12">
					<h2 className="text-3xl sm:text-4xl font-bold text-gray-900 dark:text-white mb-4">
						From single scripts to complex data pipelines
					</h2>
					<p className="text-lg text-gray-600 dark:text-gray-300">
						Trigger anything: a simple <Link to="/docs/getting_started/scripts_quickstart" className="text-blue-600 dark:text-blue-400 hover:underline !no-underline">script</Link> that runs in seconds or a <Link to="/use-cases/data-pipelines" className="text-blue-600 dark:text-blue-400 hover:underline !no-underline">multi-step pipeline</Link> with branching, loops and error handling. Same platform, same reliability.</p>
				</motion.div>

				{/* Two columns */}
				<div className="flex flex-col md:flex-row gap-6 items-center md:min-h-[600px]">
					{[
						{ key: 'script', title: 'Trigger scripts', desc: 'Write scripts in 20+ languages and trigger them from schedules, webhooks, Kafka and more', img: '/img/money-pages/scripts-preview.webp', alt: 'Script triggered with execution history' },
						{ key: 'flow', title: 'Trigger multi-step workflows', desc: 'Build multi-step DAG pipelines and trigger them from any event source', img: '/img/money-pages/flow-preview.webp', alt: 'Data pipeline DAG with parallel branches' },
					].map((card) => (
						<button
							key={card.key}
							onClick={() => setScheduleView(card.key)}
							className={`text-left rounded-2xl border-2 overflow-hidden cursor-pointer bg-white dark:bg-gray-950 transition-all duration-500 ease-in-out ${
								scheduleView === card.key
									? 'border-blue-500 dark:border-blue-400 shadow-lg shadow-blue-500/10 flex-[2]'
									: 'border-gray-200 dark:border-gray-700 opacity-60 hover:opacity-80 flex-[1]'
							}`}
						>
							<div className="p-5 sm:p-6">
								<div className="mb-4">
									<h3 className="text-base font-semibold text-gray-900 dark:text-white">{card.title}</h3>
									{scheduleView === card.key && <p className="text-sm text-gray-500 dark:text-gray-400">{card.desc}</p>}
								</div>
								<img
									src={card.img}
									alt={card.alt}
									className={`w-full rounded-lg border border-gray-100 dark:border-gray-800 transition-all duration-500 ${
										scheduleView === card.key ? 'max-h-[500px]' : 'max-h-[200px] object-cover object-top'
									}`}
								/>
							</div>
						</button>
					))}
				</div>
			</div>

			{/* Journey: Write → Connect → Deploy → Use → Monitor */}
			<TabFeatureSection
				title="From script to production endpoint in 5 steps"
				subtitle={<>Write a function in <Link to="/docs/getting_started/scripts_quickstart" className="text-blue-600 dark:text-blue-400 hover:underline">any language</Link>, connect it to your <Link to="/docs/core_concepts/resources_and_types" className="text-blue-600 dark:text-blue-400 hover:underline">resources</Link>, deploy it, expose it as a <Link to="/docs/core_concepts/webhooks" className="text-blue-600 dark:text-blue-400 hover:underline">webhook</Link> or <Link to="/docs/core_concepts/scheduling" className="text-blue-600 dark:text-blue-400 hover:underline">scheduled job</Link>, and monitor every execution.</>}
				features={journeyFeatures}
			/>


			{/* Case study + embedded trigger */}
			<CaseStudyAndEmbed />

			{/* More use cases */}
			<div className="">
			<UseCaseCarousel
				current="triggers"
				subtitle="Triggers are just one use case. The same platform powers internal tools, data pipelines, AI agents and workflow automations."
			/>
			</div>

			{/* FAQ */}
			<div>
			<UseCaseFAQ faqs={[
				{ question: 'What trigger types does Windmill support?', textAnswer: 'Windmill supports standard cron expressions (6 fields including seconds) plus extended modifiers like nearest weekday (W), nth day-of-week (#), and last day-of-month (L). You can also use the visual cron builder to create schedules without memorizing cron syntax.' },
				{ question: 'Can I monitor and retry failed triggered jobs?', textAnswer: 'Yes. Every run is logged with inputs, outputs, duration and status. You can configure automatic retries with constant or exponential backoff, attach error handler scripts that run on failure, and recovery handlers that fire when a trigger succeeds again. Alerts can be routed to Slack, Teams, email or any webhook.' },
				{ question: 'How does Windmill compare to cron on a server?', textAnswer: 'Windmill triggers run on managed infrastructure with built-in logging, error handling, retries, alerting and a visual UI. Unlike raw cron, you get execution history, permission controls, and the ability to trigger the same scripts from other events like webhooks or Kafka.' },
				{ question: 'Can I trigger flows (multi-step pipelines)?', textAnswer: 'Yes. Anything that can be triggered manually in Windmill — scripts and flows — can be triggered. Flows let you chain multiple steps with branching, loops, error handling and approval steps, all from any trigger source.' },
				{ question: 'Are Windmill triggers free?', textAnswer: 'Windmill Community Edition is free and open-source with unlimited triggers. The Pro and Enterprise plans add features like audit logs, SAML SSO, priority support and more workers.' },
			]} />
			</div>

			{/* CTA */}
			<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-16">
				<motion.div {...fadeIn} className="text-center py-16 px-8 rounded-3xl bg-gradient-to-br from-blue-50 to-blue-100 dark:from-blue-950 dark:to-gray-900 border border-blue-200 dark:border-blue-800">
					<h2 className="text-3xl font-bold text-gray-900 dark:text-white mb-4">Start triggering tasks today</h2>
					<p className="text-gray-600 dark:text-gray-300 mb-8 max-w-2xl mx-auto text-lg">
						Get started for free on Windmill Cloud or <Link to="/docs/advanced/self_host" className="text-blue-600 dark:text-blue-400 hover:underline !no-underline">self-host</Link> the open-source version.
					</p>
					<div className="flex flex-col sm:flex-row gap-4 justify-center">
						<a
							href="https://app.windmill.dev"
							target="_blank"
							rel="noopener noreferrer"
							className="inline-flex items-center gap-2 px-6 py-3 rounded-lg bg-blue-600 text-white font-medium hover:bg-blue-700 transition-colors !no-underline"
						>
							Try Windmill free
							<ArrowRight className="w-4 h-4" />
						</a>
						<Link
							to="/pricing"
							className="inline-flex items-center gap-2 px-6 py-3 rounded-lg border border-gray-300 dark:border-gray-600 text-gray-700 dark:text-gray-200 font-medium hover:border-gray-400 dark:hover:border-gray-500 transition-colors !no-underline"
						>
							View pricing
						</Link>
					</div>
				</motion.div>
			</div>
		</div>
	);
}
