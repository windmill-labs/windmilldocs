---
title: "How to run scheduled tasks with Windmill"
description: "How do I run scheduled tasks with Windmill? Cron jobs with a visual builder, execution history, error handlers, recovery handlers and alerting."
---

import React from 'react';
import Link from '@docusaurus/Link';
import { motion } from 'framer-motion';
import { ArrowRight, Clock, AlertTriangle, Eye, Calendar, Bell, Shield } from 'lucide-react';

export const fadeIn = {
	initial: { opacity: 0, y: 30 },
	whileInView: { opacity: 1, y: 0 },
	viewport: { once: true },
	transition: { duration: 0.5 }
};

export const stagger = (delay = 0) => ({
	...fadeIn,
	transition: { duration: 0.5, delay }
});

export default function ScheduledTasksContent({ title, description }) {
	return (
		<div className="pt-32 max-w-full">
			{/* Hero */}
			<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 pt-16 pb-8">
				<div className="grid grid-cols-1 md:grid-cols-2 gap-12 items-center">
					<motion.div {...fadeIn}>
						<h1 className="!text-4xl sm:!text-6xl !font-semibold !tracking-tight text-gray-900 dark:text-white mb-6">
							Schedule any task with confidence
						</h1>
						<p className="text-xl text-gray-600 dark:text-gray-100 mb-8">
							For ops and platform teams who need production-grade cron with visibility. Define schedules for any script or flow with error handling, recovery and team-wide audit trails.
						</p>
						<div className="flex flex-col sm:flex-row gap-4">
							<a
								href="https://app.windmill.dev"
								target="_blank"
								rel="noopener noreferrer"
								className="inline-flex items-center gap-2 px-6 py-3 rounded-lg bg-blue-600 text-white font-medium hover:bg-blue-700 transition-colors !no-underline"
							>
								Try Windmill free
								<ArrowRight className="w-4 h-4" />
							</a>
							<Link
								to="/docs/core_concepts/scheduling"
								className="inline-flex items-center gap-2 px-6 py-3 rounded-lg border border-gray-300 dark:border-gray-600 text-gray-700 dark:text-gray-200 font-medium hover:border-gray-400 dark:hover:border-gray-500 transition-colors !no-underline"
							>
								Read the docs
							</Link>
						</div>
						<div className="mt-8 flex flex-col gap-3">
							<div className="flex items-center gap-3 text-base text-gray-600 dark:text-gray-100">
								<span className="text-blue-500 font-bold">✓</span>
								<span><span className="text-blue-500">Any language, any schedule</span> — Python, TypeScript, SQL, Go or Bash on any cron expression</span>
							</div>
							<div className="flex items-center gap-3 text-base text-gray-600 dark:text-gray-100">
								<span className="text-blue-500 font-bold">✓</span>
								<span><span className="text-blue-500">Error and recovery handlers</span> — automated alerts to Slack, Teams or email</span>
							</div>
							<div className="flex items-center gap-3 text-base text-gray-600 dark:text-gray-100">
								<span className="text-blue-500 font-bold">✓</span>
								<span><span className="text-blue-500">Enterprise controls</span> — RBAC, audit logs, SSO and team-wide visibility</span>
							</div>
							<div className="flex items-center gap-3 text-base text-gray-600 dark:text-gray-100">
								<span className="text-blue-500 font-bold">✓</span>
								<span><span className="text-blue-500">Part of the Windmill platform</span> — schedule scripts, flows, pipelines and agents</span>
							</div>
						</div>
					</motion.div>

					<div className="hidden md:flex items-center justify-center">
						<div className="relative w-[480px] h-[480px]">
							{/* Orbit ring */}
							<div className="absolute inset-[60px] rounded-full border border-gray-200 dark:border-gray-700/50" />
							{/* Tick marks — all 12, fully inside the orbit */}
							<svg className="absolute inset-0 w-full h-full" style={{ pointerEvents: 'none' }}>
								{Array.from({ length: 12 }, (_, i) => {
									const angle = (i * 30 - 90) * Math.PI / 180;
									const r1 = 166;
									const r2 = 178;
									const cx = 240, cy = 240;
									return (
										<line key={`tick-${i}`}
											x1={cx + r1 * Math.cos(angle)} y1={cy + r1 * Math.sin(angle)}
											x2={cx + r2 * Math.cos(angle)} y2={cy + r2 * Math.sin(angle)}
											className="stroke-gray-400 dark:stroke-gray-500" strokeWidth={2} strokeLinecap="round"
										/>
									);
								})}
								{/* Lines from center to each card */}
								{[-90, 0, 90, 180].map((angle, i) => {
									const rad = (angle * Math.PI) / 180;
									return (
										<line key={`line-${i}`}
											x1={240 + 40 * Math.cos(rad)} y1={240 + 40 * Math.sin(rad)}
											x2={240 + 180 * Math.cos(rad)} y2={240 + 180 * Math.sin(rad)}
											className="stroke-gray-200 dark:stroke-gray-700/50" strokeWidth={1.5}
										/>
									);
								})}
							</svg>
							{/* Center Windmill logo */}
							<div className="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 z-10">
								<img src="/img/windmill.svg" alt="Windmill" className="w-16 h-16" />
							</div>
							{/* Clock hand — 12s full rotation, triggers each card at 12/3/6/9 o'clock */}
							<motion.div className="absolute z-[5]" style={{ left: 240, top: 240, transformOrigin: '0 0' }}
								animate={{ rotate: 360 }}
								transition={{ duration: 12, repeat: Infinity, ease: 'linear' }}
							>
								<div className="w-[2px] h-[120px] bg-blue-500 rounded-full" style={{ transform: 'translateX(-1px) translateY(-120px)' }} />
							</motion.div>
							{/* Center dot */}
							<div className="absolute z-[6] w-3 h-3 rounded-full bg-blue-500" style={{ left: 240 - 6, top: 240 - 6 }} />
							{/* Task cards on the orbit — pulse when hand passes (every 3s per card, 12s cycle) */}
							{[
								{ label: 'DB backup', time: '12:00', angle: -90, triggerDelay: 0 },
								{ label: 'Sync CRM', time: '03:00', angle: 0, triggerDelay: 3 },
								{ label: 'Send report', time: '06:00', angle: 90, triggerDelay: 6 },
								{ label: 'Cleanup', time: '09:00', angle: 180, triggerDelay: 9 },
							].map((task) => {
								const rad = (task.angle * Math.PI) / 180;
								const r = 180;
								const x = 240 + r * Math.cos(rad);
								const y = 240 + r * Math.sin(rad);
								return (
									<div key={task.label} className="absolute z-10"
										style={{ left: x, top: y, transform: 'translate(-50%, -50%)' }}
									>
										<motion.div
											className="px-3 py-2 rounded-xl bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 shadow-sm text-center min-w-[90px]"
											animate={{
												borderColor: ['rgb(229,231,235)', 'rgb(59,130,246)', 'rgb(59,130,246)', 'rgb(229,231,235)'],
												boxShadow: ['0 1px 2px rgba(0,0,0,0.05)', '0 0 16px rgba(59,130,246,0.4)', '0 0 16px rgba(59,130,246,0.4)', '0 1px 2px rgba(0,0,0,0.05)'],
											}}
											transition={{ duration: 1.2, delay: task.triggerDelay, repeat: Infinity, repeatDelay: 10.8, times: [0, 0.2, 0.7, 1] }}
										>
											<div className="text-[11px] font-semibold text-gray-700 dark:text-gray-200">{task.label}</div>
											<div className="text-[10px] text-gray-400 font-mono">{task.time}</div>
										</motion.div>
									</div>
								);
							})}
						</div>
					</div>
				</div>
			</div>

			{/* Why not raw cron */}
			<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-24">
				<motion.div {...fadeIn} className="mb-16">
					<h2 className="text-3xl sm:text-4xl font-bold text-gray-900 dark:text-white mb-4">
						Why not raw cron
					</h2>
					<p className="text-lg text-gray-600 dark:text-gray-300">
						Traditional crontabs work, but they come with operational pain that Windmill eliminates.
					</p>
				</motion.div>

				<div className="grid grid-cols-1 md:grid-cols-2 gap-8">
					{[
						{ icon: Eye, title: 'No execution history', desc: 'Cron requires you to build your own logging. Windmill logs every run with inputs, outputs, duration and status automatically.' },
						{ icon: AlertTriangle, title: 'No error handling', desc: 'A failed cron job is silent. Windmill schedules support error handlers that notify Slack, Teams, email or any webhook.' },
						{ icon: Clock, title: 'No manual runs', desc: 'Running a cron job outside its schedule is difficult. In Windmill, click "Run now" at any time with the same or different arguments.' },
						{ icon: Shield, title: 'No centralized UI', desc: 'Cron jobs live on individual servers with no team visibility. Windmill gives every schedule a dashboard with permissions.' },
					].map((item, i) => (
						<motion.div key={item.title} {...stagger(i * 0.08)} className="p-6 rounded-2xl border border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-950">
							<div className="w-12 h-12 rounded-xl bg-blue-100 dark:bg-blue-900/30 flex items-center justify-center mb-4">
								<item.icon className="w-6 h-6 text-blue-600 dark:text-blue-400" />
							</div>
							<h3 className="text-xl font-semibold text-gray-900 dark:text-white mb-2">{item.title}</h3>
							<p className="text-gray-600 dark:text-gray-300">{item.desc}</p>
						</motion.div>
					))}
				</div>
			</div>

			{/* Features */}
			<div className="bg-gray-50 dark:bg-gray-900/50">
				<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-24">
					<motion.div {...fadeIn} className="mb-16">
						<h2 className="text-3xl sm:text-4xl font-bold text-gray-900 dark:text-white mb-4">
							Built for production scheduling
						</h2>
					</motion.div>

					<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
						{[
							{ icon: Calendar, title: 'Advanced cron syntax', desc: 'Standard cron plus extended modifiers: nearest weekday (W), nth day-of-week (#), last day-of-month (L). Visual builder included.' },
							{ icon: Bell, title: 'Error & recovery handlers', desc: 'Error handler runs on failure. Recovery handler runs when a previously failing schedule succeeds. Pre-built Slack/Teams handlers.' },
							{ icon: Shield, title: 'Dynamic skip validation', desc: 'Skip runs based on custom logic — weekends, holidays, external conditions. If the handler returns false, the run is skipped.' },
							{ icon: Clock, title: 'Multiple schedules', desc: 'One primary schedule plus unlimited additional schedules with different arguments per script or flow.' },
							{ icon: Eye, title: 'Trigger scripts', desc: 'Poll external sources with persistent state. Return only new items since the last run — perfect when APIs don\'t support webhooks.' },
							{ icon: Calendar, title: 'One-off scheduling', desc: 'Schedule a single future execution without a recurring schedule. Run later, once.' },
						].map((item, i) => (
							<motion.div key={item.title} {...stagger(i * 0.08)} className="p-6 rounded-2xl border border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-950">
								<div className="w-12 h-12 rounded-xl bg-blue-100 dark:bg-blue-900/30 flex items-center justify-center mb-4">
									<item.icon className="w-6 h-6 text-blue-600 dark:text-blue-400" />
								</div>
								<h3 className="text-xl font-semibold text-gray-900 dark:text-white mb-2">{item.title}</h3>
								<p className="text-gray-600 dark:text-gray-300">{item.desc}</p>
							</motion.div>
						))}
					</div>
				</div>
			</div>

			{/* CTA */}
			<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-24">
				<motion.div {...fadeIn} className="text-center py-16 px-8 rounded-3xl bg-gradient-to-br from-blue-50 to-blue-100 dark:from-blue-950 dark:to-gray-900 border border-blue-200 dark:border-blue-800">
					<h2 className="text-3xl font-bold text-gray-900 dark:text-white mb-4">Start scheduling tasks today</h2>
					<p className="text-gray-600 dark:text-gray-300 mb-8 max-w-2xl mx-auto text-lg">
						Get started for free on Windmill Cloud or self-host the open-source version.
					</p>
					<div className="flex flex-col sm:flex-row gap-4 justify-center">
						<a
							href="https://app.windmill.dev"
							target="_blank"
							rel="noopener noreferrer"
							className="inline-flex items-center gap-2 px-6 py-3 rounded-lg bg-blue-600 text-white font-medium hover:bg-blue-700 transition-colors !no-underline"
						>
							Try Windmill free
							<ArrowRight className="w-4 h-4" />
						</a>
						<Link
							to="/pricing"
							className="inline-flex items-center gap-2 px-6 py-3 rounded-lg border border-gray-300 dark:border-gray-600 text-gray-700 dark:text-gray-200 font-medium hover:border-gray-400 dark:hover:border-gray-500 transition-colors !no-underline"
						>
							View pricing
						</Link>
					</div>
				</motion.div>
			</div>
		</div>
	);
}
