---
title: "How to build with scripts on Windmill"
description: "How do I run production scripts? Write scripts in TypeScript, Python, Go, Bash, SQL and trigger them from webhooks, schedules, queues or an auto-generated UI with full observability."
---

import React from 'react';
import Link from '@docusaurus/Link';
import { motion } from 'framer-motion';
import { Globe, RefreshCw, CheckCircle, BookOpen } from 'lucide-react';
import LazyVideo from '../../components/products/LazyVideo';
import UseCaseFAQ from '../../components/use-cases/UseCaseFAQ';
import CaseStudyCard from '../../components/use-cases/CaseStudyCard';
import UseCaseCarousel from '../../components/use-cases/UseCaseCarousel';
import LogoClouds from '../../landing/LogoClouds';
import GithubStarCount from '../../landing/GithubStarCount';
import HeroCTAButtons from '../../components/products/HeroCTAButtons';
import ProductCTA from '../../components/products/ProductCTA';
import { pantherLabsCaseStudy } from '../../data/case-studies/panther-labs';
import { cfaInstituteCaseStudy } from '../../data/case-studies/cfa-institute';

export const fadeIn = {
	initial: { opacity: 0, y: 30 },
	whileInView: { opacity: 1, y: 0 },
	viewport: { once: true },
	transition: { duration: 0.5 }
};

export const stagger = (delay = 0) => ({
	...fadeIn,
	transition: { duration: 0.5, delay }
});

export const scriptExamples = [
	{ category: 'Cybersecurity', title: 'Incident response automation', desc: 'Query your SIEM, enrich alerts with threat intelligence feeds, create tickets in PagerDuty and notify the on-call team on Slack.' },
	{ category: 'Finance', title: 'Payment reconciliation', desc: 'Pull transactions from Stripe, match them against your ledger in PostgreSQL and flag discrepancies for review.' },
	{ category: 'DevOps', title: 'Customer provisioning', desc: 'Spin up AWS accounts, configure IAM roles, deploy baseline infrastructure and notify the customer with onboarding credentials.' },
	{ category: 'E-commerce', title: 'Inventory synchronization', desc: 'Sync stock levels between your ERP, warehouse system and storefront every 15 minutes to prevent overselling.' },
	{ category: 'Healthcare', title: 'Lab result notifications', desc: 'Poll the LIMS for completed results, format them for clinician review and send secure notifications via the patient portal API.' },
	{ category: 'Environmental', title: 'Sensor data collection', desc: 'Ingest readings from IoT sensors, validate ranges, store in TimescaleDB and trigger alerts when thresholds are exceeded.' },
];

export default function ScriptsContent({ title, description }) {
	return (
		<div className="pt-32 max-w-full">
			{/* Hero */}
			<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 pt-16 pb-8">
				<div className="grid grid-cols-1 md:grid-cols-2 gap-12 items-center">
					<motion.div {...fadeIn}>
						<div className="mb-4"><GithubStarCount /></div>
						<h1 className="!text-4xl sm:!text-6xl !font-semibold !tracking-tight text-gray-900 dark:text-white mb-6">
							Run scripts as production endpoints
						</h1>
						<p className="text-xl text-gray-600 dark:text-gray-100 mb-8">
							Write a script in any language, Windmill turns it into an <Link to="/docs/core_concepts/webhooks" className="text-blue-400 hover:underline !no-underline">API endpoint</Link>, a <Link to="/docs/core_concepts/scheduling" className="text-blue-400 hover:underline !no-underline">scheduled job</Link>, or a <Link to="/docs/core_concepts/auto_generated_uis" className="text-blue-400 hover:underline !no-underline">shareable tool with a UI</Link>. No infrastructure to manage.
						</p>
						<HeroCTAButtons />
						<div className="mt-8 flex flex-col gap-3">
							<div className="flex items-center gap-3 text-base text-gray-600 dark:text-gray-100">
								<span className="text-blue-500 font-bold">✓</span>
								<span>Write in <Link to="/docs/getting_started/scripts_quickstart" className="text-blue-500 hover:underline !no-underline">TypeScript, Python, Go, Bash, SQL</Link> and 20+ languages</span>
							</div>
							<div className="flex items-center gap-3 text-base text-gray-600 dark:text-gray-100">
								<span className="text-blue-500 font-bold">✓</span>
								<span>Trigger from <Link to="/docs/core_concepts/webhooks" className="text-blue-500 hover:underline !no-underline">webhooks</Link>, <Link to="/docs/core_concepts/scheduling" className="text-blue-500 hover:underline !no-underline">schedules</Link>, queues, Postgres CDC or the auto-generated UI</span>
							</div>
							<div className="flex items-center gap-3 text-base text-gray-600 dark:text-gray-100">
								<span className="text-blue-500 font-bold">✓</span>
								<span>Production-ready with <Link to="/docs/script_editor/concurrency_limit" className="text-blue-500 hover:underline !no-underline">caching, concurrency limits</Link>, retries and <Link to="/docs/core_concepts/audit_logs" className="text-blue-500 hover:underline !no-underline">audit logs</Link></span>
							</div>
							<div className="flex items-center gap-3 text-base text-gray-600 dark:text-gray-100">
								<span className="text-blue-500 font-bold">✓</span>
								<span>Compose into <Link to="/use-cases/workflows" className="text-blue-500 hover:underline !no-underline">workflows</Link> or use as backends for <Link to="/use-cases/internal-tools" className="text-blue-500 hover:underline !no-underline">internal tools</Link></span>
							</div>
						</div>
					</motion.div>

					{/* Hero animation: script execution flow */}
					<div className="hidden md:flex items-center justify-center">
						<div className="relative w-[420px] h-[480px]">
							{/* Connecting lines */}
							<svg className="absolute inset-0 w-full h-full" style={{ pointerEvents: 'none' }}>
								<line x1="210" y1="72" x2="210" y2="148" className="stroke-gray-200 dark:stroke-gray-700/50" strokeWidth={2} strokeDasharray="6 4" />
								<line x1="210" y1="268" x2="210" y2="340" className="stroke-gray-200 dark:stroke-gray-700/50" strokeWidth={2} strokeDasharray="6 4" />
							</svg>

							{/* Step 1: Trigger */}
							<motion.div
								className="absolute top-0 left-1/2 -translate-x-1/2 w-[280px]"
								animate={{
									borderColor: ['rgb(229,231,235)', 'rgb(59,130,246)', 'rgb(229,231,235)'],
									boxShadow: ['0 1px 2px rgba(0,0,0,0.05)', '0 0 16px rgba(59,130,246,0.3)', '0 1px 2px rgba(0,0,0,0.05)'],
								}}
								transition={{ duration: 3, repeat: Infinity, repeatDelay: 7, times: [0, 0.15, 0.4] }}
								style={{ borderWidth: 1, borderStyle: 'solid', borderRadius: '0.75rem' }}
							>
								<div className="rounded-xl bg-white dark:bg-gray-800 px-5 py-4 text-center">
									<div className="flex items-center justify-center gap-2 mb-1">
										<Globe className="w-4 h-4 text-blue-500" />
										<span className="text-sm font-semibold text-gray-700 dark:text-gray-200">POST /api/run</span>
									</div>
									<span className="text-xs text-gray-400 font-mono">webhook trigger</span>
								</div>
							</motion.div>

							{/* Data flowing down line 1 */}
							<motion.div
								className="absolute left-1/2 -translate-x-1/2 w-2 h-2 rounded-full bg-blue-500"
								animate={{ top: [72, 148], opacity: [0, 1, 1, 0] }}
								transition={{ duration: 0.8, delay: 0.5, repeat: Infinity, repeatDelay: 9.2, times: [0, 0.1, 0.8, 1] }}
							/>

							{/* Step 2: Script execution */}
							<motion.div
								className="absolute top-[148px] left-1/2 -translate-x-1/2 w-[320px]"
								animate={{
									borderColor: ['rgb(229,231,235)', 'rgb(229,231,235)', 'rgb(59,130,246)', 'rgb(59,130,246)', 'rgb(229,231,235)'],
									boxShadow: ['0 1px 2px rgba(0,0,0,0.05)', '0 1px 2px rgba(0,0,0,0.05)', '0 0 20px rgba(59,130,246,0.3)', '0 0 20px rgba(59,130,246,0.3)', '0 1px 2px rgba(0,0,0,0.05)'],
								}}
								transition={{ duration: 5, repeat: Infinity, repeatDelay: 5, times: [0, 0.25, 0.3, 0.7, 0.85] }}
								style={{ borderWidth: 1, borderStyle: 'solid', borderRadius: '0.75rem' }}
							>
								<div className="rounded-xl bg-white dark:bg-gray-800 px-5 py-5">
									<div className="flex items-center justify-between mb-3">
										<span className="text-sm font-semibold text-gray-700 dark:text-gray-200">process_order.py</span>
										<motion.div
											animate={{ opacity: [0, 0, 1, 1, 0] }}
											transition={{ duration: 5, repeat: Infinity, repeatDelay: 5, times: [0, 0.25, 0.3, 0.7, 0.85] }}
										>
											<motion.div
												animate={{ rotate: [0, 360] }}
												transition={{ duration: 1, repeat: Infinity, ease: 'linear' }}
											>
												<RefreshCw className="w-3.5 h-3.5 text-blue-500" />
											</motion.div>
										</motion.div>
									</div>
									{/* Language icons */}
									<div className="flex gap-3 items-center">
										{[
											{ label: 'TS', color: 'bg-blue-100 text-blue-700 dark:bg-blue-900 dark:text-blue-300' },
											{ label: 'PY', color: 'bg-yellow-100 text-yellow-700 dark:bg-yellow-900 dark:text-yellow-300' },
											{ label: 'GO', color: 'bg-cyan-100 text-cyan-700 dark:bg-cyan-900 dark:text-cyan-300' },
											{ label: 'SH', color: 'bg-gray-100 text-gray-700 dark:bg-gray-700 dark:text-gray-300' },
											{ label: 'SQL', color: 'bg-purple-100 text-purple-700 dark:bg-purple-900 dark:text-purple-300' },
										].map((lang, i) => (
											<motion.span
												key={lang.label}
												className={`text-[10px] font-bold px-2 py-1 rounded ${lang.color}`}
												animate={{ scale: [1, 1, 1.15, 1, 1] }}
												transition={{ duration: 10, repeat: Infinity, delay: i * 2, times: [0, 0.05, 0.1, 0.15, 1] }}
											>
												{lang.label}
											</motion.span>
										))}
									</div>
									{/* Code preview */}
									<div className="mt-3 rounded-lg bg-gray-50 dark:bg-gray-900 p-3 font-mono text-[11px] text-gray-500 dark:text-gray-400 leading-relaxed">
										<div><span className="text-purple-500">def</span> main(order_id: <span className="text-blue-500">str</span>):</div>
										<div className="ml-4"><span className="text-gray-400">order</span> = db.get(order_id)</div>
										<div className="ml-4"><span className="text-purple-500">return</span> process(order)</div>
									</div>
								</div>
							</motion.div>

							{/* Data flowing down line 2 */}
							<motion.div
								className="absolute left-1/2 -translate-x-1/2 w-2 h-2 rounded-full bg-emerald-500"
								animate={{ top: [268, 340], opacity: [0, 1, 1, 0] }}
								transition={{ duration: 0.8, delay: 4.5, repeat: Infinity, repeatDelay: 9.2, times: [0, 0.1, 0.8, 1] }}
							/>

							{/* Step 3: Result */}
							<motion.div
								className="absolute top-[340px] left-1/2 -translate-x-1/2 w-[280px]"
								animate={{
									borderColor: ['rgb(229,231,235)', 'rgb(229,231,235)', 'rgb(34,197,94)', 'rgb(34,197,94)', 'rgb(229,231,235)'],
									boxShadow: ['0 1px 2px rgba(0,0,0,0.05)', '0 1px 2px rgba(0,0,0,0.05)', '0 0 16px rgba(34,197,94,0.3)', '0 0 16px rgba(34,197,94,0.3)', '0 1px 2px rgba(0,0,0,0.05)'],
								}}
								transition={{ duration: 6, repeat: Infinity, repeatDelay: 4, times: [0, 0.6, 0.65, 0.85, 1] }}
								style={{ borderWidth: 1, borderStyle: 'solid', borderRadius: '0.75rem' }}
							>
								<div className="rounded-xl bg-white dark:bg-gray-800 px-5 py-4">
									<div className="flex items-center gap-2 mb-2">
										<motion.div
											animate={{ opacity: [0, 0, 1, 1, 0] }}
											transition={{ duration: 6, repeat: Infinity, repeatDelay: 4, times: [0, 0.6, 0.65, 0.85, 1] }}
										>
											<CheckCircle className="w-4 h-4 text-emerald-500" />
										</motion.div>
										<span className="text-sm font-semibold text-gray-700 dark:text-gray-200">200 OK</span>
										<span className="text-xs text-gray-400 ml-auto">142ms</span>
									</div>
									<div className="rounded-lg bg-gray-50 dark:bg-gray-900 p-2 font-mono text-[11px] text-gray-500 dark:text-gray-400">
										{'{ "status": "processed", "id": "ord_82f" }'}
									</div>
								</div>
							</motion.div>
						</div>
					</div>
				</div>
			</div>

			<div className="mt-8 -mb-12 md:-mb-16"><LogoClouds /></div>

			{/* What is a script on Windmill? */}
			<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-24">
				<div className="grid grid-cols-1 md:grid-cols-2 gap-12 items-center">
					<motion.div {...fadeIn}>
						<h2 className="text-3xl sm:text-4xl font-bold text-gray-900 dark:text-white mb-4">
							What is a script on Windmill?
						</h2>
						<p className="text-base text-gray-600 dark:text-gray-300 mb-4 leading-relaxed">
							A script is a function in <Link to="/docs/getting_started/scripts_quickstart" className="text-blue-600 dark:text-blue-400 hover:underline">any language</Link> that you write, deploy and monitor directly on Windmill. Windmill handles the runtime, the dependencies, the UI and the API. You just write the logic.
						</p>
						<p className="text-base text-gray-600 dark:text-gray-300 mb-6 leading-relaxed">
							Each script automatically gets an <Link to="/docs/core_concepts/auto_generated_uis" className="text-blue-600 dark:text-blue-400 hover:underline">auto-generated form</Link>, webhook endpoints and real-time logs. Scripts can run on demand or through <Link to="/platform/triggers" className="text-blue-600 dark:text-blue-400 hover:underline">built-in triggers</Link>, and can be composed into <Link to="/use-cases/workflows" className="text-blue-600 dark:text-blue-400 hover:underline">workflows</Link> and <Link to="/use-cases/internal-tools" className="text-blue-600 dark:text-blue-400 hover:underline">apps</Link>.
						</p>
					</motion.div>
					<motion.div {...stagger(0.1)}>
						<LazyVideo
							className="rounded-2xl border border-gray-200 dark:border-gray-700 w-full shadow-lg"
							src="/img/platform/script-editor/platform-script-realtime-logs.webm"
						/>
					</motion.div>
				</div>
			</div>

			{/* Two ways to write scripts */}
			<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-16">
				<motion.div {...fadeIn} className="mb-16">
					<h2 className="text-3xl sm:text-4xl font-bold text-gray-900 dark:text-white mb-4">
						Two ways to write scripts
					</h2>
					<p className="text-lg text-gray-600 dark:text-gray-300">
						Write scripts directly in the Windmill UI or develop locally with your own editor. Both paths deploy to the same platform.
					</p>
				</motion.div>

				<div className="grid grid-cols-1 md:grid-cols-2 gap-8">
					{/* Windmill UI */}
					<motion.div {...stagger(0)} className="rounded-2xl border border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-800/50 p-8">
						<h3 className="text-xl font-semibold text-gray-900 dark:text-white mb-4">In the Windmill UI</h3>
						<p className="text-base text-gray-600 dark:text-gray-300 leading-relaxed mb-4">
							The built-in editor gives you autocomplete, LSP support, a live preview of the auto-generated UI, one-click deploy and <Link to="/docs/core_concepts/ai_generation" className="text-blue-600 dark:text-blue-400 hover:underline">AI assistance</Link> (BYOK).
						</p>
						<LazyVideo
							className="rounded-xl border border-gray-200 dark:border-gray-700 w-full mb-4"
							src="/img/platform/script-editor/platform-script-realtime-logs.webm"
						/>
						<Link
							to="/platform/script-editor"
							className="inline-flex items-center gap-2 text-sm text-blue-600 dark:text-blue-400 font-medium hover:underline !no-underline"
						>
							<BookOpen className="w-4 h-4" />
							Learn more about the script editor
						</Link>
					</motion.div>

					{/* Local development */}
					<motion.div {...stagger(0.1)} className="rounded-2xl border border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-800/50 p-8">
						<h3 className="text-xl font-semibold text-gray-900 dark:text-white mb-4">In your local editor</h3>
						<p className="text-base text-gray-600 dark:text-gray-300 leading-relaxed mb-4">
							Use the <Link to="/docs/advanced/cli" className="text-blue-600 dark:text-blue-400 hover:underline">CLI</Link>, the <Link to="/docs/script_editor/vs_code_scripts" className="text-blue-600 dark:text-blue-400 hover:underline">VS Code extension</Link> or AI coding tools like Claude Code. Scripts are standard files on disk, ready for any editor or AI agent.
						</p>
						<LazyVideo
							className="rounded-xl border border-gray-200 dark:border-gray-700 w-full mb-4"
							src="/img/platform/local-dev/platform-local-dev-vscode-extension.webm"
						/>
						<Link
							to="/platform/local-dev"
							className="inline-flex items-center gap-2 text-sm text-blue-600 dark:text-blue-400 font-medium hover:underline !no-underline"
						>
							<BookOpen className="w-4 h-4" />
							Learn more about local development
						</Link>
					</motion.div>
				</div>
			</div>

			{/* Production-grade platform */}
			<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-16">
				<motion.div {...fadeIn} className="mb-16">
					<h2 className="text-3xl sm:text-4xl font-bold text-gray-900 dark:text-white mb-4">
						Enterprise-grade by default
					</h2>
					<p className="text-lg text-gray-600 dark:text-gray-300">
						Every script runs within a platform built for enterprise requirements: immutable versioning, role-based access control, full audit trails, SSO and self-hosting. No extra setup needed.
					</p>
				</motion.div>

				{/* Versioning and deploy */}
				<div className="grid grid-cols-1 md:grid-cols-2 gap-12 items-center mb-20">
					<motion.div {...fadeIn}>
						<h3 className="text-xl font-semibold text-gray-900 dark:text-white mb-3">Versioning and deploy</h3>
						<p className="text-base text-gray-600 dark:text-gray-300 leading-relaxed">
							Every script version is immutable and addressable by hash. Deploy from the UI, the <Link to="/docs/advanced/cli" className="text-blue-600 dark:text-blue-400 hover:underline">CLI</Link> or <Link to="/docs/advanced/git_sync" className="text-blue-600 dark:text-blue-400 hover:underline">Git sync</Link>. Roll back to any previous version instantly. Sync your workspace with <Link to="/docs/advanced/deploy_gh_gl" className="text-blue-600 dark:text-blue-400 hover:underline">GitHub or GitLab</Link> and use your existing code review workflows.
						</p>
					</motion.div>
					<motion.div {...stagger(0.1)}>
						<img
							src="/img/money-pages/deploy.webp"
							alt="Script deployment interface"
							className="rounded-2xl border border-gray-200 dark:border-gray-700 w-full shadow-lg"
							loading="lazy"
						/>
					</motion.div>
				</div>

				{/* Observability (reversed) */}
				<div className="grid grid-cols-1 md:grid-cols-2 gap-12 items-center mb-20">
					<motion.div {...stagger(0.1)} className="order-2 md:order-1">
						<img
							src="/img/money-pages/monitoring.webp"
							alt="Script monitoring and logs"
							className="rounded-2xl border border-gray-200 dark:border-gray-700 w-full shadow-lg"
							loading="lazy"
						/>
					</motion.div>
					<motion.div {...fadeIn} className="order-1 md:order-2">
						<h3 className="text-xl font-semibold text-gray-900 dark:text-white mb-3">Full observability</h3>
						<p className="text-base text-gray-600 dark:text-gray-300 leading-relaxed">
							Every run is logged with inputs, outputs, duration and status. Real-time <Link to="/docs/core_concepts/jobs" className="text-blue-600 dark:text-blue-400 hover:underline">execution logs</Link> stream as your script runs. Set up <Link to="/docs/core_concepts/error_handling" className="text-blue-600 dark:text-blue-400 hover:underline">error handlers</Link> to send alerts on failure. <Link to="/docs/core_concepts/audit_logs" className="text-blue-600 dark:text-blue-400 hover:underline">Audit trails</Link> track who ran what and when.
						</p>
					</motion.div>
				</div>

				{/* RBAC and permissions */}
				<div className="grid grid-cols-1 md:grid-cols-2 gap-12 items-center mb-20">
					<motion.div {...fadeIn}>
						<h3 className="text-xl font-semibold text-gray-900 dark:text-white mb-3">RBAC and permissions</h3>
						<p className="text-base text-gray-600 dark:text-gray-300 leading-relaxed">
							Control who can view, edit and run each script with <Link to="/docs/core_concepts/roles_and_permissions" className="text-blue-600 dark:text-blue-400 hover:underline">role-based access control</Link>. Organize scripts into <Link to="/docs/core_concepts/groups_and_folders" className="text-blue-600 dark:text-blue-400 hover:underline">folders</Link> with group-level permissions. Authenticate users with SSO. Every action is recorded in the audit log.
						</p>
					</motion.div>
					<motion.div {...stagger(0.1)}>
						{/* placeholder: screenshot of script permissions panel showing folder permissions, group access and RBAC settings */}
						<img
							src="/img/money-pages/secrets.webp"
							alt="Script permissions and secrets"
							className="rounded-2xl border border-gray-200 dark:border-gray-700 w-full shadow-lg"
							loading="lazy"
						/>
					</motion.div>
				</div>

			</div>

			{/* Scripts as building blocks */}
			<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-16">
				<motion.div {...fadeIn} className="mb-16">
					<h2 className="text-3xl sm:text-4xl font-bold text-gray-900 dark:text-white mb-4">
						Scripts as building blocks
					</h2>
					<p className="text-lg text-gray-600 dark:text-gray-300">
						Every script you write can be reused across the platform. Use them standalone or compose them into larger systems.
					</p>
				</motion.div>

				<div className="grid grid-cols-1 md:grid-cols-3 gap-8">
					<motion.div {...stagger(0)} className="rounded-2xl border border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-800/50 p-8">
						<h3 className="text-xl font-semibold text-gray-900 dark:text-white mb-3">Workflows</h3>
						<p className="text-base text-gray-600 dark:text-gray-300 leading-relaxed mb-4">
							Chain scripts into DAG-based flows with branching, loops, retries and approval steps. Each script becomes a step that passes its output to the next.
						</p>
						<Link
							to="/use-cases/workflows"
							className="inline-flex items-center gap-2 text-sm text-blue-600 dark:text-blue-400 font-medium hover:underline !no-underline"
						>
							<BookOpen className="w-4 h-4" />
							Explore workflows
						</Link>
					</motion.div>

					<motion.div {...stagger(0.08)} className="rounded-2xl border border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-800/50 p-8">
						<h3 className="text-xl font-semibold text-gray-900 dark:text-white mb-3">Data pipelines</h3>
						<p className="text-base text-gray-600 dark:text-gray-300 leading-relaxed mb-4">
							Use scripts as extraction, transformation or loading steps in data pipelines. Connect to any database or API and process data at scale.
						</p>
						<Link
							to="/use-cases/data-pipelines"
							className="inline-flex items-center gap-2 text-sm text-blue-600 dark:text-blue-400 font-medium hover:underline !no-underline"
						>
							<BookOpen className="w-4 h-4" />
							Explore data pipelines
						</Link>
					</motion.div>

					<motion.div {...stagger(0.16)} className="rounded-2xl border border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-800/50 p-8">
						<h3 className="text-xl font-semibold text-gray-900 dark:text-white mb-3">Internal apps</h3>
						<p className="text-base text-gray-600 dark:text-gray-300 leading-relaxed mb-4">
							Use scripts as backends for internal tools. Build a UI with the app editor and wire buttons, tables and forms to your scripts.
						</p>
						<Link
							to="/use-cases/internal-tools"
							className="inline-flex items-center gap-2 text-sm text-blue-600 dark:text-blue-400 font-medium hover:underline !no-underline"
						>
							<BookOpen className="w-4 h-4" />
							Explore internal tools
						</Link>
					</motion.div>
				</div>
			</div>

			{/* Examples: the core of the page */}
			<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-16">
				<motion.div {...fadeIn} className="mb-16">
					<h2 className="text-3xl sm:text-4xl font-bold text-gray-900 dark:text-white mb-4">
						What you can build with a script
					</h2>
					<p className="text-lg text-gray-600 dark:text-gray-300">
						A Windmill script is a function that takes typed inputs and returns a result. What makes it powerful is the platform around it: triggers, permissions, logging, retries and a UI generated from the function signature. Here's what teams build across industries.
					</p>
				</motion.div>

				<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
					{scriptExamples.map((example, i) => (
						<motion.div
							key={example.title}
							{...stagger(i * 0.08)}
							className="rounded-xl border border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-800/50 p-6"
						>
							<span className="text-xs font-medium text-blue-600 dark:text-blue-400 uppercase tracking-wide">{example.category}</span>
							<h3 className="text-base font-semibold text-gray-900 dark:text-white mt-2 mb-2">{example.title}</h3>
							<p className="text-sm text-gray-600 dark:text-gray-400 leading-relaxed">{example.desc}</p>
						</motion.div>
					))}
				</div>
			</div>

			{/* Case studies */}
			<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-16">
				<motion.div {...fadeIn} className="mb-12">
					<h2 className="text-3xl sm:text-4xl font-bold text-gray-900 dark:text-white mb-4">
						Scripts in production
					</h2>
					<p className="text-lg text-gray-600 dark:text-gray-300">
						From cybersecurity to synthetic monitoring, teams rely on Windmill scripts for their most critical operations.
					</p>
				</motion.div>
				<div className="grid grid-cols-1 md:grid-cols-2 gap-8">
					<CaseStudyCard
						caseStudy={pantherLabsCaseStudy}
						title="How Panther Labs automates customer provisioning with Windmill scripts"
						description="Panther Labs uses Windmill scripts for customer provisioning, AWS account management and infrastructure deployment across 16 workers. Scripts serve as the building blocks of their platform operations."
						from="scripts"
					/>
					<CaseStudyCard
						caseStudy={cfaInstituteCaseStudy}
						title="How CFA Institute runs synthetic monitoring scripts every 5 minutes"
						description="CFA Institute schedules 25 synthetic monitoring scripts that run every 5 minutes with ChromeDriver, catching production regressions before users do."
						from="scripts"
					/>
				</div>
			</div>

			{/* More use cases */}
			<UseCaseCarousel
				current="scripts"
				subtitle="Scripts are the building blocks. The same platform powers workflows, internal tools, data pipelines, AI agents and scheduled tasks."
			/>

			{/* FAQ */}
			<UseCaseFAQ faqs={[
				{ question: 'What languages does Windmill support for scripts?', textAnswer: 'Windmill supports TypeScript (Deno and Bun), Python, Go, Bash, SQL (PostgreSQL, MySQL, MS SQL, BigQuery, Snowflake), REST, GraphQL, PowerShell, Rust, PHP, C#, Ansible, Nushell and any Docker container. Each script runs in its own isolated sandbox with automatic dependency resolution.' },
				{ question: 'How do I trigger a Windmill script?', textAnswer: 'Every script automatically gets sync and async webhook URLs. You can also schedule scripts with cron, expose them as HTTP routes, or trigger them from Postgres CDC, Kafka, NATS, SQS, MQTT, GCP Pub/Sub, email or WebSocket events. Scripts can also be run manually from the auto-generated UI or via the CLI and API.' },
				{ question: 'Can I use scripts as API endpoints?', textAnswer: 'Yes. Every Windmill script automatically gets a webhook URL that accepts POST requests. You can also create custom HTTP routes with path parameters and method routing. Scripts can return JSON, control HTTP status codes and set custom response headers.' },
				{ question: 'How are scripts different from workflows in Windmill?', textAnswer: 'Scripts are single units of code that take inputs and return outputs. Workflows chain multiple scripts together with branching, loops, error handling and approval steps. Scripts are the building blocks that can be used standalone or composed into workflows.' },
				{ question: 'Does Windmill handle script dependencies automatically?', textAnswer: 'Yes. Windmill parses import statements in your scripts and automatically resolves and caches dependencies. You can pin specific versions in your imports. Dependencies are cached across runs for fast cold starts.' },
				{ question: 'Can I self-host Windmill?', textAnswer: 'Yes. Windmill is open source (AGPLv3) and can be self-hosted on your own infrastructure, from a single-node VPS to large Kubernetes clusters. The Community Edition is free with unlimited scripts and runs. Enterprise features like SSO, audit logs and priority support require a paid plan.' },
			]} />

			{/* CTA */}
			<ProductCTA />
		</div>
	);
}
