---
title: "How to build AI agents with Windmill"
description: "How do I build AI agents with Windmill? Add agent steps to flows with tool calling, multi-provider LLM support, structured outputs and full observability."
---

import React from 'react';
import Link from '@docusaurus/Link';
import { motion } from 'framer-motion';
import { ArrowRight, Bot, Wrench, Eye, MessageSquare, Plug, Settings, Database } from 'lucide-react';

export const fadeIn = {
	initial: { opacity: 0, y: 30 },
	whileInView: { opacity: 1, y: 0 },
	viewport: { once: true },
	transition: { duration: 0.5 }
};

export const stagger = (delay = 0) => ({
	...fadeIn,
	transition: { duration: 0.5, delay }
});

export default function AiAgentsContent({ title, description }) {
	return (
		<div className="pt-32 max-w-full">
			{/* Hero */}
			<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 pt-16 pb-8">
				<div className="grid grid-cols-1 md:grid-cols-2 gap-12 items-center">
				<motion.div {...fadeIn}>
					<h1 className="!text-4xl sm:!text-6xl !font-semibold !tracking-tight text-gray-900 dark:text-white mb-6">
						Build AI agents with real tool calling
					</h1>
					<p className="text-xl text-gray-600 dark:text-gray-100 mb-8">
						Add LLM-powered agent steps to your flows. The agent calls any Windmill script as a tool, queries databases, triggers workflows and returns structured outputs — with every tool call logged and observable.
					</p>
					<div className="flex flex-col sm:flex-row gap-4">
						<a
							href="https://app.windmill.dev"
							target="_blank"
							rel="noopener noreferrer"
							className="inline-flex items-center gap-2 px-6 py-3 rounded-lg bg-blue-600 text-white font-medium hover:bg-blue-700 transition-colors !no-underline"
						>
							Try Windmill free
							<ArrowRight className="w-4 h-4" />
						</a>
						<Link
							to="/docs/core_concepts/ai_agents"
							className="inline-flex items-center gap-2 px-6 py-3 rounded-lg border border-gray-300 dark:border-gray-600 text-gray-700 dark:text-gray-200 font-medium hover:border-gray-400 dark:hover:border-gray-500 transition-colors !no-underline"
						>
							Read the docs
						</Link>
					</div>
					<div className="mt-8 flex flex-col gap-3">
						<div className="flex items-center gap-3 text-base text-gray-600 dark:text-gray-100">
							<span className="text-blue-500 font-bold">✓</span>
							<span><span className="text-blue-500">Any Windmill script as a tool</span> — typed parameters become the tool schema</span>
						</div>
						<div className="flex items-center gap-3 text-base text-gray-600 dark:text-gray-100">
							<span className="text-blue-500 font-bold">✓</span>
							<span><span className="text-blue-500">OpenAI, Anthropic, Mistral, Gemini</span> and 10+ providers</span>
						</div>
						<div className="flex items-center gap-3 text-base text-gray-600 dark:text-gray-100">
							<span className="text-blue-500 font-bold">✓</span>
							<span><span className="text-blue-500">Full observability</span> — every tool call logged as a flow node</span>
						</div>
						<div className="flex items-center gap-3 text-base text-gray-600 dark:text-gray-100">
							<span className="text-blue-500 font-bold">✓</span>
							<span><span className="text-blue-500">MCP integration</span> with auto-discovered tools and OAuth</span>
						</div>
					</div>
				</motion.div>

				<div className="hidden md:flex items-center justify-center">
					<div className="relative w-[480px] h-[480px]">
						{/* Concentric pulse rings */}
						{[160, 200, 240].map((r, i) => (
							<motion.div key={`ring-${i}`} className="absolute rounded-full border border-blue-200 dark:border-blue-800/40"
								style={{ width: r * 2, height: r * 2, left: 240 - r, top: 240 - r }}
								animate={{ opacity: [0.15, 0.35, 0.15], scale: [1, 1.03, 1] }}
								transition={{ duration: 3, delay: i * 0.6, repeat: Infinity }}
							/>
						))}
						{/* Center Bot icon */}
						<div className="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 z-10">
							<motion.div className="w-20 h-20 rounded-2xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center shadow-lg"
								animate={{ scale: [1, 1.05, 1] }}
								transition={{ duration: 3, repeat: Infinity }}
							>
								<Bot className="w-10 h-10 text-white" />
							</motion.div>
						</div>
						{/* Tool nodes arranged in a circle */}
						{[
							{ Icon: Wrench, label: 'Script tool', angle: -90 },
							{ Icon: Plug, label: 'MCP', angle: -30 },
							{ Icon: MessageSquare, label: 'Chat', angle: 30 },
							{ Icon: Eye, label: 'Observe', angle: 90 },
							{ Icon: Settings, label: 'Structured', angle: 150 },
							{ Icon: Database, label: 'Database', angle: 210 },
						].map((tool, i) => {
							const rad = (tool.angle * Math.PI) / 180;
							const r = 170;
							const x = 240 + r * Math.cos(rad);
							const y = 240 + r * Math.sin(rad);
							return (
								<React.Fragment key={tool.label}>
									{/* Connection line */}
									<svg className="absolute inset-0 w-full h-full" style={{ pointerEvents: 'none', zIndex: 1 }}>
										<line x1={240} y1={240} x2={x} y2={y}
											className="stroke-gray-200 dark:stroke-gray-700/50" strokeWidth={1.5} />
										{/* Pulse dot traveling from center to tool */}
										<motion.circle r={4} className="fill-blue-400 dark:fill-blue-300" style={{ opacity: 0.8 }}
											animate={{ cx: [240, x], cy: [240, y] }}
											transition={{ duration: 2, delay: i * 0.8, repeat: Infinity, repeatDelay: 4, ease: 'easeInOut' }}
										/>
									</svg>
									{/* Tool node */}
									<motion.div className="absolute z-[2]"
										style={{ left: x, top: y, transform: 'translate(-50%, -50%)' }}
										initial={{ opacity: 0, scale: 0.8 }}
										animate={{ opacity: 1, scale: 1 }}
										transition={{ duration: 0.5, delay: i * 0.12 }}
									>
										<div className="flex flex-col items-center gap-1.5">
											<div className="w-14 h-14 rounded-xl bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 shadow-sm flex items-center justify-center">
												<tool.Icon className="w-6 h-6 text-blue-600 dark:text-blue-400" />
											</div>
											<span className="text-xs font-medium text-gray-500 dark:text-gray-400">{tool.label}</span>
										</div>
									</motion.div>
								</React.Fragment>
							);
						})}
					</div>
				</div>
			</div>
			</div>

			{/* Multi-provider */}
			<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-24">
				<motion.div {...fadeIn} className="mb-16">
					<h2 className="text-3xl sm:text-4xl font-bold text-gray-900 dark:text-white mb-4">
						Any LLM provider, one interface
					</h2>
					<p className="text-lg text-gray-600 dark:text-gray-300">
						Switch providers or models without changing your agent logic. API keys are stored as <Link to="/docs/core_concepts/resources_and_types" className="text-blue-600 dark:text-blue-400 hover:underline">resources</Link> and reused across flows.
					</p>
				</motion.div>
				<motion.div {...fadeIn}>
					<div className="flex flex-wrap gap-3">
						{['OpenAI', 'Anthropic', 'Azure OpenAI', 'Mistral', 'DeepSeek', 'Google AI (Gemini)', 'Groq', 'OpenRouter', 'Together AI', 'Custom endpoints'].map((provider) => (
							<span key={provider} className="px-4 py-2 rounded-lg bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 text-gray-700 dark:text-gray-300 text-sm font-medium">
								{provider}
							</span>
						))}
					</div>
				</motion.div>
			</div>

			{/* Key capabilities */}
			<div className="bg-gray-50 dark:bg-gray-900/50">
				<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-24">
					<motion.div {...fadeIn} className="mb-16">
						<h2 className="text-3xl sm:text-4xl font-bold text-gray-900 dark:text-white mb-4">
							Agents that actually do things
						</h2>
						<p className="text-lg text-gray-600 dark:text-gray-300">
							Not just chat. Windmill agents call tools, execute code and interact with your infrastructure.
						</p>
					</motion.div>

					<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
						{[
							{ icon: Wrench, title: 'Scripts as tools', desc: 'Any Windmill script becomes a callable tool. The script\'s parameter types and descriptions are the tool schema.' },
							{ icon: Plug, title: 'MCP integration', desc: 'Connect to MCP servers. Tools are auto-discovered and available to the agent with OAuth and custom auth support.' },
							{ icon: MessageSquare, title: 'Chat mode', desc: 'Run flows with agent steps as a conversational UI. Users send messages, the agent reasons and streams results.' },
							{ icon: Eye, title: 'Full observability', desc: 'Every tool call appears as a node on the flow graph. Inspect inputs, outputs and the full message history.' },
							{ icon: Settings, title: 'Structured outputs', desc: 'Define a JSON Schema for responses. The agent returns predictable, typed outputs for downstream steps.' },
							{ icon: Bot, title: 'Memory management', desc: 'Auto memory (Windmill manages history) or manual (you provide message history in OpenAI format).' },
						].map((item, i) => (
							<motion.div key={item.title} {...stagger(i * 0.08)} className="p-6 rounded-2xl border border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-950">
								<div className="w-12 h-12 rounded-xl bg-blue-100 dark:bg-blue-900/30 flex items-center justify-center mb-4">
									<item.icon className="w-6 h-6 text-blue-600 dark:text-blue-400" />
								</div>
								<h3 className="text-xl font-semibold text-gray-900 dark:text-white mb-2">{item.title}</h3>
								<p className="text-gray-600 dark:text-gray-300">{item.desc}</p>
							</motion.div>
						))}
					</div>
				</div>
			</div>

			{/* Tool sources */}
			<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-24">
				<motion.div {...fadeIn} className="mb-16">
					<h2 className="text-3xl sm:text-4xl font-bold text-gray-900 dark:text-white mb-4">
						Three ways to add tools
					</h2>
				</motion.div>

				<div className="grid grid-cols-1 md:grid-cols-3 gap-8">
					{[
						{ step: '01', title: 'Inline scripts', desc: 'Write custom tools directly within the flow. Full code editor with AI assistance.' },
						{ step: '02', title: 'Workspace scripts', desc: 'Reuse existing scripts from your workspace. Already tested, versioned and deployed.' },
						{ step: '03', title: 'Hub scripts', desc: 'Pre-built tools from the Windmill Hub for common integrations — Slack, GitHub, databases and more.' },
					].map((item, i) => (
						<motion.div key={item.step} {...stagger(i * 0.1)} className="relative">
							<span className="text-6xl font-bold text-blue-100 dark:text-blue-900/40">{item.step}</span>
							<h3 className="text-xl font-semibold text-gray-900 dark:text-white mt-2 mb-2">{item.title}</h3>
							<p className="text-gray-600 dark:text-gray-300">{item.desc}</p>
						</motion.div>
					))}
				</div>
			</div>

			{/* CTA */}
			<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-24">
				<motion.div {...fadeIn} className="text-center py-16 px-8 rounded-3xl bg-gradient-to-br from-blue-50 to-blue-100 dark:from-blue-950 dark:to-gray-900 border border-blue-200 dark:border-blue-800">
					<h2 className="text-3xl font-bold text-gray-900 dark:text-white mb-4">Start building AI agents today</h2>
					<p className="text-gray-600 dark:text-gray-300 mb-8 max-w-2xl mx-auto text-lg">
						Get started for free on Windmill Cloud or self-host the open-source version.
					</p>
					<div className="flex flex-col sm:flex-row gap-4 justify-center">
						<a
							href="https://app.windmill.dev"
							target="_blank"
							rel="noopener noreferrer"
							className="inline-flex items-center gap-2 px-6 py-3 rounded-lg bg-blue-600 text-white font-medium hover:bg-blue-700 transition-colors !no-underline"
						>
							Try Windmill free
							<ArrowRight className="w-4 h-4" />
						</a>
						<Link
							to="/pricing"
							className="inline-flex items-center gap-2 px-6 py-3 rounded-lg border border-gray-300 dark:border-gray-600 text-gray-700 dark:text-gray-200 font-medium hover:border-gray-400 dark:hover:border-gray-500 transition-colors !no-underline"
						>
							View pricing
						</Link>
					</div>
				</motion.div>
			</div>
		</div>
	);
}
