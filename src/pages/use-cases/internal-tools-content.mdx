---
title: "How to build internal tools with Windmill"
description: "How do I build internal tools with Windmill? Write backend scripts in TypeScript, Python or SQL, store data in built-in tables, and deploy a React, Vue or Svelte frontend on a production engine."
---

import React, { useState } from 'react';
import Link from '@docusaurus/Link';
import { motion } from 'framer-motion';
import { ArrowRight, Code2, Database, LayoutGrid, Palette, Lock, Eye, Layers, Plug, Terminal, FileCode, Play, BookOpen, Cpu, Wrench, GitBranch, Table2, HardDrive, Globe, Bot, Shield, Activity } from 'lucide-react';
import { DotLottiePlayer } from '@dotlottie/react-player';
import UseCaseFAQ from '../../components/use-cases/UseCaseFAQ';

export const fadeIn = {
	initial: { opacity: 0, y: 30 },
	whileInView: { opacity: 1, y: 0 },
	viewport: { once: true },
	transition: { duration: 0.5 }
};

export const stagger = (delay = 0) => ({
	...fadeIn,
	transition: { duration: 0.5, delay }
});

{/* ── Hero animation: dashboard with monitoring overlay ── */}

export const logEntries = [
	{ script: 'sync_users', duration: '120ms', status: 'success' },
	{ script: 'generate_report', duration: '2.4s', status: 'success' },
	{ script: 'onboard_user', duration: '890ms', status: 'success' },
	{ script: 'backup_db', duration: '4.1s', status: 'success' },
	{ script: 'send_invoices', duration: '340ms', status: 'error', retry: '1/3' },
	{ script: 'refresh_dashboard', duration: '67ms', status: 'success' },
	{ script: 'send_invoices', duration: '290ms', status: 'success', retry: '2/3' },
	{ script: 'deploy_staging', duration: '12.3s', status: 'success' },
	{ script: 'check_permissions', duration: '45ms', status: 'success' },
	{ script: 'etl_daily_metrics', duration: '8.7s', status: 'success' },
];

export function HeroAnimation() {
	return (
		<div className="relative w-[520px] h-[480px]">
			{/* Lottie SaaS dashboard */}
			<div className="rounded-2xl overflow-hidden border border-gray-200 dark:border-gray-700 shadow-xl bg-white dark:bg-gray-900">
				<DotLottiePlayer
					src="/img/money-pages/saas-dashboard.lottie"
					loop
					autoplay
					style={{ width: '100%', height: 360 }}
				/>
			</div>

			{/* Floating monitoring overlay */}
			<motion.div
				className="absolute -bottom-4 -right-4 w-[280px] z-20"
				initial={{ opacity: 0, y: 20 }}
				animate={{ opacity: 1, y: 0 }}
				transition={{ duration: 0.6, delay: 0.8 }}
			>
				<div className="rounded-xl border border-gray-200 dark:border-gray-700 bg-white/95 dark:bg-gray-900/95 backdrop-blur-sm overflow-hidden shadow-lg">
					<div className="flex items-center justify-between px-3 py-2 border-b border-gray-200 dark:border-gray-700 bg-gray-50 dark:bg-gray-800/50">
						<div className="flex items-center gap-2">
							<Eye className="w-3.5 h-3.5 text-blue-500" />
							<span className="text-[10px] font-semibold text-gray-500 dark:text-gray-400 uppercase tracking-wider">Live executions</span>
						</div>
						<div className="flex items-center gap-1.5">
							<span className="w-1.5 h-1.5 rounded-full bg-emerald-400 animate-pulse" />
							<span className="text-[10px] text-gray-400">live</span>
						</div>
					</div>
					<div className="relative h-[120px] overflow-hidden">
						<motion.div
							animate={{ y: [0, -(logEntries.length * 28)] }}
							transition={{ duration: logEntries.length * 2.5, repeat: Infinity, ease: 'linear' }}
						>
							{[...logEntries, ...logEntries].map((entry, i) => (
								<div key={i} className="flex items-center gap-2 px-3 h-[28px] text-[11px] font-mono border-b border-gray-100 dark:border-gray-800/50">
									<span className={entry.status === 'success' ? 'text-emerald-500' : 'text-red-400'}>
										{entry.status === 'success' ? '●' : '✕'}
									</span>
									<span className="text-gray-700 dark:text-gray-300 truncate flex-1">{entry.script}</span>
									{entry.retry && <span className="text-amber-500 text-[10px]">retry {entry.retry}</span>}
									<span className="text-gray-400 dark:text-gray-500 tabular-nums">{entry.duration}</span>
								</div>
							))}
						</motion.div>
					</div>
				</div>
			</motion.div>
		</div>
	);
}

{/* ── Feature panel component ── */}

export function FeaturePanel({ features }) {
	const [active, setActive] = useState(0);
	return (
		<div className="grid grid-cols-1 md:grid-cols-[280px_1fr] gap-6">
			{/* Left: clickable list */}
			<div className="flex flex-col gap-2">
				{features.map((feat, i) => (
					<button
						key={feat.title}
						onClick={() => setActive(i)}
						className={`flex items-center gap-3 px-4 py-3 rounded-xl text-left transition-colors border ${
							i === active
								? 'bg-blue-600 text-white border-blue-600'
								: 'bg-white dark:bg-gray-950 text-gray-700 dark:text-gray-300 border-gray-200 dark:border-gray-700 hover:border-gray-300 dark:hover:border-gray-600'
						}`}
					>
						<feat.icon className={`w-5 h-5 flex-shrink-0 ${i === active ? 'text-white' : 'text-blue-500'}`} />
						<span className="font-medium text-sm">{feat.title}</span>
					</button>
				))}
			</div>

			{/* Right: selected content */}
			<div className="p-8 rounded-2xl border border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-950 relative overflow-hidden">
				{features.map((feat, i) => (
					<div
						key={feat.title}
						className={`transition-opacity duration-200 ${i === active ? 'opacity-100 relative' : 'opacity-0 absolute inset-0 pointer-events-none p-8'}`}
					>
						<div className="flex items-center gap-3 mb-4">
							<div className="w-10 h-10 rounded-lg bg-blue-100 dark:bg-blue-900/30 flex items-center justify-center">
								<feat.icon className="w-5 h-5 text-blue-600 dark:text-blue-400" />
							</div>
							<h3 className="text-2xl font-semibold text-gray-900 dark:text-white">{feat.title}</h3>
						</div>
						<p className="text-gray-600 dark:text-gray-300 text-lg mb-4">{feat.desc}</p>
						{feat.image ? (
							<img src={feat.image} alt={feat.title} className="rounded-lg border border-gray-200 dark:border-gray-700 w-full" />
						) : (
							<div className="w-full h-48 rounded-lg bg-gray-100 dark:bg-gray-800 border border-gray-200 dark:border-gray-700 flex items-center justify-center">
								<span className="text-gray-400 dark:text-gray-500 text-sm">Screenshot coming soon</span>
							</div>
						)}
						{feat.docLink && (
							<div className="flex justify-end mt-3">
								<Link to={feat.docLink} className="inline-flex items-center gap-1.5 text-blue-600 dark:text-blue-400 text-sm font-medium hover:underline !no-underline">
									<BookOpen className="w-4 h-4" /> Read the docs
								</Link>
							</div>
						)}
					</div>
				))}
			</div>
		</div>
	);
}

{/* ── Three-tab card content ── */}

export const backendCards = [
	{ title: '20+ languages', desc: <span>Write backend logic in <Link to="/docs/getting_started/scripts_quickstart" className="text-blue-600 dark:text-blue-400 hover:underline">TypeScript, Python, Go, SQL, Bash</Link> and more. Each script runs server-side on dedicated <Link to="/docs/core_concepts/worker_groups" className="text-blue-600 dark:text-blue-400 hover:underline">workers</Link> with full resource isolation.</span>, image: '/img/money-pages/scripts-preview.webp', docLink: '/docs/getting_started/scripts_quickstart' },
	{ title: 'Workflow engine', desc: <span>Chain scripts into <Link to="/docs/flows/flow_editor" className="text-blue-600 dark:text-blue-400 hover:underline">flows</Link> with parallel branches, <Link to="/docs/getting_started/triggers" className="text-blue-600 dark:text-blue-400 hover:underline">triggers</Link> (cron, webhooks, Kafka, Postgres CDC), <Link to="/docs/flows/retries" className="text-blue-600 dark:text-blue-400 hover:underline">retries with exponential backoff</Link> and custom error handlers per step.</span>, image: '/img/money-pages/flow-preview.webp', docLink: '/docs/flows/flow_editor' },
	{ title: '100+ integrations, no boilerplate', desc: <span>Connect to PostgreSQL, Slack, GitHub, Stripe, REST APIs and <Link to="/docs/integrations/integrations_on_windmill" className="text-blue-600 dark:text-blue-400 hover:underline">100+ services</Link> out of the box. Credentials are stored as shared <Link to="/docs/core_concepts/resources_and_types" className="text-blue-600 dark:text-blue-400 hover:underline">resources</Link>, not hardcoded. No SDK wrappers or auth plumbing to write.</span>, image: null, docLink: '/docs/integrations/integrations_on_windmill' },
];

export const dataCards = [
	{ title: 'Connect to your databases', desc: <span>Connect to PostgreSQL, MySQL, BigQuery, Snowflake or any database via shared <Link to="/docs/core_concepts/resources_and_types" className="text-blue-600 dark:text-blue-400 hover:underline">resources</Link>. Query S3 files with <Link to="/docs/integrations/duckdb" className="text-blue-600 dark:text-blue-400 hover:underline">DuckDB</Link>, manage datasets with <Link to="/docs/core_concepts/persistent_storage/ducklake" className="text-blue-600 dark:text-blue-400 hover:underline">Ducklake</Link>. Zero-config connections.</span>, image: '/img/money-pages/ducklake.webp', docLink: '/docs/core_concepts/persistent_storage' },
	{ title: 'Data tables', desc: <span><Link to="/docs/core_concepts/persistent_storage/data_tables" className="text-blue-600 dark:text-blue-400 hover:underline">Built-in SQL storage</Link> with near-zero setup. Workspace-scoped, safe parameterized queries, no external database to spin up or manage. Perfect for CRUD apps, config stores and reference data.</span>, image: '/img/money-pages/ducklake.webp', docLink: '/docs/core_concepts/persistent_storage/data_tables' },
	{ title: 'Database studio', desc: <span>Explore and query your databases directly from the Windmill UI with the built-in <Link to="/docs/apps/4_app_configuration_settings/database_studio" className="text-blue-600 dark:text-blue-400 hover:underline">database studio</Link>. Browse tables, run queries and inspect results without switching tools.</span>, image: '/img/money-pages/ducklake.webp', docLink: '/docs/apps/4_app_configuration_settings/database_studio' },
];

export const frontendCards = [
	{ title: 'Full-code app builder', desc: <span><Link to="/docs/react_vue_svelte_apps/react" className="text-blue-600 dark:text-blue-400 hover:underline">Import your own React, Vue or Svelte app</Link>. Full control over the frontend with <Link to="/docs/apps/css_editor" className="text-blue-600 dark:text-blue-400 hover:underline">CSS editing</Link>, theming and no ceiling on what you can build.</span>, image: null, docLink: '/docs/react_vue_svelte_apps/react' },
	{ title: 'Connected to backend runnables', desc: <span>Your frontend apps connect to the same <Link to="/docs/getting_started/scripts_quickstart" className="text-blue-600 dark:text-blue-400 hover:underline">backend scripts</Link> and <Link to="/docs/core_concepts/persistent_storage/data_tables" className="text-blue-600 dark:text-blue-400 hover:underline">data tables</Link>. No separate API layer to build. Permissions, versioning and observability apply across the stack.</span>, image: null, docLink: '/docs/getting_started/scripts_quickstart' },
	{ title: 'Local development', desc: <span>Develop frontends locally with your own IDE and tools. Use <Link to="/docs/core_concepts/ai_agents" className="text-blue-600 dark:text-blue-400 hover:underline">Claude Code or Codex</Link> to generate React components and full apps. Sync and deploy via the <Link to="/docs/advanced/local_development" className="text-blue-600 dark:text-blue-400 hover:underline">Windmill CLI</Link>.</span>, image: null, docLink: '/docs/advanced/local_development' },
];

export function CardGrid({ cards }) {
	return (
		<div className="grid grid-cols-1 md:grid-cols-3 gap-6">
			{cards.map((card, i) => (
				<motion.div key={card.title} {...stagger(i * 0.1)} className="flex flex-col p-6 rounded-2xl border border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-950">
					<h3 className="text-lg font-semibold text-gray-900 dark:text-white mb-2">{card.title}</h3>
					<p className="text-sm text-gray-600 dark:text-gray-300 mb-4 flex-1">{card.desc}</p>
					{card.image ? (
						<div className="w-full h-44 rounded-lg border border-gray-200 dark:border-gray-700 overflow-hidden">
							<img src={card.image} alt={card.title} className="w-full h-full object-cover object-top" />
						</div>
					) : (
						<div className="w-full h-44 rounded-lg bg-gray-100 dark:bg-gray-800 border border-gray-200 dark:border-gray-700 flex items-center justify-center">
							<span className="text-gray-400 dark:text-gray-500 text-sm">Screenshot coming soon</span>
						</div>
					)}
					{card.docLink && (
						<div className="flex justify-end mt-3">
							<Link to={card.docLink} className="inline-flex items-center gap-1.5 text-blue-600 dark:text-blue-400 text-sm font-medium hover:underline !no-underline">
								<BookOpen className="w-4 h-4" /> Read the docs
							</Link>
						</div>
					)}
				</motion.div>
			))}
		</div>
	);
}

export function BuildIntegrateTabs() {
	const [activeTab, setActiveTab] = useState(0);
	const tabs = [
		{ icon: Code2, label: 'Backend' },
		{ icon: Database, label: 'Data' },
		{ icon: LayoutGrid, label: 'Frontend' },
	];
	const tabContent = [
		{ title: 'Write backend with no overhead', subtitle: <span>Scripts in <Link to="/docs/getting_started/scripts_quickstart" className="text-blue-600 dark:text-blue-400 hover:underline">TypeScript, Python, Go or SQL</Link> run as isolated production runnables with logging, retries and error handling built in. Chain them into <Link to="/docs/flows/flow_editor" className="text-blue-600 dark:text-blue-400 hover:underline">flows</Link> with triggers and branching.</span>, cards: backendCards },
		{ title: 'Connect to any database natively', subtitle: <span>PostgreSQL, MySQL, BigQuery, Snowflake or S3 via shared <Link to="/docs/core_concepts/resources_and_types" className="text-blue-600 dark:text-blue-400 hover:underline">resources</Link>. Built-in <Link to="/docs/core_concepts/persistent_storage/data_tables" className="text-blue-600 dark:text-blue-400 hover:underline">data tables</Link> and <Link to="/docs/integrations/duckdb" className="text-blue-600 dark:text-blue-400 hover:underline">DuckDB</Link>/<Link to="/docs/core_concepts/persistent_storage/ducklake" className="text-blue-600 dark:text-blue-400 hover:underline">Ducklake</Link> for datasets that live on the platform. Combined with the workflow engine, Windmill is also optimized for <Link to="/use-cases/data-pipelines" className="text-blue-600 dark:text-blue-400 hover:underline">heavy data pipelines</Link>.</span>, cards: dataCards },
		{ title: 'Build your own frontend', subtitle: <span>Build your frontend in <Link to="/docs/react_vue_svelte_apps/react" className="text-blue-600 dark:text-blue-400 hover:underline">React, Vue or Svelte</Link>. Your app connects directly to backend scripts and <Link to="/docs/core_concepts/persistent_storage/data_tables" className="text-blue-600 dark:text-blue-400 hover:underline">data tables</Link>, no API layer to build.</span>, cards: frontendCards },
	];
	return (
		<div>
			<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-24">
				<motion.div {...fadeIn} className="mb-8">
					<h2 className="text-3xl sm:text-4xl font-bold text-gray-900 dark:text-white mb-4">
						Backend, data and frontend in one platform
					</h2>
					<p className="text-lg text-gray-600 dark:text-gray-300">
						Full code flexibility to build what you need, built-in integrations to connect your resources. No wiring required.
					</p>
				</motion.div>
				{/* Tab buttons */}
				<div className="grid grid-cols-3 mb-12 border-b border-gray-200 dark:border-gray-700">
					{tabs.map((tab, i) => (
						<button
							key={tab.label}
							onClick={() => setActiveTab(i)}
							className={`flex items-center justify-center gap-2 px-6 py-3 text-base font-semibold border-b-2 transition-colors -mb-px ${
								i === activeTab
									? 'border-blue-600 text-blue-600 dark:text-blue-400 dark:border-blue-400'
									: 'border-transparent text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:hover:text-gray-300'
							}`}
						>
							<tab.icon className="w-5 h-5" />
							{tab.label}
						</button>
					))}
				</div>
				{/* Tab content */}
				{tabContent.map((content, i) => (
					activeTab === i && (
						<div key={content.title}>
							<div className="mb-12">
								<h2 className="text-2xl sm:text-3xl font-bold text-gray-900 dark:text-white mb-4">
									{content.title}
								</h2>
								<p className="text-lg text-gray-600 dark:text-gray-300">
									{content.subtitle}
								</p>
							</div>
							<CardGrid cards={content.cards} />
						</div>
					)
				))}
			</div>
		</div>
	);
}

{/* ── Monitor feature list ── */}

export const monitorFeatures = [
	{ icon: Eye, title: 'Full execution history', desc: <span>Every run is logged with input, output, duration, errors and resource usage. Search, filter and debug past <Link to="/docs/core_concepts/jobs" className="text-blue-600 dark:text-blue-400 hover:underline">executions</Link> across all scripts and apps.</span>, image: null, docLink: '/docs/core_concepts/jobs' },
	{ icon: Activity, title: 'Error handlers & retries', desc: <span>Configurable <Link to="/docs/flows/retries" className="text-blue-600 dark:text-blue-400 hover:underline">retry count with exponential backoff</Link>. Custom error handler scripts per step. Apps that don't break silently at 2am.</span>, image: null, docLink: '/docs/flows/retries' },
	{ icon: Lock, title: 'RBAC & SSO', desc: <span>Granular <Link to="/docs/core_concepts/roles_and_permissions" className="text-blue-600 dark:text-blue-400 hover:underline">permissions</Link> control who can build, view and execute apps. SSO with SAML, OAuth and LDAP.</span>, image: null, docLink: '/docs/core_concepts/roles_and_permissions' },
	{ icon: Shield, title: 'Audit logs', desc: 'Every action is tracked. Who ran what, when, with which inputs. Full audit trail for compliance and debugging.', image: null, docLink: '/docs/core_concepts/audit_logs' },
	{ icon: GitBranch, title: 'Version control & rollback', desc: <span>Every script is versioned. Deploy from the UI or via <Link to="/docs/advanced/deploy_gh_gl" className="text-blue-600 dark:text-blue-400 hover:underline">Git sync</Link> with GitHub or GitLab. Roll back to any previous version instantly.</span>, image: null, docLink: '/docs/advanced/deploy_gh_gl' },
];

{/* ── Main page ── */}

export default function InternalToolsContent({ title, description }) {
	return (
		<div className="pt-32 max-w-full">
			{/* Hero */}
			<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 pt-16 pb-8">
				<div className="grid grid-cols-1 md:grid-cols-2 gap-12 items-center">
					<motion.div {...fadeIn}>
						<h1 className="!text-4xl sm:!text-6xl !font-semibold !tracking-tight text-gray-900 dark:text-white mb-6">
							Build production-grade internal tools
						</h1>
						<p className="text-xl text-gray-600 dark:text-gray-100 mb-8">
							For platform teams who need to ship internal tools with production-grade observability, enterprise controls and zero infrastructure overhead.
						</p>
						<div className="flex flex-col sm:flex-row gap-4">
							<a
								href="https://app.windmill.dev"
								target="_blank"
								rel="noopener noreferrer"
								className="inline-flex items-center gap-2 px-6 py-3 rounded-lg bg-blue-600 text-white font-medium hover:bg-blue-700 transition-colors !no-underline"
							>
								Try Windmill free
								<ArrowRight className="w-4 h-4" />
							</a>
							<Link
								to="/docs/apps/app_editor"
								className="inline-flex items-center gap-2 px-6 py-3 rounded-lg border border-gray-300 dark:border-gray-600 text-gray-700 dark:text-gray-200 font-medium hover:border-gray-400 dark:hover:border-gray-500 transition-colors !no-underline"
							>
								Read the docs
							</Link>
						</div>
						<div className="mt-8 flex flex-col gap-3">
							<div className="flex items-center gap-3 text-base text-gray-600 dark:text-gray-100">
								<span className="text-blue-500 font-bold">✓</span>
								<span><span className="text-blue-500">Build</span> <Link to="/docs/getting_started/scripts_quickstart" className="text-blue-500 hover:underline !no-underline">backend</Link> in 20+ languages, <Link to="/docs/react_vue_svelte_apps/react" className="text-blue-500 hover:underline !no-underline">and React, Vue or Svelte</Link> frontends</span>
							</div>
							<div className="flex items-center gap-3 text-base text-gray-600 dark:text-gray-100">
								<span className="text-blue-500 font-bold">✓</span>
								<span>Native integrations with <Link to="/docs/integrations/duckdb" className="text-blue-500 hover:underline !no-underline">DuckDB</Link>, <Link to="/docs/core_concepts/persistent_storage/ducklake" className="text-blue-500 hover:underline !no-underline">Ducklake</Link>, S3 and any database</span>
							</div>
							<div className="flex items-center gap-3 text-base text-gray-600 dark:text-gray-100">
								<span className="text-blue-500 font-bold">✓</span>
								<span><span className="text-blue-500">Monitor</span> full <Link to="/docs/core_concepts/jobs" className="text-blue-500 hover:underline !no-underline">execution logs</Link>, error handlers, RBAC, SSO and audit trails</span>
							</div>
							<div className="flex items-center gap-3 text-base text-gray-600 dark:text-gray-100">
								<span className="text-blue-500 font-bold">✓</span>
								<span>One platform, no infrastructure to manage, <Link to="/docs/advanced/self_host" className="text-blue-500 hover:underline !no-underline">self-host</Link> or cloud</span>
							</div>
						</div>
					</motion.div>

					<div className="hidden md:flex items-center justify-center">
						<HeroAnimation />
					</div>
				</div>
			</div>

			{/* ═══════════════ 1. Build & Integrate (3 tabs) ═══════════════ */}
			<BuildIntegrateTabs />

			{/* ═══════════════ 2. Monitor ═══════════════ */}
			<div className="bg-gray-50 dark:bg-gray-900/50">
				<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-24">
					<motion.div {...fadeIn} className="mb-12">
						<h2 className="text-3xl sm:text-4xl font-bold text-gray-900 dark:text-white mb-4">
							Production-grade observability and enterprise controls
						</h2>
						<p className="text-lg text-gray-600 dark:text-gray-300">
							This is what vibe coding tools don't give you. Every execution is logged. Every error is caught. Every action is audited. RBAC, SSO and version control are built in.
						</p>
					</motion.div>
					<FeaturePanel features={monitorFeatures} />
				</div>
			</div>

			{/* ═══════════════ 4. No overhead — comparison table ═══════════════ */}
			<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-24">
				<motion.div {...fadeIn} className="mb-12">
					<h2 className="text-3xl sm:text-4xl font-bold text-gray-900 dark:text-white mb-4">
						One platform, no infrastructure overhead
					</h2>
					<p className="text-lg text-gray-600 dark:text-gray-300">
						No separate tools for backend, frontend, storage, monitoring and deployment. Windmill replaces the entire stack. <Link to="/docs/advanced/self_host" className="text-blue-600 dark:text-blue-400 hover:underline">Self-host</Link> or use Windmill Cloud.
					</p>
				</motion.div>

				<motion.div {...fadeIn} className="overflow-x-auto">
					<table className="w-full text-sm text-left border-collapse">
						<thead>
							<tr className="border-b-2 border-gray-200 dark:border-gray-700">
								<th className="py-4 pr-4 font-semibold text-gray-900 dark:text-white w-48"></th>
								<th className="py-4 px-4 font-semibold text-blue-600 dark:text-blue-400">Windmill</th>
								<th className="py-4 px-4 font-semibold text-gray-500 dark:text-gray-400">Retool / Appsmith</th>
								<th className="py-4 px-4 font-semibold text-gray-500 dark:text-gray-400">Lovable / Bolt</th>
								<th className="py-4 px-4 font-semibold text-gray-500 dark:text-gray-400">DIY (full code)</th>
							</tr>
						</thead>
						<tbody className="divide-y divide-gray-200 dark:divide-gray-700">
							{[
								['Backend logic', 'Full scripts in 20+ languages', 'Sandboxed JavaScript', 'AI-generated, no engine', 'Full control, manual setup'],
								['Frontend', 'React/Vue/Svelte on the platform', 'Drag-and-drop only', 'Full code (AI-generated)', 'Full code (manual)'],
								['Storage', 'Built-in data tables + any DB', 'Retool DB (limited) / none', 'You manage your own', 'You manage your own'],
								['Observability', 'Execution history, logs, error handlers', 'Basic logs', 'None', 'Build it yourself'],
								['Enterprise', 'RBAC, SSO, audit logs, self-host', 'Enterprise tier only', 'No', 'Build it yourself'],
								['AI integration', 'Claude Code / Codex in the IDE', 'Limited AI assist', 'Core product', 'External tools'],
								['Workflow engine', 'Flows, schedules, triggers, AI agents', 'Separate product / none', 'None', 'Build it yourself'],
								['Versioning', 'Git sync, rollback', 'Limited', 'Git (manual)', 'Git (manual)'],
							].map(([label, windmill, retool, lovable, diy]) => (
								<tr key={label}>
									<td className="py-3 pr-4 font-medium text-gray-900 dark:text-white">{label}</td>
									<td className="py-3 px-4 text-gray-700 dark:text-gray-200">{windmill}</td>
									<td className="py-3 px-4 text-gray-500 dark:text-gray-400">{retool}</td>
									<td className="py-3 px-4 text-gray-500 dark:text-gray-400">{lovable}</td>
									<td className="py-3 px-4 text-gray-500 dark:text-gray-400">{diy}</td>
								</tr>
							))}
						</tbody>
					</table>
				</motion.div>

				<motion.div {...fadeIn} className="mt-12 text-center py-8 px-6 rounded-2xl border-2 border-blue-200 dark:border-blue-800 bg-blue-50/50 dark:bg-blue-950/30">
					<p className="text-lg text-gray-600 dark:text-gray-300 max-w-3xl mx-auto">
						Internal tools built on Windmill connect to the rest of the platform: <Link to="/use-cases/scheduled-tasks" className="text-blue-600 dark:text-blue-400 hover:underline">scheduled tasks</Link>, <Link to="/use-cases/data-pipelines" className="text-blue-600 dark:text-blue-400 hover:underline">data pipelines</Link>, <Link to="/use-cases/ai-agents" className="text-blue-600 dark:text-blue-400 hover:underline">AI agents</Link> and <Link to="/use-cases/workflow-automation" className="text-blue-600 dark:text-blue-400 hover:underline">workflow automation</Link>.
					</p>
				</motion.div>
			</div>

			{/* ═══════════════ FAQ ═══════════════ */}
			<div className="bg-gray-50 dark:bg-gray-900/50">
				<UseCaseFAQ faqs={[
					{ question: 'How is Windmill different from Retool?', textAnswer: 'Windmill lets you write backend logic in 20+ languages (TypeScript, Python, Go, SQL, Bash and more) running server-side on dedicated workers. Retool limits you to sandboxed JavaScript. Windmill also includes built-in data tables, a workflow engine with schedules and triggers, Git-based version control, and full self-hosting support. You deploy your own React, Vue or Svelte frontend on the same platform, with a real execution engine underneath.' },
					{ question: 'How is Windmill different from Lovable?', textAnswer: 'Lovable generates full-stack apps with AI, but the result is a standalone codebase with no execution engine, observability or enterprise controls. Windmill gives you the same AI-powered development (via Claude Code and Codex integration) but runs your code on a production engine with execution history, error handlers, RBAC, SSO, audit logs and built-in data storage. Apps built on Windmill are production-grade, not prototypes.' },
					{ question: 'Can I use Claude Code to build internal tools on Windmill?', textAnswer: 'Yes. Windmill integrates Claude Code and Codex directly into the development experience. AI generates backend scripts and app configurations that deploy instantly on the Windmill platform with full observability, versioning and permissions. You vibe-code the logic, Windmill handles the production infrastructure.' },
					{ question: 'Do I need to set up an external database?', textAnswer: 'No. Windmill Data Tables provide built-in SQL storage with near-zero setup, workspace-scoped and with safe parameterized queries. For heavier workloads you can connect any external database (PostgreSQL, MySQL, BigQuery, Snowflake) via shared resources.' },
					{ question: 'Can I import my own React app into Windmill?', textAnswer: 'Yes. You can import full React, Vue or Svelte apps that run inside Windmill with access to the same backend scripts, data tables and permissions.' },
					{ question: 'Can I self-host Windmill?', textAnswer: 'Yes. Windmill is open source and can be self-hosted on your own infrastructure, including air-gapped deployments. The Community Edition is free. Enterprise features like SSO, audit logs and priority support require a paid plan.' },
				]} />
			</div>

			{/* ═══════════════ CTA ═══════════════ */}
			<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-24">
				<motion.div {...fadeIn} className="text-center py-16 px-8 rounded-3xl bg-gradient-to-br from-blue-50 to-blue-100 dark:from-blue-950 dark:to-gray-900 border border-blue-200 dark:border-blue-800">
					<h2 className="text-3xl font-bold text-gray-900 dark:text-white mb-4">Start building internal tools today</h2>
					<p className="text-gray-600 dark:text-gray-300 mb-8 max-w-2xl mx-auto text-lg">
						Get started for free on Windmill Cloud or <Link to="/docs/advanced/self_host" className="text-blue-600 dark:text-blue-400 hover:underline !no-underline">self-host</Link> the open-source version.
					</p>
					<div className="flex flex-col sm:flex-row gap-4 justify-center">
						<a
							href="https://app.windmill.dev"
							target="_blank"
							rel="noopener noreferrer"
							className="inline-flex items-center gap-2 px-6 py-3 rounded-lg bg-blue-600 text-white font-medium hover:bg-blue-700 transition-colors !no-underline"
						>
							Try Windmill free
							<ArrowRight className="w-4 h-4" />
						</a>
						<Link
							to="/pricing"
							className="inline-flex items-center gap-2 px-6 py-3 rounded-lg border border-gray-300 dark:border-gray-600 text-gray-700 dark:text-gray-200 font-medium hover:border-gray-400 dark:hover:border-gray-500 transition-colors !no-underline"
						>
							View pricing
						</Link>
					</div>
				</motion.div>
			</div>
		</div>
	);
}
